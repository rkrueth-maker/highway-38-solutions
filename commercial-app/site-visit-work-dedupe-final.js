(function(){
'use strict';
const BUILD='20260821-site-visit-work-dedupe-final-7-phone';
const C=window.H38_FIELD_VISIT_CORE;
let timer=0,forcedIdentity=null;
const text=value=>String(value==null?'':value).trim();
const payload=row=>row?.payload&&typeof row.payload==='object'?row.payload:row;
const value=(row,...keys)=>{const source=payload(row);for(const key of keys){if(source&&source[key]!==undefined&&source[key]!==null&&source[key]!=='')return source[key];}return'';};
const normalize=value=>text(value).toLowerCase().replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim();
const rows=name=>Array.isArray(window.state?.snapshot?.[name])?window.state.snapshot[name]:[];
const quotes=()=>rows('quotes');
const sessions=()=>rows('siteCaptureSessions').filter(row=>text(value(row,'Capture Session ID','captureSessionId')));
const quoteById=id=>quotes().find(row=>text(value(row,'Quote ID','quoteId'))===text(id))||null;
function serverIdentity(row){const sessionId=text(value(row,'Capture Session ID','captureSessionId')),visitId=text(value(row,'Site Visit ID','siteVisitId')),quoteId=text(value(row,'Quote ID','quoteId')),quote=quoteById(quoteId),customerId=text(value(row,'Customer ID','customerId')||value(quote,'Customer ID','customerId')),title=text(value(row,'Project Title','projectTitle')||value(quote,'Project Title','projectTitle')),scope=text(value(row,'Scope','scope')||value(quote,'Scope','scope')),status=text(value(row,'Status','status')||'IN_PROGRESS');return{row,sessionId,visitId,quoteId,customerId,businessId:text(value(row,'Business ID','businessId')||value(quote,'Business ID','businessId')||window.state?.businessId),title:normalize(title),projectTitle:title,scope,projectType:text(value(row,'Project Type','projectType')),status};}
function localAliasIdentity(identity){const status=text(identity?.status).toUpperCase();return /LOCAL[_ -]?DRAFT/.test(status)||(!identity?.quoteId&&!identity?.customerId&&Boolean(identity?.title));}
function identities(){const all=sessions().map(serverIdentity),canonicalTitles=new Set(all.filter(identity=>identity.title&&!localAliasIdentity(identity)&&(identity.quoteId||identity.customerId)).map(identity=>identity.title));return all.filter(identity=>!(localAliasIdentity(identity)&&identity.title&&canonicalTitles.has(identity.title)));}
function objectClues(row){const source=payload(row)||{};return{sessionId:text(value(source,'Capture Session ID','captureSessionId','sessionId')),visitId:text(value(source,'Site Visit ID','siteVisitId','visitId')),quoteId:text(value(source,'Quote ID','quoteId')),customerId:text(value(source,'Customer ID','customerId')),title:normalize(value(source,'Project Title','projectTitle')),local:/LOCAL[_ -]?DRAFT/i.test(text(value(source,'Status','status')))||/^LOCAL-/i.test(text(value(source,'Quote Number','quoteNumber')))};}
function compatible(clue,identity){if(clue.sessionId&&identity.sessionId!==clue.sessionId)return false;if(clue.visitId&&identity.visitId&&identity.visitId!==clue.visitId)return false;if(clue.quoteId&&identity.quoteId&&identity.quoteId!==clue.quoteId)return false;if(clue.customerId&&identity.customerId&&identity.customerId!==clue.customerId)return false;if(clue.title&&identity.title&&identity.title!==clue.title)return false;return true;}
function single(list){return list.length===1?list[0]:null;}
function resolveClues(clue){const all=identities();if(!all.length)return null;if(clue.sessionId){const exact=single(all.filter(identity=>identity.sessionId===clue.sessionId));if(exact)return exact;}if(clue.visitId){const match=single(all.filter(identity=>identity.visitId&&identity.visitId===clue.visitId));if(match)return match;}if(clue.quoteId){const match=single(all.filter(identity=>identity.quoteId&&identity.quoteId===clue.quoteId&&compatible(clue,identity)));if(match)return match;}if(clue.customerId&&clue.title){const match=single(all.filter(identity=>identity.customerId===clue.customerId&&identity.title===clue.title&&compatible(clue,identity)));if(match)return match;}if(clue.title){const match=single(all.filter(identity=>identity.title===clue.title));if(match)return match;}return null;}
function resolve(row){return resolveClues(objectClues(row));}
function sameLogical(local,identity){const clue=objectClues(local);if(clue.sessionId||clue.visitId||clue.quoteId||clue.customerId)return compatible(clue,identity);return!!clue.title&&clue.title===identity.title;}
function canonicalVisit(local,identity){const seed=local&&sameLogical(local,identity)?local:(typeof C?.blank==='function'?C.blank():{});return Object.assign({},seed,{businessId:identity.businessId||text(seed?.businessId),sessionId:identity.sessionId||text(seed?.sessionId),visitId:identity.visitId||text(seed?.visitId),quoteId:identity.quoteId||text(seed?.quoteId),customerId:identity.customerId||text(seed?.customerId),projectTitle:identity.projectTitle||text(seed?.projectTitle),projectType:identity.projectType||text(seed?.projectType),scope:identity.scope||text(seed?.scope),status:identity.status||text(seed?.status)||'IN_PROGRESS'});}
function installRestoreAuthority(){if(!C||typeof C.restore!=='function'||C.restore.__h38SiteVisitIdentityAuthority)return false;const base=C.restore,wrapped=async function(){const local=await base.apply(this,arguments),identity=forcedIdentity||resolve(local||{});return identity?canonicalVisit(local,identity):local;};wrapped.__h38SiteVisitIdentityAuthority=true;wrapped.__h38SiteVisitIdentityBase=base;C.restore=wrapped;return true;}
function installOpenAuthority(){const api=window.H38_FIELD_VISIT;if(!api||typeof api.open!=='function'||api.open.__h38SiteVisitIdentityAuthority)return false;installRestoreAuthority();const base=api.open,wrapped=async function(opts={}){const identity=resolve(opts)||resolve(C?.state?.visit||{});if(!identity)return base.apply(this,arguments);forcedIdentity=identity;try{return await base.call(this,Object.assign({},opts,{captureSessionId:identity.sessionId,sessionId:identity.sessionId,siteVisitId:identity.visitId,visitId:identity.visitId,quoteId:identity.quoteId,customerId:identity.customerId,projectTitle:identity.projectTitle,scope:identity.scope}));}finally{forcedIdentity=null;}};wrapped.__h38SiteVisitIdentityAuthority=true;wrapped.__h38SiteVisitIdentityBase=base;api.open=wrapped;return true;}
function workSurface(){const main=document.getElementById('mainContent');if(!main)return null;if(text(window.state?.page)==='work')return main;const heading=text(main.querySelector('.page-head h1,h1')?.textContent),body=text(main.textContent);if(/^(?:jobs|work(?:\s*&\s*tasks)?)$/i.test(heading)&&/site visits/i.test(body))return main;return null;}
function isWork(){return!!workSurface();}
function openLabel(button){return normalize(button?.textContent);}
function isOpenButton(button){const label=openLabel(button);return label==='open'||label==='open edit'||label.startsWith('open edit ');}
function rawServerTitle(raw){const normalized=normalize(raw),matches=identities().filter(identity=>identity.title&&normalized.includes(identity.title)),bySession=new Map(matches.map(identity=>[identity.sessionId,identity]));return bySession.size===1?Array.from(bySession.values())[0]:null;}
function hasCardIdentitySignal(node){const raw=text(node?.textContent);if(!raw)return false;if(rawServerTitle(raw))return true;if(/\b(?:LOCAL[_ -]?DRAFT|ATTACHED_TO_DRAFT_QUOTE|IN_PROGRESS|COMPLETE|COMPLETED)\b/i.test(raw))return true;return!!node?.querySelector?.('strong,[data-h38-site-visit-title],h3,h4');}
function isVisitCard(node){if(!(node instanceof Element))return false;const buttons=Array.from(node.querySelectorAll('button'));return buttons.some(isOpenButton)&&hasCardIdentitySignal(node);}
function cardForButton(button){const main=workSurface()||document.getElementById('mainContent');let node=button?.parentElement;while(node&&node!==main){if(isVisitCard(node))return node;node=node.parentElement;}return null;}
function visitCards(main=workSurface()){if(!main)return[];const found=[],seen=new Set();for(const button of Array.from(main.querySelectorAll('button')).filter(isOpenButton)){const card=cardForButton(button);if(card&&!seen.has(card)){seen.add(card);found.push(card);}}return found;}
function extractId(raw,prefix){const match=text(raw).match(new RegExp(`\\b${prefix}-[A-Z0-9-]{8,}\\b`,'i'));return match?match[0]:'';}
function rowClues(row){const buttons=Array.from(row.querySelectorAll('button')),dataset=[row,...buttons].reduce((out,node)=>Object.assign(out,node?.dataset||{}),{}),raw=text(row.textContent),strongs=Array.from(row.querySelectorAll('strong,[data-h38-site-visit-title],h3,h4')).map(node=>text(node.textContent)).filter(Boolean),markupTitle=strongs.find(item=>!/^(local[_ -]?draft|attached_to_|in_progress|draft|closed|complete|completed)/i.test(item))||'',server=rawServerTitle(raw),title=markupTitle||text(server?.projectTitle);return{sessionId:text(dataset.h38SiteVisitSessionId||dataset.h38CanonicalSessionId||dataset.captureSessionId||dataset.sessionId||extractId(raw,'SCAN')),visitId:text(dataset.siteVisitId||dataset.visitId||extractId(raw,'VISIT')),quoteId:text(dataset.quoteId||extractId(raw,'QUOTE')),customerId:text(dataset.customerId||extractId(raw,'CUSTOMER')),title:normalize(title),raw,local:/\bLOCAL[_ -]?DRAFT\b/i.test(raw)||/\blocal draft\b/i.test(raw)||/\bLOCAL-\d+/i.test(raw)};}
function resolveRow(row){const clue=rowClues(row);return resolveClues(clue)||rawServerTitle(clue.raw);}
function score(item){let result=0;if(item.clue.sessionId&&item.clue.sessionId===item.identity.sessionId)result+=100;if(item.clue.visitId&&item.clue.visitId===item.identity.visitId)result+=40;if(item.clue.quoteId&&item.clue.quoteId===item.identity.quoteId)result+=20;if(!item.clue.local)result+=10;return result;}
function reconcile(){clearTimeout(timer);timer=0;installOpenAuthority();const main=workSurface();if(!main)return 0;const visitRows=visitCards(main);if(!visitRows.length)return 0;const mapped=visitRows.map(row=>{const clue=rowClues(row),identity=resolveClues(clue)||rawServerTitle(clue.raw);return{row,clue,identity};}).filter(item=>item.identity),groups=new Map();for(const item of mapped){const key=item.identity.sessionId;if(!key)continue;if(!groups.has(key))groups.set(key,[]);groups.get(key).push(item);}let removed=0;for(const items of groups.values()){if(items.length<2){const keep=items[0];if(keep){keep.row.dataset.h38LogicalSiteVisit='canonical';keep.row.dataset.h38CanonicalSessionId=keep.identity.sessionId;}continue;}items.sort((a,b)=>score(b)-score(a));const keep=items[0];keep.row.dataset.h38LogicalSiteVisit='canonical';keep.row.dataset.h38CanonicalSessionId=keep.identity.sessionId;for(const duplicate of items.slice(1)){if(!duplicate.row.isConnected)continue;duplicate.row.remove();removed++;}}return removed;}
function schedule(delay=40){clearTimeout(timer);timer=setTimeout(reconcile,delay);}
function arm(){installOpenAuthority();if(!isWork())return;schedule(0);[90,260,700,1400,2600,4500].forEach(delay=>setTimeout(reconcile,delay));}
function interceptOpen(event){const button=event.target instanceof Element?event.target.closest('button'):null;if(!button||!isOpenButton(button))return;const row=cardForButton(button);if(!row)return;const identity=resolveRow(row),api=window.H38_FIELD_VISIT;if(!identity||!api?.open)return;event.preventDefault();event.stopPropagation();event.stopImmediatePropagation();void api.open({captureSessionId:identity.sessionId,sessionId:identity.sessionId,siteVisitId:identity.visitId,visitId:identity.visitId,quoteId:identity.quoteId,customerId:identity.customerId,projectTitle:identity.projectTitle,scope:identity.scope});}
function jobsNavigation(event){const control=event.target instanceof Element?event.target.closest('button,a,[role="button"],[data-h38-primary]'):null;if(!control)return;const label=normalize(control.textContent),target=text(control.dataset?.h38Primary||control.dataset?.page||control.getAttribute?.('href')).toLowerCase();if(label==='jobs'||target==='work'||/\bwork\b/.test(target)){[0,80,260,700,1400,2600,4500].forEach(delay=>setTimeout(()=>{installRestoreAuthority();installOpenAuthority();if(isWork())arm();},delay));}}
document.addEventListener('click',interceptOpen,true);document.addEventListener('click',jobsNavigation,true);
if(typeof window.renderWork==='function'&&!window.renderWork.__h38LogicalSiteVisitDedupe){const base=window.renderWork,wrapped=function(){const result=base.apply(this,arguments);arm();return result;};wrapped.__h38LogicalSiteVisitDedupe=true;window.renderWork=wrapped;}
window.addEventListener('h38:business-snapshot-updated',()=>{installOpenAuthority();if(isWork())arm();});window.addEventListener('h38:auth-cleared',()=>{forcedIdentity=null;});window.addEventListener('focus',()=>{if(isWork())arm();});document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible'&&isWork())arm();});
[0,120,350,900,1800,3600,6000].forEach(delay=>setTimeout(()=>{installRestoreAuthority();installOpenAuthority();if(isWork())arm();},delay));
window.H38_SITE_VISIT_IDENTITY_AUTHORITY=Object.freeze({enabled:true,build:BUILD,resolve,resolveClues,resolveRow,reconcile,visitCards,workSurface,identityPriority:['Capture Session ID','Site Visit ID','unique Quote ID','Customer ID + exact Project Title','unique exact Project Title','unique project title contained in physical card text'],titleOnlyRequiresUniqueServerSession:true,conflictingIdentifiersBlockFallback:true,openCanonicalizedBeforeRestore:true,serverEvidenceNeverDeleted:true,genuineDifferentServerSessionsPreserved:true,persistentJobsObserver:false,eventDrivenJobsReconciliation:true,physicalCardStructureSupported:true,physicalCardRawTitleFallback:true,minimalOpenCardDetection:true,boundedLateMobileRenderRetries:true,jobsNavigationReconciliation:true,localSnapshotAliasSuppressed:true,linkedCanonicalTitleWins:true,automaticApproval:false,automaticCustomerSending:false});
window.H38_SITE_VISIT_WORK_DEDUPE_FINAL=Object.freeze({enabled:true,build:BUILD,identityAuthority:true,localDraftReconcilesWithServer:true,genuineDifferentServerSessionsPreserved:true,domOnlyNoEvidenceDeletion:true,persistentObserver:false,eventDrivenReconciliation:true,physicalCardStructureSupported:true,physicalCardRawTitleFallback:true,minimalOpenCardDetection:true,boundedLateMobileRenderRetries:true,jobsNavigationReconciliation:true,localSnapshotAliasSuppressed:true,linkedCanonicalTitleWins:true,automaticApproval:false,automaticCustomerSending:false});
})();
