(function(){
'use strict';
const BUILD='20260821-quote-measurement-action-photo-guard-5-phone';
const Bridge=window.H38Bridge;
const shared=window.H38_SUPABASE_SHARED_CLIENT;
if(!Bridge||!Bridge.prototype)return;
const previousRequest=Bridge.prototype.request;
const text=value=>String(value==null?'':value);
function valueOf(row,...keys){const source=row?.payload&&typeof row.payload==='object'?row.payload:row;for(const key of keys){if(source&&source[key]!==undefined&&source[key]!==null&&source[key]!=='')return source[key];}return'';}
function rows(name){const list=window.state?.snapshot?.[name];return Array.isArray(list)?list:[];}
function compactMeasurement(row){const value=Number(valueOf(row,'Value','value'));const label=text(valueOf(row,'Label','label')).trim();if(!label||!Number.isFinite(value)||value<=0)return null;return{measurementId:text(valueOf(row,'Site Measurement ID','measurementId','Measurement ID')),label,value,unit:text(valueOf(row,'Unit','unit')||'in'),source:text(valueOf(row,'Source','source')),verificationStatus:text(valueOf(row,'Verification Status','verificationStatus')||'UNVERIFIED'),notes:text(valueOf(row,'Notes','notes'))};}
function uniqueMeasurements(items){const seen=new Set();return items.map(compactMeasurement).filter(Boolean).filter(item=>{const key=[item.measurementId,item.label,item.value,item.unit,item.source,item.verificationStatus].join('|');if(seen.has(key))return false;seen.add(key);return true;}).slice(0,80);}
function quoteRecord(quoteId){return rows('quotes').find(row=>text(valueOf(row,'Quote ID','quoteId'))===text(quoteId))||null;}
function linkedMeasurementEvidence(args){const supplied=uniqueMeasurements([...(Array.isArray(args?.measurementEvidence)?args.measurementEvidence:[]),...(Array.isArray(args?.siteMeasurements)?args.siteMeasurements:[])]);if(supplied.length)return supplied;const quoteId=text(args?.quoteId).trim();if(!quoteId)return[];const quote=quoteRecord(quoteId);const sessionId=text(valueOf(quote,'Site Scanner Session ID','siteScannerSessionId')).trim();const all=[...rows('siteMeasurements'),...rows('measurements')];return uniqueMeasurements(all.filter(row=>{const rowQuote=text(valueOf(row,'Quote ID','quoteId'));const rowSession=text(valueOf(row,'Capture Session ID','captureSessionId'));return(sessionId&&rowSession===sessionId)||rowQuote===quoteId;}));}
async function liveMeasurementEvidence(args){const quoteId=text(args?.quoteId).trim(),businessId=text(args?.businessId||window.state?.businessId).trim();if(!quoteId||!businessId)return[];const api=shared?.ensure?.();if(!api)return[];const quote=quoteRecord(quoteId),sessionId=text(valueOf(quote,'Site Scanner Session ID','siteScannerSessionId')).trim();try{const result=await api.from('business_records').select('record_key,payload,updated_at').eq('business_id',businessId).eq('collection','siteMeasurements').eq('record_status','active').order('updated_at',{ascending:false}).limit(500);if(result.error)throw result.error;return uniqueMeasurements((result.data||[]).map(row=>row?.payload||{}).filter(payload=>{const rowQuote=text(valueOf(payload,'Quote ID','quoteId'));const rowSession=text(valueOf(payload,'Capture Session ID','captureSessionId'));return rowQuote===quoteId||(sessionId&&rowSession===sessionId);}));}catch(error){console.warn('[H38 Quote measurement guard] live measurement hydration failed',error);return[];}}
function materialBreakoutInstructions(){return['QUOTE COST BREAKOUT REQUIREMENT: Show material cost and labor cost as separate required quote lines whenever both are part of the actual project scope. System policy examples are never project scope.','MATERIAL ORDER ALLOWANCE: calculate purchase/order quantities at 110% of the measured installed material quantity when an ordering allowance applies. Labor quantities use the actual installed/net work quantity.','Keep all generated prices owner-review required. Never approve, send, purchase, pay, or financially commit automatically.'].join('\n');}
function applyMaterialBreakoutPolicy(prepared){prepared.systemQuotePolicy=[text(prepared.systemQuotePolicy),materialBreakoutInstructions()].filter(Boolean).join('\n\n');prepared.materialOrderAllowancePercent=10;prepared.separateMaterialAndLabor=true;}
function actionPhotoMap(){return window.H38_QUOTE_ACTION_PHOTO_BY_QUOTE||(window.H38_QUOTE_ACTION_PHOTO_BY_QUOTE=Object.create(null));}
function savedQuoteActionPhotoId(quoteId){const quote=quoteRecord(quoteId),saved=text(valueOf(quote,'Action Picture ID','actionPictureId')).trim();if(saved)return saved;const linked=rows('documents').find(row=>{const sourceType=text(valueOf(row,'Source Type','sourceType')).toLowerCase(),sourceId=text(valueOf(row,'Source ID','sourceId')),mime=text(valueOf(row,'Mime Type','mimeType')).toLowerCase();return sourceType==='quote'&&sourceId===quoteId&&mime.startsWith('image/')&&(valueOf(row,'Action Picture','actionPicture')===true||text(valueOf(row,'Action Picture Source ID','actionPictureSourceId')).trim());});return text(valueOf(linked,'Action Picture Source ID','actionPictureSourceId','Original Document ID','originalDocumentId','Document ID','documentId')).trim();}
function actionPhotoId(quoteId,args){const explicit=text(args?.actionPhotoDocumentId||args?.actionPictureId).trim();if(explicit)return explicit;const saved=savedQuoteActionPhotoId(quoteId);if(saved)return saved;const mapped=text(actionPhotoMap()[quoteId]).trim();if(mapped)return mapped;const visit=window.H38_FIELD_VISIT_CORE?.state?.visit;if(visit&&text(visit.quoteId)===quoteId)return text(visit.actionPictureId).trim();return'';}
function skipRender(result){if(!result||typeof result!=='object')return result;return{...result,photoCount:0,renderStatus:'SKIPPED_NO_ACTION_PHOTO',actionPhotoRequiredForRender:true};}
async function promoteActionPhotoSource(quoteId,selected){const api=shared?.ensure?.(),businessId=text(window.state?.businessId).trim();if(!api||!businessId||!quoteId||!selected)throw new Error('The saved Action Picture could not be prepared securely for rendering.');if(typeof window.sync==='function')await window.sync(false);const result=await api.from('business_records').select('record_key,payload').eq('business_id',businessId).eq('collection','documents').eq('record_status','active').limit(500);if(result.error)throw result.error;const candidates=(result.data||[]).filter(row=>{const payload=row?.payload||{},sourceType=text(valueOf(payload,'Source Type','sourceType')).toLowerCase(),sourceId=text(valueOf(payload,'Source ID','sourceId')),mime=text(valueOf(payload,'Mime Type','mimeType')).toLowerCase(),documentId=text(valueOf(payload,'Document ID','documentId')),originalId=text(valueOf(payload,'Original Document ID','originalDocumentId')),actionSource=text(valueOf(payload,'Action Picture Source ID','actionPictureSourceId'));return sourceType==='quote'&&sourceId===quoteId&&mime.startsWith('image/')&&(documentId===selected||originalId===selected||actionSource===selected||valueOf(payload,'Action Picture','actionPicture')===true);});if(!candidates.length)throw new Error('The saved Site Visit Action Picture is not linked to this quote yet. Reopen the Site Visit and finish it again before rendering.');const target=candidates.sort((a,b)=>(valueOf(b.payload,'Action Picture','actionPicture')===true?1:0)-(valueOf(a.payload,'Action Picture','actionPicture')===true?1:0))[0],now=new Date().toISOString(),payload={...(target.payload||{}),'Action Picture':true,'Action Picture Source ID':selected,'Action Picture Selected Time':now,'Visibility':'Internal Action Picture','Updated Time':now};const updated=await api.from('business_records').update({payload,updated_at:now}).eq('business_id',businessId).eq('collection','documents').eq('record_key',target.record_key);if(updated.error)throw updated.error;return text(target.record_key);}
Bridge.prototype.request=async function(action,args,timeout){
  if(action==='aiBuildQuoteDraft'){
    const prepared={...(args||{})};applyMaterialBreakoutPolicy(prepared);let evidence=linkedMeasurementEvidence(prepared);const live=await liveMeasurementEvidence(prepared);if(live.length)evidence=live;if(evidence.length){prepared.measurementEvidence=evidence;prepared.siteMeasurements=evidence;}const result=await previousRequest.call(this,action,prepared,timeout);const quoteId=text(prepared.quoteId).trim();if(!actionPhotoId(quoteId,prepared))return skipRender(result);return result;
  }
  if(action==='aiRenderQuoteConcept'){
    const prepared={...(args||{})},quoteId=text(prepared.quoteId).trim(),selected=actionPhotoId(quoteId,prepared);if(!selected)return{status:'PASS',renderStatus:'SKIPPED_NO_ACTION_PHOTO',renderedConcepts:[],actionPhotoRequiredForRender:true,ownerReviewRequired:true,externalActionOccurred:false};prepared.actionPhotoDocumentId=selected;prepared.actionPhotoQuoteLinkId=await promoteActionPhotoSource(quoteId,selected);return previousRequest.call(this,action,prepared,timeout);
  }
  return previousRequest.call(this,action,args,timeout);
};
window.H38_QUOTE_MEASUREMENT_ACTION_PHOTO_GUARD=Object.freeze({build:BUILD,linkedMeasurementHydrationRestored:true,acceptsSiteMeasurementsPayload:true,liveSupabaseMeasurementHydration:true,measurementEvidencePassedToQuoteAi:true,separateMaterialAndLabor:true,materialOrderAllowancePercent:10,noBlendedInstalledRateForSeparatedComponents:true,noAutomaticRenderWithoutActionPhoto:true,actionPhotoRequiredForRender:true,selectedActionPhotoPromotedAsRenderSource:true,selectedActionPhotoMustBeLinkedToQuote:true,savedQuoteActionPictureAuthority:true,quoteRecordActionPictureFallback:true,internalActionPictureIndependentOfCustomerSelection:true,systemPolicyCannotCreateScope:true,automaticApproval:false,automaticCustomerSending:false,automaticFinancialAction:false});
})();