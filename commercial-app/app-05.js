function renderWork(){const requests=records('requests'),jobs=records('jobs'),tasks=records('tasks'),customers=records('customers'),users=activeRows(records('users'));$('mainContent').innerHTML=pageHead('Work','Turn requests into organized jobs, tasks, work orders and field assignments.')+`<div class="grid"><section class="card span4"><h2>New request</h2><form id="requestForm"><label>Customer</label><select name="customerId">${optionRows(customers,['Customer ID'],row=>v(row,'Customer Name'),'Generic or unknown customer')}</select><label>Subject</label><input name="subject" required><label>Details</label><textarea name="details"></textarea><div class="two"><div><label>Source</label><select name="source"><option>Office</option><option>Phone</option><option>Email</option><option>SMS</option><option>Portal</option><option>Field</option></select></div><div><label>Priority</label><select name="priority"><option>Normal</option><option>Low</option><option>High</option><option>Urgent</option></select></div></div><div class="actions"><button>Save request</button></div></form></section><section class="card span4"><h2>New job</h2><form id="jobForm"><label>Customer</label><select name="customerId">${optionRows(customers,['Customer ID'],row=>v(row,'Customer Name'),'No customer')}</select><label>Project title</label><input name="projectTitle" required><label>Status</label><select name="status"><option>Lead</option><option>Quoted</option><option>Approved</option><option>Scheduled</option><option>In Progress</option><option>Complete</option></select><div class="actions"><button>Save job</button></div></form></section><section class="card span4"><h2>Assign task</h2><form id="taskForm"><label>Job</label><select name="jobId">${optionRows(jobs,['Job ID'],row=>`${v(row,'Job Number')} — ${v(row,'Project Title')}`,'No job')}</select><label>Task</label><input name="taskTitle" required><label>Assigned user</label><select name="assignedUserId">${optionRows(users,['User ID'],row=>v(row,'Display Name','Email'),'Unassigned')}</select><label>Due</label><input name="dueTime" type="datetime-local"><div class="actions"><button>Save task</button></div></form></section><section class="card span4"><h2>Requests</h2><div class="list">${requests.length?requests.slice(0,30).map(row=>`<div class="row"><div class="row-top"><strong>${esc(v(row,'Subject'))}</strong>${pill(v(row,'Status')||'New')}</div><small>${esc(v(row,'Source'))} · ${esc(v(row,'Priority'))} · ${esc(customerName(v(row,'Customer ID')))}</small></div>`).join(''):empty('No requests.')}</div></section><section class="card span4"><h2>Jobs</h2><div class="list">${jobs.length?jobs.slice(0,40).map(row=>`<div class="row"><div class="row-top"><strong>${esc(v(row,'Project Title'))}</strong>${pill(v(row,'Status'))}</div><small>${esc(v(row,'Job Number'))} · ${esc(customerName(v(row,'Customer ID')))}</small></div>`).join(''):empty('No jobs.')}</div></section><section class="card span4"><h2>Tasks</h2><div class="list">${tasks.length?tasks.slice(0,50).map(row=>`<div class="row"><div class="row-top"><strong>${esc(v(row,'Task Title'))}</strong>${pill(v(row,'Status'))}</div><small>${esc(jobName(v(row,'Job ID')))} · ${esc(userName(v(row,'Assigned User ID')))} · ${dateTime(v(row,'Due Time'))}</small></div>`).join(''):empty('No tasks.')}</div></section></div>`;bindForm('requestForm',async(data,form)=>{const id=newId('REQUEST'),record={'Request ID':id,'Business ID':state.businessId,'Customer ID':data.customerId,'Source':data.source,'Subject':requireValue(data.subject,'Subject is required.'),'Details':data.details,'Priority':data.priority,'Status':'New','Created Time':now(),'Updated Time':now(),'Record Version':1};await queueOperation('SAVE_REQUEST','Request',id,{requestId:id,...data},{collection:'requests',record,idKeys:['Request ID']});form.reset();toast('Request queued.');renderWork();});bindForm('jobForm',async(data,form)=>{const id=newId('JOB'),record={'Job ID':id,'Business ID':state.businessId,'Customer ID':data.customerId,'Job Number':`LOCAL-${Date.now()}`,'Project Title':requireValue(data.projectTitle,'Project title is required.'),'Status':data.status,'Created Time':now(),'Updated Time':now(),'Record Version':1};await queueOperation('SAVE_JOB','Job',id,{jobId:id,...data},{collection:'jobs',record,idKeys:['Job ID']});form.reset();toast('Job queued.');renderWork();});bindForm('taskForm',async(data,form)=>{const id=newId('TASK'),record={'Task ID':id,'Business ID':state.businessId,'Job ID':data.jobId,'Task Title':requireValue(data.taskTitle,'Task title is required.'),'Assigned User ID':data.assignedUserId,'Status':'Open','Due Time':data.dueTime,'Created Time':now(),'Updated Time':now(),'Record Version':1};await queueOperation('SAVE_TASK','Task',id,{taskId:id,...data},{collection:'tasks',record,idKeys:['Task ID']});form.reset();toast('Task queued.');renderWork();});}

