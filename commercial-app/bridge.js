class H38Bridge{
  constructor(frame,url,onStatus){this.frame=frame;this.url=url;this.onStatus=onStatus||(()=>{});this.ready=false;this.pending=new Map();this.listener=event=>this.receive(event);addEventListener('message',this.listener);}
  setUrl(url){this.url=url;this.ready=false;this.connect();}
  connect(){if(!this.url)return;this.ready=false;this.onStatus('connecting');this.frame.src=this.url+(this.url.includes('?')?'&':'?')+'v='+Date.now();clearTimeout(this.timer);this.timer=setTimeout(()=>{if(!this.ready)this.onStatus('unavailable');},12000);}
  receive(event){if(event.source!==this.frame.contentWindow)return;const message=event.data||{};if(message.type==='H38_BRIDGE_READY'){this.ready=true;clearTimeout(this.timer);this.onStatus('ready');return;}if(message.type!=='H38_BRIDGE_RESPONSE')return;const pending=this.pending.get(message.requestId);if(!pending)return;this.pending.delete(message.requestId);clearTimeout(pending.timer);message.ok?pending.resolve(message.result):pending.reject(new Error(message.error||'Secure bridge request failed.'));}
  request(action,args={},timeout=45000){if(!this.ready)return Promise.reject(new Error('Secure bridge is not ready. Open the Google beta once while signed in, then try again.'));const requestId=crypto.randomUUID();return new Promise((resolve,reject)=>{const timer=setTimeout(()=>{this.pending.delete(requestId);reject(new Error(`${action} timed out.`));},timeout);this.pending.set(requestId,{resolve,reject,timer});this.frame.contentWindow.postMessage({type:'H38_BRIDGE_REQUEST',requestId,action,args},'*');});}
}
window.H38Bridge=H38Bridge;
