(function(){
'use strict';
const BUILD='20260807-0035';
let running=false;
const text=value=>String(value==null?'':value);
const clone=value=>JSON.parse(JSON.stringify(value==null?null:value));
const num=value=>Number(value||0);
function message(error){return text(error&&error.message?error.message:error||'Build Quote failed.');}
function button(){return document.getElementById('h38AiQuoteDraftButton');}
function ensureRowId(){if(typeof window.rowId==='function')return window.rowId;window.rowId=(row,...keys)=>{for(const key of keys){if(row&&row[key]!==undefined&&row[key]!==null&&row[key]!=='')return String(row[key]);}return'';};return window.rowId;}
function installWorkingHammer(){if(window.H38_WORKING_HAMMER)return window.H38_WORKING_HAMMER;const style=document.createElement('style');style.textContent=`#h38WorkingHammer{position:fixed;inset:0;z-index:12000;display:grid;place-items:center;padding:20px;background:rgba(11,36,56,.72);backdrop-filter:blur(3px)}#h38WorkingHammer[hidden]{display:none}.h38-wh-card{width:min(92vw,390px);padding:28px 24px;border-radius:18px;background:#fff;color:#172033;text-align:center;box-shadow:0 24px 70px rgba(0,0,0,.3)}.h38-wh-tools{position:relative;width:112px;height:112px;margin:0 auto 18px;display:grid;place-items:center}.h38-wh-spinner{position:absolute;inset:0;border:8px solid rgba(34,84,61,.16);border-top-color:#22543d;border-radius:50%;animation:h38whspin 1s linear infinite}.h38-wh-hammer{font-size:50px;line-height:1;transform-origin:72% 72%;animation:h38whhammer .72s ease-in-out infinite}.h38-wh-card strong{display:block;font-size:1.2rem;margin-bottom:8px}.h38-wh-card p{margin:0;color:#475569;line-height:1.45}@keyframes h38whspin{to{transform:rotate(360deg)}}@keyframes h38whhammer{0%,100%{transform:rotate(-24deg) translateY(-2px)}50%{transform:rotate(18deg) translateY(4px)}}@media(prefers-reduced-motion:reduce){.h38-wh-spinner,.h38-wh-hammer{animation:none}}`;document.head.appendChild(style);const overlay=document.createElement('div');overlay.id='h38WorkingHammer';overlay.hidden=true;overlay.setAttribute('role','status');overlay.setAttribute('aria-live','assertive');overlay.innerHTML='<div class="h38-wh-card"><div class="h38-wh-tools" aria-hidden="true"><span class="h38-wh-spinner"></span><span class="h38-wh-hammer">🔨</span></div><strong id="h38WhTitle">Highway 38 is working</strong><p id="h38WhMessage">Please keep this screen open.</p></div>';document.body.appendChild(overlay);let activeButton=null;const api={show(title,detail,source){activeButton=source||activeButton;overlay.hidden=false;document.body.setAttribute('aria-busy','true');document.getElementById('h38WhTitle').textContent=title||'Highway 38 is working';document.getElementById('h38WhMessage').textContent=detail||'Please keep this screen open.';},hide(source){if(source&&activeButton&&source!==activeButton)return;activeButton=null;overlay.hidden=true;document.body.removeAttribute('aria-busy');}};const delayWords=/working|loading|saving|syncing|uploading|analyzing|building|printing|sending|preparing|estimating|creating|updating|refreshing|processing/i;document.addEventListener('click',event=>{const target=event.target&&event.target.closest&&event.target.closest('button');if(!target)return;setTimeout(()=>{const label=text(target.textContent);if(target.disabled||delayWords.test(label))api.show('Highway 38 is working',label||'Processing your request',target);},0);},true);const observer=new MutationObserver(()=>{if(!activeButton)return;if(!document.contains(activeButton)||!activeButton.disabled&&!delayWords.test(text(activeButton.textContent)))api.hide(activeButton);});observer.observe(document.documentElement,{subtree:true,attributes:true,attributeFilter:['disabled'],childList:true,characterData:true});window.H38_WORKING_HAMMER=api;return api;}
function ensureStatus(){let node=document.getElementById('h38QuoteBuildStatus');if(node)return node;const tools=document.querySelector('.page-tools');if(!tools)return null;node=document.createElement('div');node.id='h38QuoteBuildStatus';node.className='notice';node.setAttribute('role','status');node.setAttribute('aria-live','assertive');node.hidden=true;tools.insertAdjacentElement('afterend',node);return node;}
function show(value,kind){const node=ensureStatus();if(!node)return;node.hidden=false;node.className=`notice${kind==='error'?' warn':''}`;node.textContent=text(value);node.dataset.state=kind||'pending';node.scrollIntoView({behavior:'smooth',block:'nearest'});}
function savedQuote(){return Boolean(text(window.state?.quote?.quoteId).trim());}
function hasReviewedLines(){return Array.isArray(window.state?.quote?.lines)&&window.state.quote.lines.length>0;}
function protectedQuote(){return savedQuote()||hasReviewedLines();}
function exactSavedQuoteReady(){if(!savedQuote())return true;const q=window.state?.quote||{};if(q.hydrationComplete!==true)return false;const saved=num(q.loadedSavedTotal||q.savedTotal);const current=(q.lines||[]).reduce((sum,line)=>sum+num(line.quantity)*num(line.unitPrice),0);return !(saved>0&&Math.abs(saved-current)>0.01&&!q.ownerEdited);}
function chooseMode(){return new Promise(resolve=>{let modal=document.getElementById('h38QuoteRevisionChoice');if(modal)modal.remove();modal=document.createElement('div');modal.id='h38QuoteRevisionChoice';modal.className='modal';modal.innerHTML=`<div class="modal-card" role="dialog" aria-modal="true" aria-labelledby="h38QuoteRevisionTitle"><h2 id="h38QuoteRevisionTitle">Keep the current quote total?</h2><p>This saved quote is protected. Running AI again will not replace its reviewed line items or total.</p><div class="actions"><button type="button" data-choice="suggestions">Update AI Suggestions Only</button><button type="button" class="secondary" data-choice="revision">Create New Revision</button><button type="button" class="secondary" data-choice="cancel">Cancel</button></div></div>`;document.body.appendChild(modal);modal.querySelectorAll('[data-choice]').forEach(node=>node.onclick=()=>{const choice=node.dataset.choice;modal.remove();resolve(choice);});});}
function input(id){return document.getElementById(id);}
function requestPayload(){const customer=input('quoteCustomer'),projectTitle=input('quoteTitle'),scope=input('quoteScope'),measurements=input('quoteMeasurements');if(!customer||!customer.value)throw new Error('Select a customer first.');if(!text(scope?.value).trim()&&!text(measurements?.value).trim())throw new Error('Add scope, site notes, measurements or a saved quote photo first.');return {businessId:window.state.businessId,customerId:customer.value,quoteId:window.state.quote?.quoteId||'',projectTitle:projectTitle?.value||'',scope:scope?.value||'',measurementNotes:measurements?.value||'',notes:'Use the full saved scope, all linked photos, measurements, owner-edited quantities, prior revision context and Price Catalog. Never replace, remove, reduce or reprice reviewed owner line items. Return additions as suggestions unless Create New Revision was explicitly selected.'};}
function mapLines(draft){return (Array.isArray(draft?.suggestedLines)?draft.suggestedLines:[]).map(line=>({quoteLineId:(typeof window.newId==='function'?window.newId('QUOTE-LINE'):`QUOTE-LINE-${Date.now()}-${Math.random()}`),description:text(line.description||'Suggested work item'),quantity:Math.max(0.01,num(line.quantity||1)),unit:text(line.unit||'each'),unitPrice:num(line.rate||line.unitPrice||0),priceSource:line.catalogId?'Price Catalog + AI assistance':'AI suggestion — manual price required',priceStatus:'Owner review required'}));}
async function buildDraft(){if(!navigator.onLine||!window.state?.bridgeReady)throw new Error('AI quote drafting needs an online secure Office connection.');installWorkingHammer().show('Building the quote','Reviewing the full saved scope, photos, measurements and Price Book',button());const result=await window.state.bridge.request('aiBuildQuoteDraft',requestPayload(),180000);if(result.status!=='PASS')throw new Error(result.message||'AI quote draft did not complete.');const draft=result.draft||{},lines=mapLines(draft);if(!lines.length)throw new Error('The AI request finished without creating quote suggestions.');return {provider:result.provider||'AI',draft,lines};}
function observations(draft){const measurements=text(input('quoteMeasurements')?.value);return [measurements,...(draft.photoObservations||[]).map(item=>`AI photo observation: ${item}`),...(draft.missingInformation||[]).map(item=>`Needs confirmation: ${item}`)].filter(Boolean).join('\n');}
async function executeBuild(mode){const original=clone(window.state?.quote||{}),originalLines=clone(original.lines||[]),originalRevision=Number(original.revision||1);const generated=await buildDraft();if(mode==='suggestions'){window.state.quote=Object.assign({},original,{lines:originalLines,aiSuggestedLines:generated.lines,aiSuggestionUpdatedAt:new Date().toISOString(),aiObservations:observations(generated.draft)});if(typeof window.renderQuotes==='function')window.renderQuotes();return `${generated.lines.length} AI suggestion${generated.lines.length===1?'':'s'} updated. The current quote lines and total were not changed.`;}if(mode==='revision'){window.state.quote=Object.assign({},original,{projectTitle:generated.draft.projectTitle||original.projectTitle||'',scope:generated.draft.scope||original.scope||'',measurementNotes:observations(generated.draft),lines:generated.lines,revision:originalRevision+1,previousRevision:originalRevision,hydrationComplete:true,revisionHistory:[...(Array.isArray(original.revisionHistory)?original.revisionHistory:[]),{revision:originalRevision,savedAt:new Date().toISOString(),lines:originalLines,scope:original.scope||'',measurementNotes:original.measurementNotes||''}]});if(typeof window.renderQuotes==='function')window.renderQuotes();return `New revision ${originalRevision+1} created. Revision ${originalRevision} and its total remain preserved.`;}window.state.quote=Object.assign({},original,{projectTitle:generated.draft.projectTitle||original.projectTitle||'',scope:generated.draft.scope||original.scope||'',measurementNotes:observations(generated.draft),customerId:requestPayload().customerId,lines:generated.lines,hydrationComplete:true});if(typeof window.renderQuotes==='function')window.renderQuotes();return `AI draft loaded with ${generated.lines.length} editable line${generated.lines.length===1?'':'s'}.`;}
async function runBuild(){if(running){show('Build Quote is already running.','pending');return;}if(savedQuote()&&!exactSavedQuoteReady()){const detail='The exact saved revision is not fully loaded or its saved total does not match the loaded lines. Close and reopen the draft. Build Quote was blocked and nothing changed.';show(detail,'error');if(typeof window.toast==='function')window.toast(detail,true);return;}let mode='new';if(protectedQuote()){mode=await chooseMode();if(mode==='cancel')return;}running=true;const current=button();if(current){current.disabled=true;current.textContent=mode==='suggestions'?'Updating suggestions…':mode==='revision'?'Creating revision…':'Preparing photos…';}installWorkingHammer().show('Starting Build Quote','Checking the secure session and exact saved revision',current);show('Checking the secure session and linking Site Visit evidence…','pending');try{ensureRowId();const valid=await window.H38_SUPABASE_SESSION_RECOVERY?.validate?.();if(valid===false)throw new Error('Secure session expired. Sign in again before building the quote.');installWorkingHammer().show('Preparing site evidence','Linking photos, notes and measurements',current);const linked=await window.H38_QUOTE_PHOTO_RESTORE?.ensureQuoteLinks?.();show(`${linked?`${linked} Site Visit photo link${linked===1?'':'s'} prepared. `:''}Analyzing saved scope, measurements, photos and Price Book…`,'pending');const result=await executeBuild(mode);show(`${result} Owner review is required; nothing was approved or sent.`,'success');}catch(error){const detail=`${message(error)} The current quote, total, revisions and private photos were preserved; nothing was approved or sent.`;show(detail,'error');if(typeof window.toast==='function')window.toast(detail,true);else window.alert(detail);}finally{installWorkingHammer().hide(current);const latest=button();if(latest){latest.disabled=false;latest.textContent=protectedQuote()?'✨ Update Quote':'✨ Build Quote';}running=false;}}
ensureRowId();if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',installWorkingHammer);else installWorkingHammer();document.addEventListener('click',event=>{const target=event.target?.closest?.('#h38AiQuoteDraftButton');if(!target)return;event.preventDefault();event.stopPropagation();event.stopImmediatePropagation?.();void runBuild();},true);
window.H38_QUOTE_AI_CLICK_GUARD={enabled:true,build:BUILD,exactSavedRevisionRequired:true,savedQuoteAlwaysProtected:true,directSuggestionRequest:true,globalWorkingHammer:true,ownerEditsProtected:true,suggestionMode:true,newRevisionMode:true,automaticApproval:false,automaticCustomerSending:false,run:runBuild};
})();
