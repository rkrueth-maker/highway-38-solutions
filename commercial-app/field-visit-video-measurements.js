(function(){
'use strict';
const BUILD='20260811-video-headless-stable-1154';
const HEARTBEAT_MS=2500;
const C=window.H38_FIELD_VISIT_CORE;
const cfg=window.H38_BUSINESS_OFFICE_SUPABASE||{};
const shared=window.H38_SUPABASE_SHARED_CLIENT;
if(!C||!cfg.enabled||!window.supabase)return;
const text=v=>String(v==null?'':v);
const value=(row,...keys)=>{for(const key of keys){if(row&&row[key]!==undefined&&row[key]!==null&&row[key]!=='')return row[key]}return''};
const rows=name=>Array.isArray(window.state?.snapshot?.[name])?window.state.snapshot[name]:[];
let busy=false,lastAutoKey='',heartbeatTimer=0,runtimeDisabled=false,faultCount=0,lastNotice='';
function visit(){return C.state.visit||null}
function sessionRecord(v=visit()){if(!v?.sessionId)return null;return rows('siteCaptureSessions').find(row=>text(value(row,'Capture Session ID','captureSessionId'))===text(v.sessionId))||null}
function latestReview(v=visit()){if(!v?.sessionId)return null;return rows('siteAiReviews').filter(row=>text(value(row,'Capture Session ID','captureSessionId'))===text(v.sessionId)).sort((a,b)=>text(value(b,'Updated Time','updatedAt','Created Time','createdAt')).localeCompare(text(value(a,'Updated Time','updatedAt','Created Time','createdAt'))))[0]||null}
function materialSpec(source){const s=text(source).toLowerCase();return(/\br\s*-?\s*\d{1,2}\b/.test(s)&&/(insulat|batt|fiberglass|mineral wool|wide|width)/.test(s))||/\b(?:r-value|sku|model(?: number)?|part number|gauge|capacity)\b/.test(s)}
function dimensionSignatures(raw){const source=typeof raw==='string'?raw:[raw?.label,raw?.valueText,raw?.statement,raw?.detail,raw?.request].map(text).join(' '),out=[];const re=/(\d+(?:\.\d+)?)\s*(?:(?:ft|feet|foot|in|inch(?:es)?|["'])\s*)?(?:x|×|by)\s*(\d+(?:\.\d+)?)\s*(?:ft|feet|foot|in|inch(?:es)?|["'])?/gi;let match;while((match=re.exec(source)))out.push([Number(match[1]),Number(match[2])].sort((a,b)=>a-b).map(String).join('x'));return Array.from(new Set(out))}
function spokenMeasurements(){const session=sessionRecord();return Array.isArray(value(session,'Walkthrough Spoken Measurements','walkthroughSpokenMeasurements'))?value(session,'Walkthrough Spoken Measurements','walkthroughSpokenMeasurements'):[]}
function isVerified(item){const status=text(item?.verificationStatus||value(item,'Verification Status','verificationStatus')).toUpperCase();return item?.fieldVerified===true||['OPERATOR_VERIFIED','FIELD_VERIFIED','VERIFIED_BY_OPERATOR','VERIFIED','FIELD_MEASURED','FIELD_MEASURED_AND_CHECKED','DEVICE_CAPTURED'].includes(status)}
function sessionMeasurements(){const v=visit();if(!v?.sessionId)return[];return rows('siteMeasurements').filter(row=>text(value(row,'Capture Session ID','captureSessionId'))===text(v.sessionId))}
function verifiedSignatures(){const set=new Set();for(const item of spokenMeasurements())if(isVerified(item))dimensionSignatures(item).forEach(sig=>set.add(sig));for(const row of sessionMeasurements())if(isVerified(row))dimensionSignatures([value(row,'Label','label'),value(row,'Notes','notes')].join(' ')).forEach(sig=>set.add(sig));return set}
function cleanLabel(raw){return text(raw).toLowerCase().replace(/\b(?:verify|measure|measurement|measurements|field|dimension|dimensions|required|needed|need|confirm|record|walkthrough|estimate|estimated)\b/g,' ').replace(/[^a-z0-9]+/g,' ').replace(/\s+/g,' ').trim()}
function labelsMatch(a,b){const x=cleanLabel(a),y=cleanLabel(b);if(!x||!y)return false;if(x===y)return true;if(Math.min(x.length,y.length)>=8&&(x.includes(y)||y.includes(x)))return true;const xa=new Set(x.split(' ').filter(t=>t.length>2)),ya=new Set(y.split(' ').filter(t=>t.length>2)),common=[...xa].filter(t=>ya.has(t));return common.length>=2||(xa.size===1&&ya.size===1&&common.length===1)}
function videoMeasurementRecords(){return sessionMeasurements().filter(row=>text(value(row,'Source','source')).toUpperCase()==='CAMERA_ESTIMATE')}
function existingEstimateFor(target){return videoMeasurementRecords().find(row=>labelsMatch(value(row,'Label','label'),target))||null}
function targetList(){const review=latestReview(),raw=Array.isArray(value(review,'Missing Measurements','missingMeasurements'))?value(review,'Missing Measurements','missingMeasurements'):[],verified=verifiedSignatures();return raw.map(text).map(x=>x.trim()).filter(Boolean).filter(x=>!materialSpec(x)).filter(x=>{const sigs=dimensionSignatures(x);if(sigs.length&&sigs.every(sig=>verified.has(sig)))return false;if(existingEstimateFor(x))return false;return true}).slice(0,12)}
function referenceFingerprint(){return spokenMeasurements().filter(isVerified).map(item=>[text(item?.label),text(item?.valueText||item?.statement),text(item?.verificationStatus),item?.fieldVerified===true].join(':')).sort().join('|')}
function inputKey(){const v=visit();return[text(v?.sessionId),(v?.walkthroughFrameIds||[]).join(','),referenceFingerprint(),targetList().join('|')].join('::')}
function measurementState(){const v=visit();if(!v)return null;if(!v.videoMeasurementState)v.videoMeasurementState={status:'WAITING',message:'',estimates:[],outcome:'',updatedAt:'',inputKey:''};return v.videoMeasurementState}
function client(){if(shared?.ensure)return shared.ensure();return window.supabase.createClient(cfg.url,cfg.publishableKey,{auth:{persistSession:true,autoRefreshToken:true,detectSessionInUrl:true,flowType:'pkce'}})}
async function auth(force=false){const api=client();let result=await api.auth.getSession();if(result.error)throw result.error;let session=result.data?.session;if(!session)throw Error('Sign in again before estimating walkthrough measurements.');if(force||Number(session.expires_at||0)*1000<=Date.now()+120000){const refreshed=await api.auth.refreshSession();if(refreshed.error||!refreshed.data?.session)throw Error(refreshed.error?.message||'Secure session could not be refreshed.');session=refreshed.data.session}const verified=await api.auth.getUser(session.access_token);if(verified.error||!verified.data?.user)throw Error(verified.error?.message||'Secure session is invalid.');return{api,session,user:verified.data.user}}
function measurementId(estimate,v){const session=text(v.sessionId).replace(/[^A-Za-z0-9]/g,'').slice(-10),base=text(estimate.id||estimate.label||'VIDEO').replace(/[^A-Za-z0-9-]/g,'-').slice(0,80);return`SITE-MEASURE-VIDEO-${session}-${base}`}
async function saveEstimates(api,user,estimates){const v=visit();if(!v)return[];const saved=[];if(!window.state.snapshot)window.state.snapshot={};if(!Array.isArray(window.state.snapshot.siteMeasurements))window.state.snapshot.siteMeasurements=[];if(!Array.isArray(C.state.measurements))C.state.measurements=[];if(!Array.isArray(v.measurementIds))v.measurementIds=[];for(const estimate of estimates||[]){const id=measurementId(estimate,v),record={'Site Measurement ID':id,'Capture Session ID':v.sessionId,'Business ID':v.businessId||window.state?.businessId,'Customer ID':v.customerId||'','Quote ID':v.quoteId||'','Sequence':sessionMeasurements().length+saved.length+1,'Label':text(estimate.label||'Video estimate'),'Measurement Type':'Length','Value':Number(estimate.valueInches||0),'Unit':'in','Source':'CAMERA_ESTIMATE','Confidence':Math.max(0,Math.min(.72,Number(estimate.confidence||0))),'Verification Status':'UNVERIFIED','Start Point':estimate.startPoint||{},'End Point':estimate.endPoint||{},'Linked Frame':text(estimate.frameDocumentId),'Linked Device Reading':text(estimate.referenceId),'Confirmed By':'','Notes':`Walkthrough video reference-scale estimate. Reference: ${text(estimate.referenceLabel)} ${text(estimate.referenceValue)} (${text(estimate.referenceVerificationStatus)}). ${text(estimate.evidenceNote)} Field verification required.`,'Video Estimate Method':'SAME_FRAME_REFERENCE_SCALE','Video Reference ID':text(estimate.referenceId),'Video Reference Label':text(estimate.referenceLabel),'Video Reference Value':text(estimate.referenceValue),'Video Sample Count':Number(estimate.sampleCount||1),'Video Agreement Spread Ratio':Number(estimate.agreementSpreadRatio||0),'Conflict Review Required':estimate.conflictReviewRequired===true,'Owner Review Required':true,'Automatic Approval':false,'Automatic Customer Sending':false,'Created Time':new Date().toISOString(),'Updated Time':new Date().toISOString(),'Record Version':1};if(!(record.Value>0))continue;const result=await api.from('business_records').upsert({business_id:record['Business ID'],collection:'siteMeasurements',record_key:id,payload:record,record_status:'active',created_by:user.id,updated_by:user.id},{onConflict:'business_id,collection,record_key'});if(result.error)throw result.error;const existing=window.state.snapshot.siteMeasurements.findIndex(row=>text(value(row,'Site Measurement ID','measurementId'))===id);if(existing>=0)window.state.snapshot.siteMeasurements[existing]=record;else window.state.snapshot.siteMeasurements.unshift(record);const local=C.state.measurements.findIndex(row=>text(value(row,'Site Measurement ID','measurementId'))===id);if(local>=0)C.state.measurements[local]=record;else C.state.measurements.push(record);if(!v.measurementIds.includes(id))v.measurementIds.push(id);saved.push(record)}await C.saveDraft?.();return saved}
async function request(targets,forceRefresh=false){const v=visit(),authState=await auth(forceRefresh),response=await fetch(`${cfg.url}/functions/v1/h38-video-measurements`,{method:'POST',mode:'cors',cache:'no-store',credentials:'omit',headers:{authorization:`Bearer ${authState.session.access_token}`,apikey:cfg.publishableKey,'content-type':'application/json','x-client-info':'h38-walkthrough-headless-video-measurements-v4'},body:JSON.stringify({businessId:v.businessId||window.state?.businessId,captureSessionId:v.sessionId,quoteId:v.quoteId,targets})}),payload=await response.json().catch(()=>({}));return{...authState,response,payload}}
function notifyOnce(){const s=measurementState(),signature=JSON.stringify({sessionId:visit()?.sessionId,status:s?.status,message:s?.message,outcome:s?.outcome,measurements:(visit()?.measurementIds||[]).join(',')});if(signature===lastNotice)return;lastNotice=signature;try{window.dispatchEvent(new CustomEvent('h38:walkthrough-measurements-updated',{detail:{sessionId:visit()?.sessionId,status:s?.status}}))}catch(_){}}
async function run(force=false){const v=visit(),s=measurementState();if(!v||!s||runtimeDisabled||busy||!navigator.onLine||!v.sessionId||!v.quoteId||!latestReview())return false;const targets=targetList(),key=inputKey();if(!targets.length){const count=videoMeasurementRecords().length;s.status=count?'COMPLETE':'NO_ESTIMATE';s.message=count?`${count} walkthrough video estimate${count===1?' is':'s are'} available for field verification.`:'No additional walkthrough measurement targets remain after verified dimensions and material specifications are removed.';s.outcome=count?'ESTIMATES_READY':'NO_TARGETS';s.inputKey=key;s.updatedAt=new Date().toISOString();await C.saveDraft?.();notifyOnce();return true}if(!force&&s.inputKey===key&&['COMPLETE','NO_ESTIMATE','NO_REFERENCE','FAILED'].includes(s.status))return true;busy=true;if(!force)lastAutoKey=key;s.status='RUNNING';s.message='H38 is using verified dimensions visible in the walkthrough to estimate other same-plane measurements…';s.inputKey=key;s.updatedAt=new Date().toISOString();await C.saveDraft?.();notifyOnce();try{let attempt=await request(targets,false);if(attempt.response.status===401)attempt=await request(targets,true);if(!attempt.response.ok||attempt.payload.status!=='PASS')throw Error(attempt.payload.message||`Walkthrough measurement failed (${attempt.response.status}).`);const outcome=text(attempt.payload.outcome),estimates=Array.isArray(attempt.payload.estimates)?attempt.payload.estimates:[];if(estimates.length){const saved=await saveEstimates(attempt.api,attempt.user,estimates);s.status='COMPLETE';s.message=`${saved.length} walkthrough video estimate${saved.length===1?' is':'s are'} ready for field verification.`;s.estimates=estimates;s.outcome=outcome;s.inputKey=inputKey();C.toast?.(`${saved.length} walkthrough measurement estimate${saved.length===1?'':'s'} added to the review.`)}else{s.status=outcome==='NO_VERIFIED_REFERENCE'?'NO_REFERENCE':'NO_ESTIMATE';s.message=text(attempt.payload.message||(outcome==='NO_VERIFIED_REFERENCE'?'H38 could not find a verified reference and target on the same usable frame.':'H38 could not make a defensible same-plane measurement estimate from the current walkthrough frames.'));s.estimates=[];s.outcome=outcome}s.updatedAt=new Date().toISOString();await C.saveDraft?.();notifyOnce();return true}catch(error){s.status='FAILED';s.message=text(error?.message||error);s.updatedAt=new Date().toISOString();await C.saveDraft?.();notifyOnce();return false}finally{busy=false}}
function failClosed(error){faultCount+=1;console.warn('H38 headless walkthrough measurement fault',error);if(faultCount<2)return;runtimeDisabled=true;clearInterval(heartbeatTimer)}
function safeTick(){if(runtimeDisabled||document.visibilityState==='hidden')return;try{if(!C.state.open||!visit()?.sessionId)return;const s=measurementState(),key=inputKey();if(s?.inputKey===key&&['COMPLETE','NO_ESTIMATE','NO_REFERENCE','FAILED'].includes(s.status)){lastAutoKey=key;faultCount=0;return}if(!busy&&navigator.onLine&&latestReview()&&targetList().length&&key!==lastAutoKey){lastAutoKey=key;void run(false)}faultCount=0}catch(error){failClosed(error)}}
window.addEventListener('online',()=>setTimeout(safeTick,100));
window.addEventListener('focus',()=>setTimeout(safeTick,100));
document.addEventListener('visibilitychange',()=>{if(document.visibilityState==='visible')setTimeout(safeTick,100)});
heartbeatTimer=setInterval(safeTick,HEARTBEAT_MS);
window.H38_FIELD_VISIT_VIDEO_MEASUREMENTS=Object.freeze({build:BUILD,run,refresh:()=>safeTick(),targets:targetList,state:measurementState,method:'SAME_FRAME_REFERENCE_SCALE',source:'CAMERA_ESTIMATE',verificationStatus:'UNVERIFIED',samePlaneRequired:true,verifiedReferenceRequired:true,fieldVerificationRequired:true,automaticApproval:false,automaticCustomerSending:false,documentObserver:false,domWrites:false,idempotentRender:true,heartbeatMs:HEARTBEAT_MS,failClosed:true,integratedIntoWalkthrough:true,walkthroughOwnsMeasurementResults:true,standaloneUi:false,automaticMeasurementPass:true,automaticReviewRefinement:false,fullReviewReanalysis:false,fullGuidanceRender:false,guidedQueueRefinement:true});
setTimeout(safeTick,900);
})();
