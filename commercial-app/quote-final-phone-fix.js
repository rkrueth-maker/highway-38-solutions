(function(){
'use strict';
const BUILD='20260807-1415';
const text=v=>String(v==null?'':v);
const num=v=>Number.isFinite(Number(v))?Number(v):0;
const field=(row,...keys)=>{for(const key of keys){if(row&&row[key]!==undefined&&row[key]!==null&&row[key]!=='')return row[key];}return'';};
const esc=v=>text(v).replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[ch]));
const records=name=>Array.isArray(window.state?.snapshot?.[name])?window.state.snapshot[name]:(typeof window.records==='function'?window.records(name):[]);
const api=()=>window.H38_SUPABASE_SHARED_CLIENT?.ensure?.();
let busy=false;
let localPdfUrl='';
let displaySource={key:'',url:'',promise:null};
function quoteRecord(){const id=text(window.state?.quote?.quoteId);return records('quotes').find(row=>text(field(row,'Quote ID','quoteId'))===id)||{};}
function quoteId(){return text(window.state?.quote?.quoteId||field(quoteRecord(),'Quote ID','quoteId'));}
function sourceRows(){const id=quoteId();return records('documents').filter(row=>text(field(row,'Source Type','sourceType')).toLowerCase()==='quote'&&text(field(row,'Source ID','sourceId'))===id&&text(field(row,'Mime Type','mimeType')).toLowerCase().startsWith('image/'));}
function originalSource(){const record=quoteRecord(),wanted=text(field(record,'Render Source File','renderSourceFile')).trim(),rows=sourceRows();if(!rows.length)return null;if(wanted){const exact=rows.find(row=>text(field(row,'File Name','fileName'))===wanted||text(field(row,'Storage Path','storagePath')).endsWith('/'+wanted));if(exact)return exact;}return rows.find(row=>field(row,'Render Source','renderSource')===true&&!field(row,'Render Normalized Source','renderNormalizedSource'))||rows[0];}
function storageInfo(row){return{bucket:text(field(row,'Storage Bucket','storageBucket'))||'business-office-files',path:text(field(row,'Storage Path','storagePath')),fileName:text(field(row,'File Name','fileName')),size:num(field(row,'File Size','fileSize'))};}
function explicitRotation(){const record=quoteRecord();const raw=field(record,'Render Source Rotation','renderSourceRotation');if(raw===''||raw===null||raw===undefined)return null;return((Math.round(num(raw))%360)+360)%360;}
function exifRotation(buffer){try{const view=new DataView(buffer);if(view.getUint16(0,false)!==0xFFD8)return 0;let offset=2;while(offset+4<view.byteLength){if(view.getUint8(offset)!==0xFF){offset++;continue;}const marker=view.getUint8(offset+1),length=view.getUint16(offset+2,false);if(marker===0xE1&&length>=10){const exif=offset+4;if(view.getUint32(exif,false)===0x45786966){const tiff=exif+6,little=view.getUint16(tiff,false)===0x4949,read16=p=>view.getUint16(p,little),read32=p=>view.getUint32(p,little),ifd=tiff+read32(tiff+4),count=read16(ifd);for(let i=0;i<count;i++){const entry=ifd+2+i*12;if(read16(entry)===0x0112){const orientation=read16(entry+8);return orientation===3?180:orientation===6?90:orientation===8?270:0;}}}}if(!length)break;offset+=2+length;}return 0;}catch(_){return 0;}}
async function signedUrl(row,expires=900){const info=storageInfo(row),client=api();if(!client||!info.path)return'';const result=await client.storage.from(info.bucket).createSignedUrl(info.path,expires);if(result.error)throw result.error;return text(result.data?.signedUrl);}
async function sourceBlob(row){const url=await signedUrl(row,900);if(!url)throw new Error('Original jobsite photo could not be opened.');const response=await fetch(url,{cache:'no-store'});if(!response.ok)throw new Error(`Original jobsite photo could not be loaded (${response.status}).`);return response.blob();}
async function normalizedBlob(row,forceRotation){const blob=await sourceBlob(row),buffer=await blob.arrayBuffer(),rotation=forceRotation==null?(explicitRotation()??exifRotation(buffer)):forceRotation;if(typeof createImageBitmap!=='function')throw new Error('This phone browser cannot normalize the jobsite photo.');let bitmap;try{bitmap=await createImageBitmap(new Blob([buffer],{type:blob.type||'image/jpeg'}),{imageOrientation:'none'});}catch(_){bitmap=await createImageBitmap(new Blob([buffer],{type:blob.type||'image/jpeg'}));}const radians=rotation*Math.PI/180,swap=rotation===90||rotation===270,max=1800,rawW=swap?bitmap.height:bitmap.width,rawH=swap?bitmap.width:bitmap.height,scale=Math.min(1,max/Math.max(rawW,rawH)),outW=Math.max(1,Math.round(rawW*scale)),outH=Math.max(1,Math.round(rawH*scale)),canvas=document.createElement('canvas');canvas.width=outW;canvas.height=outH;const ctx=canvas.getContext('2d',{alpha:false});ctx.fillStyle='#fff';ctx.fillRect(0,0,outW,outH);ctx.translate(outW/2,outH/2);ctx.rotate(radians);const drawW=bitmap.width*scale,drawH=bitmap.height*scale;ctx.drawImage(bitmap,-drawW/2,-drawH/2,drawW,drawH);bitmap.close?.();const out=await new Promise(resolve=>canvas.toBlob(resolve,'image/jpeg',.92));if(!out)throw new Error('The correctly oriented jobsite photo could not be created.');return{blob:out,rotation,width:outW,height:outH};}
async function displayUrl(){const row=originalSource();if(!row)return'';const info=storageInfo(row),rot=explicitRotation(),key=`${quoteId()}|${info.path}|${rot==null?'auto':rot}`;if(displaySource.key===key&&displaySource.url)return displaySource.url;if(displaySource.key===key&&displaySource.promise)return displaySource.promise;displaySource.key=key;displaySource.promise=(async()=>{const normalized=await normalizedBlob(row,rot);if(displaySource.url)URL.revokeObjectURL(displaySource.url);displaySource.url=URL.createObjectURL(normalized.blob);return displaySource.url;})().finally(()=>{displaySource.promise=null;});return displaySource.promise;}
async function normalizeOriginals(root=document){const url=await displayUrl().catch(()=> '');if(!url)return;root.querySelectorAll?.('.quote-render-images figure,.quote-concept-grid figure').forEach(figure=>{const caption=text(figure.querySelector('figcaption')?.textContent).toLowerCase();if(!caption.includes('before')&&!caption.includes('original jobsite'))return;const img=figure.querySelector('img');if(!img||img.dataset.h38NormalizedSource===displaySource.key)return;img.src=url;img.dataset.h38NormalizedSource=displaySource.key;img.style.transform='none';img.style.width='100%';img.style.height='auto';img.style.maxHeight='420px';img.style.objectFit='contain';img.style.imageOrientation='none';});}
async function ensureNormalizedRenderSource(){const row=originalSource();if(!row)return null;const businessId=text(window.state?.businessId),id=quoteId(),client=api();if(!businessId||!id||!client)return null;const original=storageInfo(row),normalized=await normalizedBlob(row,explicitRotation()),base=text(original.fileName||'jobsite.jpg').replace(/\.[^.]+$/,''),fileName=`${base}-upright-r${normalized.rotation}.jpg`,path=`${businessId}/Quote/${id}/source-normalized/${fileName}`,upload=await client.storage.from('business-office-files').upload(path,normalized.blob,{contentType:'image/jpeg',upsert:true,cacheControl:'3600'});if(upload.error)throw upload.error;let userId=text(window.H38_SUPABASE_AUTH?.getState?.().userId);if(!userId){const session=await client.auth.getSession();userId=text(session.data?.session?.user?.id);}if(!userId)throw new Error('Supabase Auth session is required to prepare the render source.');const recordKey=`NORMALIZED-RENDER-SOURCE-${id}`,payload={'Document ID':recordKey,'Business ID':businessId,'File Name':fileName,'File Size':normalized.blob.size,'Mime Type':'image/jpeg','Source Type':'Quote','Source ID':id,'Storage Bucket':'business-office-files','Storage Path':path,'Access Classification':'Internal','Status':'Available — Private','Render Source':true,'Render Normalized Source':true,'Normalized From File':original.fileName,'Normalized From Path':original.path,'Source Rotation Applied':normalized.rotation,'Created Time':new Date().toISOString(),'Updated Time':new Date().toISOString(),'Record Version':1};const saved=await client.from('business_records').upsert({business_id:businessId,collection:'documents',record_key:recordKey,payload,record_status:'active',created_by:userId,updated_by:userId,updated_at:new Date().toISOString()},{onConflict:'business_id,collection,record_key'});if(saved.error)throw saved.error;if(window.state?.snapshot){if(!Array.isArray(window.state.snapshot.documents))window.state.snapshot.documents=[];const list=window.state.snapshot.documents,index=list.findIndex(item=>text(field(item,'Document ID','documentId'))===recordKey);if(index>=0)list[index]=payload;else list.unshift(payload);}return{fileName,path,bucket:'business-office-files',rotation:normalized.rotation,originalFileName:original.fileName};}
function installRenderNormalization(){const Bridge=window.H38Bridge;if(!Bridge?.prototype||Bridge.prototype.__h38UprightRenderSource)return;const previous=Bridge.prototype.request;Bridge.prototype.request=async function(action,args,timeout){if(action!=='aiRenderQuoteConcept')return previous.call(this,action,args,timeout);const q=window.state?.quote||{},oldName=q.renderSourceFileName,oldPath=q.renderSourcePath,normalized=await ensureNormalizedRenderSource();if(!normalized)return previous.call(this,action,args,timeout);q.renderSourceFileName=normalized.fileName;q.renderSourcePath=normalized.path;const next=Object.assign({},args||{}, {renderSourceFileName:normalized.fileName,renderSourcePath:normalized.path,renderSourceRotation:0,notes:[text(args?.notes),`RENDER SOURCE IS ALREADY PHYSICALLY UPRIGHT: ${normalized.fileName}. Do not rotate, mirror, crop, replace, or reconstruct the yard. Edit this exact image in place and preserve all unaffected pixels and perspective.`].filter(Boolean).join('\n\n')});try{return await previous.call(this,action,next,timeout);}finally{q.renderSourceFileName=oldName;q.renderSourcePath=oldPath;}};Bridge.prototype.__h38UprightRenderSource=true;}
function customerRecord(){const q=window.state?.quote||{},record=quoteRecord(),customerId=text(q.customerId||field(record,'Customer ID','customerId'));return records('customers').find(row=>text(field(row,'Customer ID','customerId'))===customerId)||{};}
function quoteData(){const q=window.state?.quote||{},record=quoteRecord(),customer=customerRecord(),lines=Array.isArray(q.lines)?q.lines:[],addOns=Array.isArray(q.aiOptionalAddOns)?q.aiOptionalAddOns:[],total=lines.reduce((sum,line)=>sum+num(line.quantity)*num(line.unitPrice),0)+num(field(record,'Tax','tax'));return{quoteId:quoteId(),quoteNumber:text(q.quoteNumber||field(record,'Quote Number','quoteNumber')||q.quoteId||'Quote'),revision:Math.max(1,Math.trunc(num(q.revision||field(record,'Revision','revision')||1))),title:text(q.projectTitle||field(record,'Project Title','projectTitle')||'Project Proposal'),scope:text(q.scope||field(record,'Customer Scope','customerScope','Scope','scope')||'Work will be completed as described in the itemized proposal.'),customerName:text(field(customer,'Customer Name','Name','customerName','name')||window.customerName?.(q.customerId)||'Quote Customer'),lines,addOns,total};}
function safeFile(value){return text(value).replace(/[^A-Za-z0-9._-]+/g,'-').replace(/^-+|-+$/g,'').slice(0,120)||'quote';}
function statusPanel(){let node=document.getElementById('h38RealPdfStatus');if(node)return node;const tools=document.querySelector('.page-tools');if(!tools)return null;node=document.createElement('div');node.id='h38RealPdfStatus';node.className='notice h38-real-pdf-status';node.setAttribute('role','status');node.setAttribute('aria-live','polite');tools.insertAdjacentElement('afterend',node);return node;}
function showStatus(message,error){const node=statusPanel();if(node){node.className=`notice h38-real-pdf-status${error?' warn':''}`;node.textContent=message;}window.toast?.(message,Boolean(error));}
function showPdfLinks(viewUrl,downloadUrl,fileName,server){const node=statusPanel();if(!node)return;node.className='notice h38-real-pdf-status';node.innerHTML=`<strong>${server?'Stored proposal PDF':'PDF created'}.</strong><div class="actions h38-pdf-actions"><a class="btn" id="h38OpenGeneratedPdf" href="${esc(viewUrl)}" target="_blank" rel="noopener">Open PDF</a><a class="btn secondary" href="${esc(downloadUrl||viewUrl)}" target="_blank" rel="noopener" ${server?'':'download="'+esc(fileName)+'"'}>Download PDF</a></div><span class="muted">Open the PDF first, then use the Android PDF viewer Print command if you want a paper copy. Nothing is sent by this button.</span>`;node.scrollIntoView({block:'nearest',behavior:'smooth'});}
async function storedPdf(){const record=quoteRecord(),path=text(field(record,'PDF Storage Path','pdfStoragePath'));if(!path)return null;const client=api();if(!client)throw new Error('Secure storage is not ready.');const [view,download]=await Promise.all([client.storage.from('customer-portal').createSignedUrl(path,600),client.storage.from('customer-portal').createSignedUrl(path,600,{download:true})]);if(view.error)throw view.error;if(!view.data?.signedUrl)throw new Error('Stored proposal PDF could not be opened.');return{viewUrl:view.data.signedUrl,downloadUrl:download.data?.signedUrl||view.data.signedUrl,fileName:path.split('/').pop()||'quote.pdf'};}
function pdfText(value){return text(value).replace(/[^\x20-\x7E]/g,' ');}
async function buildLocalPdf(){const d=quoteData();if(!d.quoteId||!d.lines.length)throw new Error('Open a saved quote with line items before creating a PDF.');if(!window.PDFLib?.PDFDocument)throw new Error('PDF engine did not load. Refresh the Business Office and try again.');const{PDFDocument,StandardFonts,rgb}=window.PDFLib,pdf=await PDFDocument.create(),regular=await pdf.embedFont(StandardFonts.Helvetica),bold=await pdf.embedFont(StandardFonts.HelveticaBold),pageSize=[612,792],margin=44;let page=pdf.addPage(pageSize),y=744;const newPage=()=>{page=pdf.addPage(pageSize);y=744;};const draw=(value,size=10,strong=false)=>{if(y<55)newPage();page.drawText(pdfText(value).slice(0,520),{x:margin,y,size,font:strong?bold:regular,color:rgb(.05,.12,.18)});y-=size+6;};page.drawText('HIGHWAY 38 SOLUTIONS',{x:margin,y:752,size:18,font:bold,color:rgb(.05,.12,.18)});y=716;draw(`Quote ${d.quoteNumber} - Revision ${d.revision}`,11,true);draw(`Prepared for: ${d.customerName}`,11,true);draw(d.title,16,true);draw(d.scope,9);y-=8;draw('Itemized proposal',11,true);for(const item of d.lines){const qty=num(item.quantity),rate=num(item.unitPrice);draw(`${text(item.customerDescription||item.description||'Project work').slice(0,62)} - ${qty} ${text(item.unit||'each')} - $${(qty*rate).toFixed(2)}`,9);}if(d.addOns.length){y-=6;draw('Optional add-ons - not included in total',11,true);for(const item of d.addOns){const qty=num(item.quantity||1),rate=num(item.unitPrice);draw(`[ ] ${text(item.description||'Optional work').slice(0,58)} - $${(qty*rate).toFixed(2)}`,9);}}y-=8;draw(`PROPOSAL TOTAL: $${d.total.toFixed(2)}`,14,true);const source=originalSource();if(source){try{const normalized=await normalizedBlob(source,explicitRotation()),bytes=new Uint8Array(await normalized.blob.arrayBuffer()),image=await pdf.embedJpg(bytes),p=pdf.addPage(pageSize),scale=Math.min(524/image.width,650/image.height,1),w=image.width*scale,h=image.height*scale;p.drawText('Before - original jobsite',{x:margin,y:744,size:12,font:bold,color:rgb(.05,.12,.18)});p.drawImage(image,{x:margin,y:720-h,width:w,height:h});}catch(error){console.warn('PDF source image skipped',error);}}const bytes=await pdf.save(),blob=new Blob([bytes],{type:'application/pdf'});if(localPdfUrl)URL.revokeObjectURL(localPdfUrl);localPdfUrl=URL.createObjectURL(blob);return{viewUrl:localPdfUrl,downloadUrl:localPdfUrl,fileName:`${safeFile(d.quoteNumber)}-revision-${d.revision}.pdf`};}
async function openPdf(){if(busy)return;busy=true;const button=document.getElementById('h38CreatePdfButton'),target=window.open('about:blank','_blank');if(target)target.opener=null;if(button){button.disabled=true;button.textContent='Opening PDF...';}window.H38_WORKING_HAMMER?.show?.('Opening quote PDF','Using the stored customer PDF when available',button||null);try{const stored=await storedPdf();if(stored){showPdfLinks(stored.viewUrl,stored.downloadUrl,stored.fileName,true);if(target&&!target.closed)target.location.replace(stored.viewUrl);else window.location.assign(stored.viewUrl);return;}const local=await buildLocalPdf();showPdfLinks(local.viewUrl,local.downloadUrl,local.fileName,false);if(target&&!target.closed)target.location.replace(local.viewUrl);}catch(error){try{target?.close();}catch(_){}showStatus(`PDF could not open: ${error.message||error}`,true);}finally{busy=false;if(button){button.disabled=false;button.textContent=text(field(quoteRecord(),'PDF Storage Path','pdfStoragePath'))?'Open PDF':'Create PDF';}window.H38_WORKING_HAMMER?.hide?.(button||null);}}
function preparePdfControl(){const old=document.getElementById('printQuoteButton')||document.getElementById('h38PhonePrintSaveButton')||document.getElementById('h38CreatePdfButton');if(!old)return;old.id='h38CreatePdfButton';old.textContent=text(field(quoteRecord(),'PDF Storage Path','pdfStoragePath'))?'Open PDF':'Create PDF';old.removeAttribute('onclick');let note=document.getElementById('h38PdfHelp');if(!note){note=document.createElement('span');note.id='h38PdfHelp';note.className='muted h38-pdf-help';note.textContent='Opens a real PDF file. Print from the Android PDF viewer.';old.insertAdjacentElement('afterend',note);}}
function refresh(){preparePdfControl();void normalizeOriginals(document);installRenderNormalization();}
const observer=new MutationObserver(()=>refresh());observer.observe(document.documentElement,{subtree:true,childList:true});document.addEventListener('click',event=>{const button=event.target?.closest?.('#h38CreatePdfButton');if(!button)return;event.preventDefault();event.stopPropagation();event.stopImmediatePropagation?.();void openPdf();},true);
const style=document.createElement('style');style.textContent='.h38-pdf-help{display:inline-block;max-width:320px;margin-left:8px;vertical-align:middle}.h38-pdf-actions{margin-top:10px}.h38-real-pdf-status a.btn{text-decoration:none}.quote-render-images img,.quote-concept-grid img{image-orientation:none}@media(max-width:640px){.h38-pdf-help{display:block;margin:6px 0 0}}@media print{.h38-pdf-help,.h38-real-pdf-status{display:none!important}}';document.head.appendChild(style);setTimeout(refresh,0);
window.H38_QUOTE_FINAL_PHONE_FIX=Object.freeze({enabled:true,build:BUILD,storedPdfFirst:true,actualPdfViewer:true,noWindowPrint:true,explicitSourceRotation:true,normalizedRenderSource:true,automaticSending:false,openPdf,ensureNormalizedRenderSource,normalizeOriginals,refresh});
})();
