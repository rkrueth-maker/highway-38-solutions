(function(){
'use strict';
const BUILD='20260807-1325';
const config=window.H38_CUSTOMER_PORTAL_SUPABASE||{};
let requestedHandled=false;
const text=value=>String(value==null?'':value);
function matchingPdf(quote,files){const quoteNumber=text(quote?.quote_number).trim();if(!quoteNumber)return null;return (files||[]).find(file=>{const name=text(file?.file_name);return name===`${quoteNumber}.pdf`||name.startsWith(`${quoteNumber}-revision-`);})||null;}
function decorate(){const portal=window.H38_CUSTOMER_PORTAL?.getState?.();if(!portal?.account)return;for(const quote of portal.quotes||[]){const card=document.querySelector(`[data-quote-id="${CSS.escape(quote.id)}"]`);if(!card||card.querySelector('[data-quote-pdf]'))continue;const file=matchingPdf(quote,portal.files);if(!file?.storage_path)continue;const button=document.createElement('button');button.type='button';button.className='btn';button.dataset.quotePdf=file.storage_path;button.textContent='Open quote PDF';button.addEventListener('click',()=>{const target=window.open('about:blank','_blank');if(target)target.opener=null;void openPdf(portal.client,file.storage_path,target).catch(error=>{console.error('Quote PDF open failed',error);try{target?.close();}catch(_){}});});card.appendChild(button);}if(requestedHandled)return;const requested=new URLSearchParams(location.search).get('quote');if(!requested)return;const card=document.querySelector(`[data-quote-id="${CSS.escape(requested)}"]`);if(!card)return;requestedHandled=true;card.classList.add('is-requested-quote');card.scrollIntoView({behavior:'smooth',block:'center'});card.querySelector('[data-review-quote]')?.focus({preventScroll:true});}
async function openPdf(client,path,target){if(!client)throw new Error('Secure portal session is not ready.');const timeout=new Promise((_,reject)=>setTimeout(()=>reject(new Error('Secure PDF link timed out. Press Open quote PDF again.')),15000));const request=client.storage.from(config.storageBucket||'customer-portal').createSignedUrl(path,300);const{data,error}=await Promise.race([request,timeout]);if(error)throw error;if(!data?.signedUrl)throw new Error('A secure PDF link was not created.');if(target&&!target.closed){target.location.replace(data.signedUrl);return;}window.location.assign(data.signedUrl);}
window.addEventListener('h38:portal-data',decorate);
window.H38_CUSTOMER_QUOTE_DELIVERY={enabled:true,build:BUILD,privatePdf:true,eventDriven:true,noDocumentMutationPolling:true,noExtraQuoteQuery:true,openPdf};
})();
