const txt=v=>String(v??'').trim();
const num=v=>{const n=Number(v);return Number.isFinite(n)?n:null};
const clean=s=>txt(s).replace(/<[^>]*>/g,' ').replace(/&nbsp;|&#160;/gi,' ').replace(/&amp;/gi,'&').replace(/\s+/g,' ').trim();
export function parseIndicators(html=''){const out=[],seen=new Set(),re=/(\d{11,14})/g;let m;while((m=re.exec(String(html)))){const upc=m[1];if(seen.has(upc))continue;const a=Math.max(0,m.index-180),b=Math.min(String(html).length,m.index+180),ctx=clean(String(html).slice(a,b));if(!/upc|remodel|product|item|beauty|gift|food|frozen|refriger|pot pie/i.test(ctx))continue;seen.add(upc);let name=ctx.replace(upc,' ').replace(/\bUPC\b\s*:?/ig,' ').replace(/\s+/g,' ').trim();if(name.length>150)name=name.slice(-150);out.push({upc,name:name||`Indicator ${upc}`,source:'Penny Puss indicator list'})}return out.slice(0,80)}
export function parseMinnesotaCandidates(html=''){const text=clean(html),out=[],seen=new Set(),re=/([A-Za-z0-9 .#'&-]{3,80}),?\s+(Grand Rapids|Bemidji|Hibbing|Duluth|Virginia|Bovey|Coleraine|Deer River|Cohasset|Eveleth|Chisholm|International Falls|Brainerd|Baxter)[, ]+MN(?:\s+(\d{5}))?/gi;let m;while((m=re.exec(text))){const key=`${m[2].toLowerCase()}|${m[3]||''}`;if(seen.has(key))continue;seen.add(key);out.push({label:`${m[1].trim()} ${m[2]}, MN${m[3]?` ${m[3]}`:''}`.replace(/\s+/g,' '),city:m[2],state:'MN',postal:m[3]||'',source:'Penny Puss Minnesota remodel list',evidence_level:'COMMUNITY SIGNAL'})}return out.slice(0,60)}
export function normalizeProbe(row={},indicator={}){const regular=num(row?.price??row?.regular_price??row?.list_price),effective=num(row?.effective_price??row?.discount_price??row?.sale_price??row?.price),ratio=regular&&effective!=null&&regular>0?effective/regular:null;return{upc:txt(row?.upc||row?.barcode||indicator?.upc),name:txt(row?.name||row?.title||indicator?.name),regular_price:regular,effective_price:effective,markdown_ratio:ratio,in_stock:row?.in_stock===true,available_quantity:num(row?.available_quantity),store_id:txt(row?.store_id),source_url:txt(row?.source_url)}}
export function scoreStore(store={},probes=[],community=false){let score=community?30:0,deep=0,moderate=0,cold=false;for(const p of probes){const r=num(p.markdown_ratio);if(r!=null&&r<=.55){score+=18;deep++}else if(r!=null&&r<=.75){score+=8;moderate++}if(/pot pie|frozen|freezer|refriger/i.test(txt(p.name))&&r!=null&&r<=.6){score+=18;cold=true}}if(deep>=3)score+=18;else if(deep>=2)score+=10;if(probes.length>=4)score+=5;score=Math.min(100,score);let label='NO REMODEL SIGNAL';if(score>=80)label='PENNY WINDOW CANDIDATE';else if(score>=60)label='REMODEL LIKELY';else if(score>=35)label='REMODEL POSSIBLE';else if(community)label='COMMUNITY SIGNAL';return{...store,score,label,indicator_hits:deep,moderate_hits:moderate,cold_case_signal:cold,community_signal:community,evidence_level:deep>=2?'LIVE PARTIAL':community?'COMMUNITY SIGNAL':'LIVE PARTIAL',truth:'Remodel score is a probability signal, not an official remodel schedule. Verify current store prices; physical register scan remains final penny truth.'}}
