import "jsr:@supabase/functions-js/edge-runtime.d.ts";

const html = String.raw`<!doctype html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#2e5a36"><title>H38 Couponing</title>
<style>
*{box-sizing:border-box}body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;background:#f1f6f1;color:#16301c}.wrap{max-width:680px;margin:auto;padding:calc(12px + env(safe-area-inset-top)) 12px calc(32px + env(safe-area-inset-bottom))}.top{display:flex;align-items:center;gap:10px}.top a{text-decoration:none;color:#356543;font-weight:800}.top h1{font-size:25px;margin:0}.card{background:#fff;border:1px solid #d7e5d9;border-radius:18px;padding:14px;margin:10px 0}.nav{display:grid;grid-template-columns:repeat(5,minmax(0,1fr));gap:5px;padding:4px 0}.nav button{white-space:normal;background:#dfece1;color:#1e4b29;padding:8px 5px;font-size:11px;line-height:1.05;min-width:0}.nav button.active{background:#2e5a36;color:#fff}button{border:0;border-radius:12px;padding:11px 13px;background:#2e5a36;color:#fff;font-weight:760;font:inherit;min-height:44px}button.secondary{background:#dfece1;color:#1e4b29}button.remove{background:#f4e3e3;color:#7d2b2b;padding:7px 9px}button:disabled{opacity:.55}input,select,textarea{width:100%;min-width:0;font:inherit;padding:12px;border:1px solid #cbdacb;border-radius:12px;background:#fff}textarea{min-height:88px}.row{display:flex;gap:8px;min-width:0}.row>*{flex:1 1 0;min-width:0;max-width:100%}.stack{display:grid;gap:8px}.small{font-size:12px;color:#607564}.status{font-size:13px;color:#55705c;min-height:18px}.item{border-top:1px solid #e1ebe2;padding:11px 0}.item:first-child{border:0}.item h3{margin:0 0 3px;font-size:16px}.price{font-size:20px;font-weight:850}.badge{display:inline-block;font-size:11px;padding:4px 7px;border-radius:999px;background:#e6efe7;margin:2px 4px 2px 0}.hidden{display:none}.empty{text-align:center;padding:22px 8px;color:#6f8272}.trip{border-left:4px solid #2e5a36;padding-left:10px;margin:10px 0}.big{font-size:24px;font-weight:900}.kpi{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.kpi div{background:#eef5ef;border-radius:12px;padding:9px;text-align:center}.kpi strong{display:block;font-size:17px}.toolbar{display:flex;gap:7px;flex-wrap:wrap}.toolbar button{flex:1 1 120px}.scanbox{text-align:center;padding:18px;border:2px dashed #bfd2c1;border-radius:16px}.section-title{display:flex;align-items:center;justify-content:space-between;gap:8px;margin:0 0 8px}.section-title h2{font-size:19px;margin:0}.checkbox{width:auto;transform:scale(1.2);margin-right:8px}.note{padding:10px;border-radius:12px;background:#eef5ef}.warn{background:#fff3d7}.match{background:#f7faf7;border:1px solid #dbe8dc;border-radius:12px;padding:10px;margin:7px 0}.match a{font-size:12px}.strong{background:#dff3e5}.watch-state{margin:7px 0;padding:9px;border-radius:11px;background:#eef5ef}.watch-hit{background:#dff3e5}.watch-none{background:#f4f6f4;color:#5f7062}.item,.match,.watch-state,.trip,.section-title,.stack{min-width:0}.item,.match,.watch-state,.trip,.small,a{overflow-wrap:anywhere}.flow-note{font-size:11px;color:#607564;margin:2px 2px 6px}.coverage-note{padding:9px 10px;border-radius:12px;background:#fff8e8;color:#6e5414;font-size:12px;margin-top:8px}@media(max-width:480px){.row{flex-wrap:wrap}.row>*{min-width:min(120px,100%)}}
</style></head>
<body data-h38-coupon-web="amazon-web-discovery-v1"><main class="wrap"><div class="top"><a href="/functions/v1/h38-deals-shell">‹ H38 Deals</a><h1>Couponing</h1></div><section id="locked" class="card hidden"><h2>Not enabled</h2><p>This account does not include Couponing.</p></section><div id="app" class="hidden"><section class="card"><div class="section-title"><h2>Savings Copilot</h2><div style="text-align:right"><span id="sharing" class="small">Checking sharing…</span><br><span id="sync" class="small">Loading…</span></div></div><p class="small">Build one shared list together. H38 refreshes supported retailer offers and combines them with saved H38 deal evidence. Local store price, stock and coupon eligibility stay clearly labeled until verified.</p><p class="small">Changes to the shared list, saved prices, watches and receipts are available to both household members.</p><div id="status" class="status"></div></section><section class="card"><div class="section-title"><h2>Shopping location</h2><span class="small">Shared across H38 Deals</span></div><div class="row"><input id="zip" inputmode="numeric" maxlength="5" placeholder="ZIP code" aria-label="Shopping ZIP code"><select id="radius" aria-label="Search radius"><option>25</option><option selected>50</option><option>75</option><option>100</option></select></div><button id="useLocation" class="secondary" style="width:100%;margin-top:8px">Use phone location</button></section><section id="handoff" class="card hidden"><div class="section-title"><h2>Deal brought from H38</h2></div><div id="handoffDetail" class="small"></div><div class="toolbar" style="margin-top:9px"><button id="handoffList" class="secondary">Add to shopping list</button><button id="handoffStack">Prepare coupon stack</button></div><p class="small">Nothing is saved until you choose an action and confirm it.</p></section><div class="flow-note">1 Build list · 2 Find savings · 3 Plan trip · 4 Shop · 5 Save receipt</div><div class="nav"><button data-view="shop" class="active">1 LIST</button><button data-view="deals">2 SAVINGS</button><button data-view="save">3 PLAN</button><button data-view="scan">4 SHOP</button><button data-view="receipts">5 RECEIPT</button></div><section id="view" class="card"></section></div></main>
<input id="handwriteFile" type="file" accept="image/*" capture="environment" hidden><input id="receiptFile" type="file" accept="image/*" capture="environment" hidden>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.95.0/dist/umd/supabase.min.js" onerror="this.onerror=null;this.src='https://unpkg.com/@supabase/supabase-js@2.95.0/dist/umd/supabase.min.js'"></script>
<script>
const SUPABASE_URL='https://jqukmwtsgcsaruucnqja.supabase.co',KEY='sb_publishable_XrF41kGmTC2SmSTgPvo5OQ_vqcBd0N1',sb=supabase.createClient(SUPABASE_URL,KEY),$=id=>document.getElementById(id);const state={view:'shop',list:null,items:[],prices:[],receipts:[],watch:[],matches:[],watchMatches:{},watchCheckedAt:null,plan:null,barcode:'',receiptText:'',loading:false,handoff:null,householdName:'',amazonConfigured:null};const SHARED_LOCATION_KEY='h38-shopping-location-v1',money=v=>'$'+Number(v||0).toFixed(2);
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c];});}
function effective(p){return Math.max(0,Number(p.shelf_price||0)*Math.max(1,Number(p.required_qty||1))-Number(p.sale_discount||0)-Number(p.store_coupon||0)-Number(p.manufacturer_coupon||0)-Number(p.rebate||0)-Number(p.loyalty_value||0));}
function status(t){$('status').textContent=t||'';}
function amazonAsin(v){const s=String(v||'').trim(),d=s.match(/^[A-Z0-9]{10}$/i);if(d)return d[0].toUpperCase();const m=s.match(/(?:\/dp\/|\/gp\/product\/|asin=)([A-Z0-9]{10})/i);return m?m[1].toUpperCase():'';}
function amazonUrl(v,item){const s=String(v||'').trim();if(/^https:\/\/(?:www\.)?amazon\.com\//i.test(s)||/^https:\/\/amzn\.to\//i.test(s))return s;const a=amazonAsin(s);return a?'https://www.amazon.com/dp/'+a:'https://www.amazon.com/s?k='+encodeURIComponent(String(item||''));}
function saveSharedLocation(){const old=readSharedLocation(),row={zip:$('zip').value.trim(),radius:$('radius').value,radiusMiles:Number($('radius').value||50),lat:old.lat,lon:old.lon};if(row.zip){delete row.lat;delete row.lon;}localStorage.setItem(SHARED_LOCATION_KEY,JSON.stringify(row));}
function readSharedLocation(){try{return JSON.parse(localStorage.getItem(SHARED_LOCATION_KEY)||localStorage.getItem('h38-penny-shopping-location-v1')||localStorage.getItem('h38-resale-location-v1')||'{}')||{};}catch(e){return {};}}
function restoreSharedLocation(){const x=readSharedLocation();$('zip').value=x.zip||'';$('radius').value=String(x.radius||x.radiusMiles||50);if(Object.keys(x).length)localStorage.setItem(SHARED_LOCATION_KEY,JSON.stringify(x));}
function acceptSharedLocation(a,b){const lat=Number(a),lon=Number(b);if(!Number.isFinite(lat)||!Number.isFinite(lon)){status('Location was unavailable. Enter a ZIP instead.');return;}localStorage.setItem(SHARED_LOCATION_KEY,JSON.stringify({zip:'',radius:$('radius').value,radiusMiles:Number($('radius').value||50),lat,lon}));$('zip').value='';status('Phone location saved for Deals, Resale and Couponing.');}
function useSharedLocation(){status('Getting phone location…');if(window.AndroidH38Deals&&AndroidH38Deals.requestLocation){AndroidH38Deals.requestLocation();return;}if(navigator.geolocation)navigator.geolocation.getCurrentPosition(p=>acceptSharedLocation(p.coords.latitude,p.coords.longitude),()=>status('Location permission was unavailable. Enter a ZIP instead.'),{timeout:12000});else status('Location is unavailable. Enter a ZIP instead.');}
function readHandoff(){const p=new URLSearchParams(location.search),h={item:String(p.get('item')||'').trim(),store:String(p.get('store')||'').trim(),upc:String(p.get('upc')||'').trim(),sku:String(p.get('sku')||'').trim(),buy:String(p.get('buy')||'').trim()};return h.item||h.upc||h.sku?h:null;}
function showHandoff(){state.handoff=readHandoff();if(!state.handoff)return;const h=state.handoff;$('handoff').classList.remove('hidden');$('handoffDetail').textContent=[h.item,h.store,h.upc?'UPC '+h.upc:'',h.sku?'SKU '+h.sku:'',h.buy?'Shelf price $'+h.buy:''].filter(Boolean).join(' · ');$('handoffList').onclick=()=>addItem(h.item||h.upc||h.sku).catch(e=>status(e.message));$('handoffStack').onclick=function(){state.view='deals';render();$('dealItem').value=h.item;$('dealStore').value=h.store;$('dealBarcode').value=h.upc||h.sku;$('dealShelf').value=h.buy;status('Deal copied into the stack builder. Add verified coupons or rebates, then save.');};}
async function gate(){const session=(await sb.auth.getSession()).data.session;if(!session){location.href='/functions/v1/h38-deals-shell';return false;}const q=await sb.from('h38_product_entitlements').select('active,expires_at').eq('product_key','coupon').maybeSingle();const ok=!!(q.data&&q.data.active&&(!q.data.expires_at||new Date(q.data.expires_at)>new Date()));$('app').classList.toggle('hidden',!ok);$('locked').classList.toggle('hidden',ok);return ok;}
async function ensureList(){const q=await sb.from('coupon_shopping_lists').select('*').in('status',['active','shopping']).order('updated_at',{ascending:false}).limit(1);if(q.error)throw q.error;if(q.data&&q.data[0]){state.list=q.data[0];return;}const ins=await sb.from('coupon_shopping_lists').insert({name:'Shopping List'}).select().single();if(ins.error)throw ins.error;state.list=ins.data;}
async function invokeMatches(entries){const rows=(entries||[]).map(x=>typeof x==='string'?{item_name:x}:x).map(x=>({id:x.id||'',item_name:String(x.item_name||x.name||'').trim(),source:String(x.source||'all'),source_ref:String(x.source_ref||''),source_url:String(x.source_url||'')})).filter(x=>x.item_name||x.source_ref);if(!rows.length)return[];const q=await sb.functions.invoke('h38-coupon-api',{body:{action:'matches',items:rows}});if(q.error)throw q.error;if(q.data&&q.data.error)throw new Error(q.data.error);if(q.data&&q.data.amazon)state.amazonConfigured=!!q.data.amazon.configured;return (q.data&&q.data.matches)||[];}
async function refreshMatches(showStatus){const items=state.items.filter(x=>!x.checked);if(!items.length){state.matches=[];if(showStatus)status('Shopping list is empty.');return;}state.matches=await invokeMatches(items.map(x=>x.item_name));if(showStatus)status(state.matches.length?state.matches.length+' current deal match'+(state.matches.length===1?'':'es')+' found. Retailer-current results may still need local price/stock verification.':'Refresh complete. No strong current deal matches were found for this list yet.');}
async function refreshWatchMatches(showStatus,force=false){const watches=(state.watch||[]).filter(x=>x.enabled!==false);state.watchMatches={};if(!watches.length){state.watchCheckedAt=new Date();if(showStatus)status('No active watch items yet.');return;}const regular=watches.filter(w=>String(w.source||'all')!=='amazon'),amazon=watches.filter(w=>String(w.source||'all')==='amazon'),matches=[];if(regular.length)matches.push(...await invokeMatches(regular));let webMeta=null;if(amazon.length){const q=await sb.functions.invoke('h38-coupon-api',{body:{action:'web_discover',force:!!force}});if(q.error)throw q.error;if(q.data&&q.data.error)throw new Error(q.data.error);webMeta=q.data||{};matches.push(...((q.data&&q.data.matches)||[]));}for(const w of watches){const source=String(w.source||'all'),key=source==='amazon'?String(w.id):String(w.item_name||'').trim().toLowerCase();state.watchMatches[key]=source==='amazon'?matches.filter(m=>String(m.watch_id||'')===String(w.id)):matches.filter(m=>String(m.item_name||'').trim().toLowerCase()===key);}state.watchCheckedAt=new Date();if(showStatus){let hitCount=0,amazonResults=0;for(const w of watches){const source=String(w.source||'all'),key=source==='amazon'?String(w.id):String(w.item_name||'').trim().toLowerCase(),hits=state.watchMatches[key]||[],target=Number(w.target_effective_price),prices=hits.map(x=>x.buy_price==null?NaN:Number(x.buy_price)).filter(Number.isFinite);if(source==='amazon')amazonResults+=hits.length;if(String(w.watch_mode||'specific')==='discovery'&&hits.length)hitCount++;else if(source==='amazon'&&Number.isFinite(target)&&target>0){if(prices.some(p=>p<=target))hitCount++;}else if(hits.length)hitCount++;}if(amazon.length){const refreshed=Array.isArray(webMeta&&webMeta.refreshed)?webMeta.refreshed.length:0;if(force||refreshed)status('Amazon web search refreshed. '+amazonResults+' shared deal/coupon candidate'+(amazonResults===1?'':'s')+' available.');else status('Loaded cached Amazon results. The next public-web search runs when the ~12-hour check is due on app open/refresh; use Check watches now to force a search.');}else status(hitCount?hitCount+' watch target'+(hitCount===1?' is':'s are')+' currently matched. Verify final price and availability before buying.':'Watches refreshed. No watch targets are currently matched.');}}
async function loadSharing(){const q=await sb.from('coupon_households').select('name').limit(1).maybeSingle();state.householdName=q.error||!q.data?'':String(q.data.name||'').trim();if($('sharing'))$('sharing').textContent=state.householdName?'Shared · '+state.householdName:'Private';}
async function load(){if(state.loading)return;state.loading=true;try{$('sync').textContent='Syncing…';await loadSharing();await ensureList();const res=await Promise.all([sb.from('coupon_list_items').select('*').eq('list_id',state.list.id).order('created_at'),sb.from('coupon_price_observations').select('*').order('observed_at',{ascending:false}).limit(300),sb.from('coupon_receipts').select('*').order('purchased_at',{ascending:false}).limit(50),sb.from('coupon_watch_rules').select('*').eq('enabled',true).order('created_at',{ascending:false})]);res.forEach(x=>{if(x.error)throw x.error;});state.items=res[0].data||[];state.prices=res[1].data||[];state.receipts=res[2].data||[];state.watch=res[3].data||[];try{await Promise.all([refreshMatches(false),refreshWatchMatches(false)]);}catch(e){state.matches=[];state.watchMatches={};} $('sync').textContent='Saved';render();}catch(e){$('sync').textContent='Error';status('Load failed: '+e.message);}finally{state.loading=false;}}
async function addItem(name){name=String(name||'').trim();if(!name)return;if(!state.list)await ensureList();if(state.items.some(x=>String(x.item_name||'').trim().toLowerCase()===name.toLowerCase())){status('"'+name+'" is already on the shopping list.');return;}const q=await sb.from('coupon_list_items').insert({list_id:state.list.id,item_name:name}).select().single();if(q.error)throw q.error;state.items.push(q.data);$('sync').textContent='Saved';status('Saved "'+name+'" to the shopping list. Refreshing current deals and prices…');try{await refreshMatches(true);}catch(e){status('Saved "'+name+'". Saved-deal check failed: '+e.message);}if(state.view==='shop')render();}
async function addMany(text){const nl=String.fromCharCode(10),raw=String(text||'').split(',').join(nl).split(nl),seen={},pending=[];if(!state.list)await ensureList();for(let i=0;i<raw.length&&i<50;i++){let name=raw[i].replace(/^[-*0-9.) ]+/,'').trim();const key=name.toLowerCase();if(name.length<2||name.length>80||seen[key])continue;seen[key]=true;if(state.items.some(x=>String(x.item_name||'').trim().toLowerCase()===key))continue;pending.push({list_id:state.list.id,item_name:name});}if(!pending.length){status('No new shopping items to add.');return;}const q=await sb.from('coupon_list_items').insert(pending).select();if(q.error)throw q.error;state.items.push(...(q.data||[]));$('sync').textContent='Saved';status((q.data||[]).length+' item'+((q.data||[]).length===1?'':'s')+' saved. Refreshing current deals and prices…');try{await refreshMatches(true);}catch(e){status((q.data||[]).length+' item'+((q.data||[]).length===1?'':'s')+' saved. Deal refresh failed: '+e.message);}if(state.view==='shop')render();}
async function removeItem(id){const q=await sb.from('coupon_list_items').delete().eq('id',id);if(q.error)throw q.error;await load();status('Shopping item removed.');}
async function toggleItem(id,v){const q=await sb.from('coupon_list_items').update({checked:v,updated_at:new Date().toISOString()}).eq('id',id);if(q.error)throw q.error;const x=state.items.find(i=>i.id===id);if(x)x.checked=v;try{await refreshMatches(false);}catch(e){}render();}
function matchCard(m){const u=String(m.source_url||''),price=m.buy_price==null?NaN:Number(m.buy_price),each=m.effective_each==null?NaN:Number(m.effective_each),gallon=m.price_per_gallon==null?NaN:Number(m.price_per_gallon),total=m.offer_total_price==null?NaN:Number(m.offer_total_price),verify=String(m.verification_status||m.source_confidence||'VERIFY LOCAL').replace(/_/g,' ').toUpperCase(),terms=String(m.deal_terms||'').trim();return '<div class="match '+(Number(m.match_score)>=90?'strong':'')+'"><span class="badge">'+esc(m.match_label||'CURRENT MATCH')+'</span><span class="badge">'+esc(verify)+'</span><h3>'+esc(m.title||m.item_name||'Deal')+'</h3><div class="price">'+(Number.isFinite(price)?money(price):'Price check needed')+'</div>'+(Number.isFinite(each)&&Math.abs(each-price)>.001?'<div class="small">Effective each '+money(each)+'</div>':'')+(Number.isFinite(total)&&total>price+.001?'<div class="small">Offer total '+money(total)+'</div>':'')+(Number.isFinite(gallon)&&gallon>0?'<div class="small">Equivalent '+money(gallon)+'/gallon</div>':'')+(terms?'<div class="note" style="margin-top:6px">'+esc(terms)+'</div>':'')+'<div class="small">For '+esc(m.item_name||'list item')+' · '+esc(m.retailer||'')+(m.upc?' · UPC '+esc(m.upc):'')+(m.sku?' · SKU '+esc(m.sku):'')+'</div>'+(u.startsWith('https://')?'<a href="'+esc(u)+'" target="_blank" rel="noopener">Open source evidence</a>':'')+'</div>';}
function shopView(){const rows=state.items.slice(0,100).map(i=>'<div class="item"><div class="row"><label style="flex:8"><input class="checkbox" type="checkbox" data-check="'+esc(i.id)+'" '+(i.checked?'checked':'')+'>'+esc(i.item_name)+'</label><button class="remove" data-remove="'+esc(i.id)+'">Remove</button></div></div>').join(''),matches=state.matches.slice(0,30).map(matchCard).join('');return '<div class="section-title"><h2>Shopping list</h2><span class="small">'+state.items.filter(x=>!x.checked).length+' left</span></div><div class="row"><input id="newItem" placeholder="Milk, Tide, hamburger…" aria-label="Add shopping item"><button id="addItem">Add</button></div><div class="toolbar" style="margin-top:8px"><button id="findMatches" class="secondary">Refresh deals & prices</button><button id="voice" class="secondary">🎙 Speak list</button><button id="handwrite" class="secondary">📷 Scan handwritten</button><button id="recipe" class="secondary">🍲 Import recipe/list</button></div><div id="recipeBox" class="hidden" style="margin-top:8px"><textarea id="recipeText" placeholder="Paste ingredients or a shopping list, one item per line" aria-label="Recipe or shopping list text"></textarea><button id="recipeAdd" style="margin-top:6px">Add items</button></div><div style="margin-top:10px">'+(rows||'<div class="empty">Add what you need to buy.</div>')+'</div><hr style="border:0;border-top:1px solid #e1ebe2;margin:16px 0"><div class="section-title"><h2>Current deal matches</h2><span class="small">Retailer refresh + H38 evidence</span></div><p class="small">Retailer-current offers are refreshed for supported staples. Results labeled VERIFY LOCAL still require local price, stock and coupon eligibility confirmation before buying.</p>'+(matches||'<div class="empty">No current deal matches yet. Add a specific brand/product or tap Refresh deals & prices.</div>');}
function planHtml(){const p=state.plan&&state.plan.best,o=state.plan&&state.plan.one;if(!p)return state.plan?'<div class="empty">No complete verified-price plan yet. Add verified prices for the remaining unchecked items, then optimize again.</div>':'<div class="empty">Tap Optimize my list after you have verified prices saved.</div>';const diff=o?Number(o.total||0)-Number(p.total||0):0;let trips=(p.stores||[]).map(s=>'<div class="trip"><strong>'+esc(s)+'</strong>'+(p.items||[]).filter(x=>x.store===s).map(x=>'<div class="item"><b>'+esc(x.item)+'</b><div class="small">'+money(x.cost)+' · saved '+money(x.savings)+' · '+esc(x.confidence||'')+'</div></div>').join('')+'</div>').join('');return '<div class="kpi"><div><strong>'+money(p.total)+'</strong><span class="small">Real cost</span></div><div><strong>'+money(p.savings)+'</strong><span class="small">Savings</span></div><div><strong>'+((p.stores||[]).length)+'</strong><span class="small">Stops</span></div></div><p class="small">Items '+money(p.subtotal)+' + estimated travel '+money(p.travel)+'.</p>'+(o&&p.stores&&p.stores.length>1?'<div class="note '+(diff<2?'warn':'')+'">'+(diff<2?'Extra stop saves only '+money(diff)+' — skip it.':'Extra stop saves '+money(diff)+' compared with one store.')+'</div>':'')+trips+'<button id="storeMode">Start Store Mode</button>';}
function saveView(){return '<div class="section-title"><h2>Optimize my list</h2></div><div class="stack"><select id="mode"><option value="single_store" '+(state.list.optimize_mode==='single_store'?'selected':'')+'>Best single store</option><option value="two_stores" '+(state.list.optimize_mode==='two_stores'?'selected':'')+'>Cheapest within 2 stores</option><option value="practical" '+(state.list.optimize_mode==='practical'?'selected':'')+'>Best practical trip</option><option value="maximum_savings" '+(state.list.optimize_mode==='maximum_savings'?'selected':'')+'>Maximum savings</option></select><div class="row"><input id="budget" type="number" step=".01" placeholder="Budget" value="'+esc(state.list.budget==null?'':state.list.budget)+'"><input id="maxStores" type="number" min="1" max="4" value="'+esc(state.list.max_stores||2)+'"></div><textarea id="assistant" placeholder="Get this under $80. No more than two stores. Brand does not matter except Tide."></textarea><div class="row"><button id="applyAssistant" class="secondary">Apply request</button><button id="runOptimize">Optimize my list</button></div></div><div style="margin-top:12px">'+planHtml()+'</div>';}
function scanView(){const code=String(state.barcode||'').replace(/\D/g,''),matches=code?state.prices.filter(p=>String(p.barcode||'').replace(/\D/g,'')===code):[];return '<div class="section-title"><h2>Scan in store</h2></div><div class="scanbox"><div class="big">'+(state.barcode?esc(state.barcode):'Barcode')+'</div><p class="small">Scan a product to compare saved nearby prices, stacks and price history.</p><button id="scanBarcode">Scan barcode</button><input id="manualBarcode" placeholder="or type UPC" value="'+esc(state.barcode)+'" style="margin-top:8px"></div><div style="margin-top:10px">'+(matches.length?matches.map(priceCard).join(''):(state.barcode?'<div class="empty">No saved offers for this barcode yet. Add one under DEALS.</div>':''))+'</div>';}
function priceCard(p){const e=effective(p),s=Number(p.shelf_price||0)*Math.max(1,Number(p.required_qty||1))-e,unit=e/Math.max(1,Number(p.package_qty||1));return '<div class="item"><h3>'+esc(p.item_name)+' — '+esc(p.store)+'</h3><span class="badge">'+esc(String(p.confidence||'medium').toUpperCase())+' CONFIDENCE</span><div class="price">'+money(e)+' effective</div><div class="small">Shelf '+money(p.shelf_price)+' · savings '+money(s)+' · '+money(unit)+'/'+esc(p.unit_label||'unit')+'</div><div class="small">Sale −'+money(p.sale_discount)+' · store coupon −'+money(p.store_coupon)+' · manufacturer −'+money(p.manufacturer_coupon)+' · rebate −'+money(p.rebate)+' · loyalty −'+money(p.loyalty_value)+'</div>'+(p.id?'<button class="remove" data-remove-price="'+esc(p.id)+'">Delete price</button>':'')+'</div>';}
function watchCard(w){const source=String(w.source||'all'),key=source==='amazon'?String(w.id):String(w.item_name||'').trim().toLowerCase(),hits=state.watchMatches[key]||[],observed=hits.map(x=>Date.parse(x.penny_sort_at||x.observed_at||'')).filter(Number.isFinite).sort((a,b)=>b-a)[0],savedChecked=Date.parse(w.last_checked_at||''),checkedAt=Number.isFinite(observed)?observed:(Number.isFinite(savedChecked)?savedChecked:null),checked=checkedAt?new Date(checkedAt).toLocaleString([], {month:'short',day:'numeric',hour:'numeric',minute:'2-digit'}):(state.watchCheckedAt?state.watchCheckedAt.toLocaleTimeString([], {hour:'numeric',minute:'2-digit'}):'not checked yet'),target=Number(w.target_effective_price);if(source==='amazon'){const mode=String(w.watch_mode||'specific'),prices=hits.map(x=>x.buy_price==null?NaN:Number(x.buy_price)).filter(Number.isFinite).sort((a,b)=>a-b),best=prices.length?prices[0]:null,hit=best!=null&&Number.isFinite(target)&&target>0&&best<=target,link=String(w.source_url||(mode==='discovery'?'https://www.amazon.com/gp/goldbox/':amazonUrl(w.source_ref,w.item_name)));let detail;if(hits.length){detail='<div class="watch-state '+(hit?'watch-hit':'')+'"><b>'+(mode==='discovery'?'AMAZON DEAL DISCOVERY':(hit?'AMAZON TARGET HIT':'AMAZON WEB RESULTS'))+'</b><div class="small">'+hits.length+' candidate'+(hits.length===1?'':'s')+(best!=null?' · lowest price clue '+money(best):'')+(Number.isFinite(target)&&target>0?' · target '+money(target):'')+' · observed '+esc(checked)+'</div>'+hits.slice(0,mode==='discovery'?6:4).map(matchCard).join('')+'</div>';}else{detail='<div class="watch-state watch-none"><b>'+(mode==='discovery'?'SCANNING AMAZON DEALS':'WATCHING AMAZON')+'</b><div class="small">No web candidates are cached yet. Last real search: '+esc(checked)+'. Search runs again on app open/refresh when the ~12-hour check is due, or immediately when you tap Check watches now.</div></div>';}return '<div class="item"><span class="badge">'+(mode==='discovery'?'AMAZON DEAL SCAN':'AMAZON WEB WATCH')+'</span><b>'+esc(mode==='discovery'?'Amazon bargains & coupons':w.item_name)+'</b><div class="small">'+(mode==='discovery'?'Broad web search for limited-time deals, coupons, Prime/special-event offers and deep discounts.':('Target '+(w.target_effective_price==null?'best current deal':money(w.target_effective_price))+(w.source_ref?' · '+esc(amazonAsin(w.source_ref)||w.source_ref):'')))+'</div>'+detail+(link.startsWith('https://')?'<a href="'+esc(link)+'" target="_blank" rel="noopener">Open Amazon</a>':'')+'<br><button class="remove" data-remove-watch="'+esc(w.id)+'">Remove watch</button></div>';}let detail;if(hits.length){detail='<div class="watch-state watch-hit"><b>MATCH FOUND — VERIFY LOCAL</b><div class="small">'+hits.length+' saved H38 match'+(hits.length===1?'':'es')+' · checked '+esc(checked)+'</div>'+hits.slice(0,3).map(matchCard).join('')+'</div>';}else{detail='<div class="watch-state watch-none"><b>WATCHING — NO SAVED MATCH YET</b><div class="small">Checked '+esc(checked)+'. H38 will check again when Couponing opens, after watch changes, and while this page stays open.</div></div>';}return '<div class="item"><b>'+esc(w.item_name)+'</b><div class="small">Target '+(w.target_effective_price==null?'any strong deal':money(w.target_effective_price))+'</div>'+detail+'<button class="remove" data-remove-watch="'+esc(w.id)+'">Remove watch</button></div>';}
function dealsView(){const cards=state.prices.slice(0,60).map(priceCard).join(''),watches=state.watch.map(watchCard).join('');return '<div class="section-title"><h2>Find savings</h2></div><div class="coverage-note">Public offer coverage varies by retailer. An empty result can mean H38 has no usable current public source for that retailer; it is not proof the store has no deal.</div><h3 style="margin:14px 0 6px">Add / verify a local deal</h3><p class="small">Use this for verified local prices/coupons. Saved H38 matches are suggestions only and are not auto-saved here.</p><div class="stack"><input id="dealItem" placeholder="Item"><input id="dealBarcode" placeholder="UPC / barcode"><div class="row"><input id="dealStore" placeholder="Store"><input id="dealShelf" type="number" step=".01" placeholder="Shelf price"></div><div class="row"><input id="dealSale" type="number" step=".01" placeholder="Sale discount"><input id="dealStoreCoupon" type="number" step=".01" placeholder="Store coupon"></div><div class="row"><input id="dealMfr" type="number" step=".01" placeholder="Manufacturer coupon"><input id="dealRebate" type="number" step=".01" placeholder="Cash back"></div><div class="row"><input id="dealLoyalty" type="number" step=".01" placeholder="Loyalty reward"><input id="dealDistance" type="number" step=".1" placeholder="Miles away"></div><div class="row"><input id="dealPackage" type="number" step=".01" value="1" placeholder="Package units"><select id="dealConfidence"><option value="high">High confidence</option><option value="medium" selected>Medium</option><option value="low">Low</option></select></div><input id="dealSource" placeholder="Source note / expiration"><button id="previewStack" class="secondary">Calculate stack</button><div id="stackPreview" class="small"></div><button id="saveDeal">Save price + stack</button></div><hr style="border:0;border-top:1px solid #e1ebe2;margin:16px 0"><div class="section-title"><h2>Deals for me / Price history</h2></div>'+(cards||'<div class="empty">Add local prices, sales and coupons. Your price history grows automatically.</div>')+'<div class="section-title" style="margin-top:14px"><h2>Watch list</h2><span class="small">'+state.watch.length+' active</span></div><p class="small">Watch a specific product, or use the Amazon bargain button for a broad public-web scan. Results are shared with both household members. Cached results may load instantly; a new web search runs on app open/refresh when the ~12-hour check is due.</p><div class="toolbar"><button id="scanAmazon" class="secondary">Scan Amazon bargains</button></div><div class="row"><input id="watchItem" placeholder="Item to watch" aria-label="Watch item"><input id="watchTarget" type="number" step=".01" placeholder="Target price (specific item)" aria-label="Watch target price"></div><div class="row" style="margin-top:8px"><select id="watchSource" aria-label="Watch source"><option value="all">Any retailer</option><option value="amazon">Amazon</option></select><input id="watchRef" placeholder="Amazon URL or ASIN (optional)" aria-label="Amazon URL or ASIN" disabled></div><div class="row" style="margin-top:8px"><button id="addWatch">Watch</button><button id="checkWatches" class="secondary">Check watches now</button></div>'+(watches||'<div class="empty">Add an item to watch. H38 will refresh deal candidates each time Couponing opens.</div>');}
function receiptsView(){const rows=state.receipts.map(r=>'<div class="item"><h3>'+esc(r.store||'Receipt')+'</h3><div class="price">'+money(r.total)+'</div><div class="small">'+new Date(r.purchased_at).toLocaleDateString()+'</div><button class="remove" data-remove-receipt="'+esc(r.id)+'">Delete receipt</button></div>').join('');return '<div class="section-title"><h2>Receipts</h2></div><div class="scanbox"><p>Take one picture. We read what we can, then you confirm the store and total.</p><button id="scanReceipt">Scan receipt</button></div><div class="row" style="margin-top:10px"><input id="receiptStore" placeholder="Store"><input id="receiptTotal" type="number" step=".01" placeholder="Total"><button id="saveReceipt">Save</button></div><div id="receiptOcr" class="small" style="white-space:pre-wrap;margin-top:8px">'+esc(state.receiptText)+'</div>'+rows;}
function render(){document.querySelectorAll('.nav button').forEach(b=>b.classList.toggle('active',b.dataset.view===state.view));$('view').innerHTML=state.view==='shop'?shopView():state.view==='save'?saveView():state.view==='scan'?scanView():state.view==='deals'?dealsView():receiptsView();wire();}
async function optimize(){const items=state.items.filter(x=>!x.checked);if(!items.length){status('Add shopping items first.');return;}if(!state.prices.length){status('No verified local prices are saved yet. Add a real store price/coupon under DEALS before optimizing.');state.view='deals';render();return;}const mode=$('mode')?$('mode').value:(state.list.optimize_mode||'practical'),max=$('maxStores')?Math.max(1,Math.min(4,Number($('maxStores').value||2))):Number(state.list.max_stores||2),budget=$('budget')&&$('budget').value?Number($('budget').value):null;state.list.optimize_mode=mode;state.list.max_stores=max;state.list.budget=budget;const saved=await sb.from('coupon_shopping_lists').update({optimize_mode:mode,max_stores:max,budget,updated_at:new Date().toISOString()}).eq('id',state.list.id);if(saved.error)throw saved.error;const common={items,prices:state.prices,travel_cost_per_mile:Number(state.list.travel_cost_per_mile||0.25)},one=await sb.functions.invoke('h38-coupon-api',{body:Object.assign({action:'optimize',max_stores:1,mode:'single_store'},common)}),best=await sb.functions.invoke('h38-coupon-api',{body:Object.assign({action:'optimize',max_stores:max,mode},common)});if(one.error||best.error)throw one.error||best.error;state.plan={one:one.data&&one.data.best,best:best.data&&best.data.best};render();status(state.plan.best?'Optimization complete. Review the trip plan below.':'Optimize complete. No complete verified-price plan yet — add verified prices for the remaining unchecked items.');}
function dealRow(){return {item_name:$('dealItem').value.trim(),barcode:$('dealBarcode').value.trim(),store:$('dealStore').value.trim(),shelf_price:Number($('dealShelf').value||0),sale_discount:Number($('dealSale').value||0),store_coupon:Number($('dealStoreCoupon').value||0),manufacturer_coupon:Number($('dealMfr').value||0),rebate:Number($('dealRebate').value||0),loyalty_value:Number($('dealLoyalty').value||0),package_qty:Number($('dealPackage').value||1),distance_miles:Number($('dealDistance').value||0),confidence:$('dealConfidence').value,source_note:$('dealSource').value.trim()};}
async function previewStack(){const row=dealRow(),q=await sb.functions.invoke('h38-coupon-api',{body:{action:'stack',price:row}});if(q.error)throw q.error;$('stackPreview').textContent='Effective '+money(q.data.stack.effective)+' · savings '+money(q.data.stack.savings);}
async function saveDeal(){const row=dealRow();if(!row.item_name||!row.store||!row.shelf_price){status('Item, store and shelf price are required.');return;}const q=await sb.from('coupon_price_observations').insert(row).select().single();if(q.error)throw q.error;state.prices.unshift(q.data);render();status('Verified price/stack saved.');}
async function addWatch(){const rawItem=$('watchItem').value.trim(),source=$('watchSource').value,ref=$('watchRef').value.trim(),mode=source==='amazon'&&!rawItem?'discovery':'specific',item=mode==='discovery'?'Amazon Deal Scan':rawItem;if(!item||(!rawItem&&source!=='amazon')){status('Enter an item, or choose Amazon and leave the item blank for a broad deal scan.');return;}if(state.watch.some(w=>String(w.source||'all')===source&&(mode==='discovery'?String(w.watch_mode||'specific')==='discovery':String(w.item_name||'').trim().toLowerCase()===item.toLowerCase()))){status(mode==='discovery'?'Amazon deal discovery is already active.':'"'+item+'" is already being watched for '+(source==='amazon'?'Amazon':'retailers')+'.');return;}const q=await sb.from('coupon_watch_rules').insert({item_name:item,target_effective_price:mode==='specific'&&$('watchTarget').value?Number($('watchTarget').value):null,target_discount_percent:mode==='discovery'?40:null,source,watch_mode:mode,source_ref:source==='amazon'&&mode==='specific'?(amazonAsin(ref)||ref.slice(0,500)):'',source_url:source==='amazon'?(mode==='discovery'?'https://www.amazon.com/gp/goldbox/':amazonUrl(ref,item)):''}).select().single();if(q.error)throw q.error;state.watch.unshift(q.data);status(mode==='discovery'?'Amazon bargain scan added. Running the first public-web search now…':'Watching "'+item+'"'+(source==='amazon'?' on Amazon/web search':'')+'. Refreshing current results now…');try{await refreshWatchMatches(true,source==='amazon');}catch(e){status('Watch saved, but search failed: '+e.message);}render();}
async function saveReceipt(){const store=$('receiptStore').value.trim(),total=Number($('receiptTotal').value||0);if(!store||!Number.isFinite(total)||total<=0){status('Confirm the store and a total greater than $0.');return;}const q=await sb.from('coupon_receipts').insert({store,total,notes:String(state.receiptText||'').slice(0,4000)}).select().single();if(q.error)throw q.error;state.receipts.unshift(q.data);state.receiptText='';render();status('Receipt saved.');}
async function deleteRecord(table,id,label){const q=await sb.from(table).delete().eq('id',id);if(q.error)throw q.error;status(label+' deleted.');await load();}
function fillReceipt(text){state.receiptText=text;const lines=text.split(String.fromCharCode(10)).map(x=>x.trim()).filter(Boolean);if($('receiptStore'))$('receiptStore').value=(lines[0]||'').slice(0,60);const nums=[];String(text).replace(/(?:total|amount)\s*[:$]?\s*\$?([0-9]+\.[0-9]{2})/ig,function(_,n){nums.push(Number(n));return _;});if(nums.length&&$('receiptTotal'))$('receiptTotal').value=Math.max.apply(null,nums).toFixed(2);}
function startVoice(){if(window.AndroidH38Deals&&AndroidH38Deals.speakList){AndroidH38Deals.speakList();return;}const R=window.SpeechRecognition||window.webkitSpeechRecognition;if(!R){status('Voice list is not supported on this device.');return;}const r=new R();r.lang='en-US';r.interimResults=false;r.onresult=e=>addMany(e.results[0][0].transcript).catch(x=>status(x.message));r.onerror=e=>status('Voice error: '+e.error);r.start();}
async function ensureOcr(){if(window.Tesseract)return;await new Promise(function(resolve,reject){const s=document.createElement('script');s.src='https://cdn.jsdelivr.net/npm/tesseract.js@5/dist/tesseract.min.js';s.onload=resolve;s.onerror=function(){s.onerror=reject;s.src='https://unpkg.com/tesseract.js@5/dist/tesseract.min.js';};document.head.appendChild(s);});}
async function ocrFile(file,target){if(!file)return '';target.textContent='Reading image…';await ensureOcr();const out=await Tesseract.recognize(file,'eng',{logger:m=>{if(m.status==='recognizing text')target.textContent='Reading '+Math.round((m.progress||0)*100)+'%…';}});target.textContent=out.data.text;return out.data.text;}
function wire(){if(state.view==='shop'){$('addItem').onclick=async function(){const b=$('addItem'),name=$('newItem').value;b.disabled=true;try{await addItem(name);}catch(e){status('Save failed: '+e.message);}finally{if($('addItem'))$('addItem').disabled=false;}};$('newItem').onkeydown=e=>{if(e.key==='Enter')$('addItem').click();};$('findMatches').onclick=async function(){const b=$('findMatches');b.disabled=true;status('Refreshing deals and prices…');try{await refreshMatches(true);render();}catch(e){status('Deal refresh failed: '+e.message);}finally{if($('findMatches'))$('findMatches').disabled=false;}};document.querySelectorAll('[data-remove]').forEach(b=>b.onclick=()=>{if(confirm('Remove this shopping item?'))removeItem(b.dataset.remove).catch(e=>status(e.message));});document.querySelectorAll('[data-check]').forEach(b=>b.onchange=()=>toggleItem(b.dataset.check,b.checked).catch(e=>status(e.message)));$('recipe').onclick=()=>$('recipeBox').classList.toggle('hidden');$('recipeAdd').onclick=async function(){const b=$('recipeAdd'),text=$('recipeText').value;b.disabled=true;try{await addMany(text);}catch(e){status('List save failed: '+e.message);}finally{if($('recipeAdd'))$('recipeAdd').disabled=false;}};$('voice').onclick=startVoice;$('handwrite').onclick=()=>{if(window.AndroidH38Deals&&AndroidH38Deals.takePhoto)AndroidH38Deals.takePhoto('shopping-list');else $('handwriteFile').click();};}
if(state.view==='save'){$('runOptimize').onclick=async function(){const b=$('runOptimize');b.disabled=true;try{await optimize();}catch(e){status('Optimize failed: '+e.message);}finally{if($('runOptimize'))$('runOptimize').disabled=false;}};$('mode').onchange=async function(){const mode=$('mode').value,max=mode==='single_store'?1:mode==='two_stores'?2:mode==='maximum_savings'?4:Number($('maxStores').value||2),q=await sb.from('coupon_shopping_lists').update({optimize_mode:mode,max_stores:max,budget:$('budget').value?Number($('budget').value):null,updated_at:new Date().toISOString()}).eq('id',state.list.id);if(q.error){status(q.error.message);return;}state.list.optimize_mode=mode;state.list.max_stores=max;};$('applyAssistant').onclick=async function(){const b=$('applyAssistant');b.disabled=true;try{const q=await sb.functions.invoke('h38-coupon-api',{body:{action:'assistant',text:$('assistant').value}});if(q.error)throw q.error;const x=q.data.intent;state.list.max_stores=x.max_stores;state.list.optimize_mode=x.mode;if(x.budget!=null)state.list.budget=x.budget;const u=await sb.from('coupon_shopping_lists').update({max_stores:x.max_stores,optimize_mode:x.mode,budget:x.budget}).eq('id',state.list.id);if(u.error)throw u.error;render();}catch(e){status('Request apply failed: '+e.message);}finally{if($('applyAssistant'))$('applyAssistant').disabled=false;}};if($('storeMode'))$('storeMode').onclick=()=>{state.view='shop';status('Store Mode: check items off as you shop.');render();};}
if(state.view==='scan'){$('scanBarcode').onclick=()=>{if(window.AndroidH38Deals&&AndroidH38Deals.scanBarcode)AndroidH38Deals.scanBarcode();else $('manualBarcode').focus();};$('manualBarcode').onchange=()=>{state.barcode=$('manualBarcode').value;render();};}
if(state.view==='deals'){const syncWatchSource=()=>{const amazon=$('watchSource').value==='amazon';$('watchRef').disabled=!amazon;$('watchRef').placeholder=amazon?'Amazon URL or ASIN (optional)':'Amazon URL or ASIN (select Amazon)';};$('watchSource').onchange=syncWatchSource;syncWatchSource();$('scanAmazon').onclick=async function(){const existing=state.watch.find(w=>String(w.source||'')==='amazon'&&String(w.watch_mode||'specific')==='discovery');if(existing){status('Searching Amazon bargains now…');try{await refreshWatchMatches(true,true);render();}catch(e){status('Amazon bargain search failed: '+e.message);}return;}$('watchSource').value='amazon';$('watchItem').value='';$('watchTarget').value='';$('watchRef').value='';syncWatchSource();try{await addWatch();}catch(e){status('Amazon bargain scan failed: '+e.message);}};$('previewStack').onclick=()=>previewStack().catch(e=>status(e.message));$('saveDeal').onclick=async function(){const b=$('saveDeal');b.disabled=true;try{await saveDeal();}catch(e){status('Price save failed: '+e.message);}finally{if($('saveDeal'))$('saveDeal').disabled=false;}};$('addWatch').onclick=async function(){const b=$('addWatch');b.disabled=true;try{await addWatch();}catch(e){status('Watch save failed: '+e.message);}finally{if($('addWatch'))$('addWatch').disabled=false;}};$('checkWatches').onclick=async function(){const b=$('checkWatches');b.disabled=true;status('Refreshing watch deals…');try{await refreshWatchMatches(true,true);render();}catch(e){status('Watch check failed: '+e.message);}finally{if($('checkWatches'))$('checkWatches').disabled=false;}};document.querySelectorAll('[data-remove-price]').forEach(b=>b.onclick=()=>{if(confirm('Delete this saved price?'))deleteRecord('coupon_price_observations',b.dataset.removePrice,'Price').catch(e=>status(e.message));});document.querySelectorAll('[data-remove-watch]').forEach(b=>b.onclick=()=>{if(confirm('Remove this watch?'))deleteRecord('coupon_watch_rules',b.dataset.removeWatch,'Watch').catch(e=>status(e.message));});}
if(state.view==='receipts'){$('scanReceipt').onclick=()=>{if(window.AndroidH38Deals&&AndroidH38Deals.takePhoto)AndroidH38Deals.takePhoto('receipt');else $('receiptFile').click();};$('saveReceipt').onclick=async function(){const b=$('saveReceipt');b.disabled=true;try{await saveReceipt();}catch(e){status('Receipt save failed: '+e.message);}finally{if($('saveReceipt'))$('saveReceipt').disabled=false;}};document.querySelectorAll('[data-remove-receipt]').forEach(b=>b.onclick=()=>{if(confirm('Delete this receipt record?'))deleteRecord('coupon_receipts',b.dataset.removeReceipt,'Receipt').catch(e=>status(e.message));});}}
$('handwriteFile').onchange=async e=>{try{const text=await ocrFile(e.target.files[0],$('status'));await addMany(text);status('Scanned list added and saved. Review item names.');}catch(err){status('List scan failed: '+err.message);}};$('receiptFile').onchange=async e=>{try{state.view='receipts';render();const text=await ocrFile(e.target.files[0],$('receiptOcr'));fillReceipt(text);status('Receipt read. Confirm store and total, then Save.');}catch(err){status('Receipt scan failed: '+err.message);}};window.H38NativeBarcodeResult=v=>{state.barcode=String(v||'');state.view='scan';render();};window.H38NativeSpeechResult=v=>addMany(String(v||'')).catch(e=>status(e.message));window.H38NativePhotoResult=async function(role,dataUrl){try{const response=await fetch(dataUrl),blob=await response.blob();if(role==='receipt'){state.view='receipts';render();const text=await ocrFile(blob,$('receiptOcr'));fillReceipt(text);status('Receipt read. Confirm store and total.');}else{const text=await ocrFile(blob,$('status'));await addMany(text);status('Scanned list added and saved.');}}catch(e){status('Photo read failed: '+e.message);}};window.H38NativePhotoError=v=>status(String(v||'Photo canceled'));document.querySelectorAll('.nav button').forEach(b=>b.onclick=()=>{state.view=b.dataset.view;render();});$('zip').onchange=saveSharedLocation;$('radius').onchange=saveSharedLocation;$('useLocation').onclick=useSharedLocation;window.H38NativeLocationResult=acceptSharedLocation;window.H38NativeLocationError=v=>status(String(v||'Location permission was unavailable. Enter a ZIP instead.'));restoreSharedLocation();(async function(){if(await gate()){await load();showHandoff();setInterval(async()=>{if(document.visibilityState==='visible'&&state.watch.length){try{await refreshWatchMatches(false);$('sync').textContent='Shared';}catch(e){}}},1800000);}})();
</script></body></html>`;

Deno.serve(() => new Response(html, {headers:{"content-type":"text/html; charset=utf-8","cache-control":"no-store","content-security-policy":"default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://unpkg.com; connect-src 'self' https://jqukmwtsgcsaruucnqja.supabase.co wss://jqukmwtsgcsaruucnqja.supabase.co https://cdn.jsdelivr.net https://unpkg.com; worker-src 'self' blob: https://cdn.jsdelivr.net https://unpkg.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: blob: https:; frame-ancestors 'self'"}}));
