import "jsr:@supabase/functions-js/edge-runtime.d.ts";

const html = String.raw`<!doctype html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#163650"><title>H38 Resale</title>
<style>
*{box-sizing:border-box}body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;background:#edf3f6;color:#142735}.wrap{max-width:760px;margin:auto;padding:calc(14px + env(safe-area-inset-top)) 14px calc(30px + env(safe-area-inset-bottom))}.top{display:flex;align-items:center;gap:10px;margin:2px 0 10px}.top a{text-decoration:none;color:#31526a;font-weight:800}.top h1{font-size:26px;margin:0}.card{background:#fff;border:1px solid #d5e0e6;border-radius:18px;padding:14px;margin:11px 0;box-shadow:0 2px 8px rgba(20,39,53,.04)}.grid{display:grid;grid-template-columns:1fr 92px;gap:8px}input,select,button{font:inherit}input,select{width:100%;padding:12px;border:1px solid #c3d1d9;border-radius:12px;background:#fff}button{border:0;border-radius:12px;padding:12px 14px;background:#163650;color:#fff;font-weight:800;min-height:44px}.secondary{background:#dce8ee;color:#173449}.tabs,.lanes,.source-strip{display:flex;gap:7px;overflow:auto;scrollbar-width:none}.tabs::-webkit-scrollbar,.lanes::-webkit-scrollbar,.source-strip::-webkit-scrollbar{display:none}.tab,.lane{white-space:nowrap;background:#dce7ed;color:#173449}.tab.active,.lane.active{background:#163650;color:#fff}.lanes{margin:0 0 10px}.lane{font-size:12px;padding:9px 10px;min-height:38px}.status{font-size:13px;color:#526b7a;margin:9px 0 0;line-height:1.35}.controls{display:grid;grid-template-columns:1fr auto;gap:8px;margin-bottom:4px}.source-strip{margin:10px 0 2px}.source-chip{flex:0 0 auto;border:1px solid #d6e0e5;border-radius:12px;padding:8px 10px;background:#f7fafb;min-width:112px}.source-chip strong{display:block;font-size:12px}.source-chip span{display:block;font-size:11px;color:#687b86;margin-top:2px}.source-chip.live{border-color:#b9dec4;background:#eef9f1}.source-chip.partial{border-color:#ead49d;background:#fff9e9}.source-chip.off{border-color:#e4c2c2;background:#fff3f3}.source-chip.route{background:#eef3f7}.scan-note{margin:10px 0 2px;padding:10px 11px;border-radius:12px;background:#f3f7f9;color:#425d6d;font-size:12px;line-height:1.4}.result-count{display:flex;justify-content:space-between;gap:8px;align-items:center;margin:12px 0 5px}.result-count strong{font-size:14px}.result-count span{font-size:12px;color:#6c7e88}.item{display:grid;grid-template-columns:92px 1fr;gap:11px;padding:13px 0;border-top:1px solid #e2e9ed}.item.noimg{grid-template-columns:1fr}.item:first-child{border-top:0}.thumb{width:92px;height:92px;border-radius:12px;object-fit:cover;background:#edf1f3;border:1px solid #e0e7eb}.item h3{font-size:16px;line-height:1.25;margin:0 0 5px}.meta{font-size:12px;color:#657987;line-height:1.35}.money{font-weight:850;font-size:17px;margin:3px 0}.profit{color:#14642d}.loss{color:#8c2d2d}.screen{color:#6b4d00}.badge{display:inline-block;border-radius:999px;background:#e6edf1;padding:4px 7px;font-size:10px;font-weight:800;margin:0 4px 4px 0}.good{background:#dff3e5;color:#12572a}.warn{background:#fff1d7;color:#705000}.risk{background:#fde7e7;color:#792828}.hunter{background:#e3eef8;color:#173d5d}.facebook{background:#e7efff;color:#1f4b93}.auction{background:#eee8fb;color:#51357e}.flag{margin-top:7px;padding:8px 9px;background:#f6f9fa;border-radius:10px;font-size:12px;color:#405865}.hidden{display:none!important}.empty{text-align:center;color:#667b87;padding:26px 10px}.empty strong{display:block;color:#2c4656;margin-bottom:6px}.more{width:100%;background:#dce7ed;color:#173449;margin-top:7px}.handoff{border-left:4px solid #163650}.actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}.action{display:inline-block;border:1px solid #bdccd5;border-radius:9px;padding:8px 10px;background:#f6f9fa;color:#173449;font-size:12px;font-weight:800;text-decoration:none}.action.primary{background:#163650;color:#fff;border-color:#163650}.subtle{font-size:11px;color:#7c8d96;margin-top:4px}@media(max-width:520px){.grid{grid-template-columns:1fr 82px}.controls{grid-template-columns:1fr}.top h1{font-size:22px}.item{grid-template-columns:76px 1fr}.thumb{width:76px;height:76px}}
</style></head>
<body data-h38-resale-web="results-polish-v19"><main class="wrap"><div class="top"><a href="/functions/v1/h38-deals-shell">‹ H38 Deals</a><h1>Resale</h1></div><section id="locked" class="card hidden"><h2>Not enabled</h2><p>This account does not include Resale.</p></section><div id="app" class="hidden"><section class="card"><div class="grid"><input id="zip" inputmode="numeric" maxlength="5" placeholder="ZIP code"><select id="radius"><option>25</option><option selected>50</option><option>75</option><option>100</option></select><button id="loc" class="secondary" style="grid-column:1/-1">Use phone location</button></div><div id="status" class="status">Loading saved H38 buy leads…</div></section><div class="tabs"><button class="tab active" data-tab="deals">Opportunity Hunter</button><button class="tab" data-tab="facebook">Facebook</button><button class="tab" data-tab="auctions">Auctions</button><button class="tab" data-tab="stores">Nearby Stores</button><button class="tab" data-tab="garage">Garage/Estate</button></div><section class="card"><div id="hunter-lanes" class="lanes"><button class="lane active" data-lane="all">All Opportunities</button><button class="lane" data-lane="repair">Repairable</button><button class="lane" data-lane="motivated">Motivated Seller</button><button class="lane" data-lane="straight">Underpriced</button></div><div class="controls"><input id="search" type="search" placeholder="Search item/model or leave blank for hunter scan…"><button id="scan">Scan Opportunity Hunter</button></div><div id="help" class="meta" style="margin-bottom:8px">Hunter scans current/public sources for repairable equipment, motivated sellers and ordinary underpriced items. Screening spread is NOT verified profit.</div><div id="source-strip" class="source-strip hidden"></div><div id="scan-note" class="scan-note hidden"></div><div id="result-count" class="result-count hidden"></div><section id="content"><div class="empty">Loading saved H38 buy leads…</div></section></section></div></main>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.95.0/dist/umd/supabase.min.js" onerror="this.onerror=null;this.src='https://unpkg.com/@supabase/supabase-js@2.95.0/dist/umd/supabase.min.js'"></script>
<script>
const SUPABASE_URL='https://jqukmwtsgcsaruucnqja.supabase.co';
const KEY='sb_publishable_XrF41kGmTC2SmSTgPvo5OQ_vqcBd0N1';
const sb=supabase.createClient(SUPABASE_URL,KEY),$=id=>document.getElementById(id);
let tab='deals',lane='all',cache={},rawCache={},lat=null,lon=null,visible=20,requestSeq=0;
const LOCATION_KEY='h38-resale-location-v1',H38_SHARED_LOCATION='h38-shopping-location-v1';
const txt=v=>String(v==null?'':v).trim(),num=v=>Number(v);
const HUNTER_TERMS={all:['ATV needs work','UTV project','snowmobile needs work','lawn tractor project','zero turn needs work',"generator won't start",'pressure washer needs work','welder project','air compressor as is','trailer needs work','mechanic special','make offer OBO'],repair:['ATV needs work','UTV project','snowmobile needs work','lawn tractor project','zero turn needs work',"generator won't start",'pressure washer needs work','welder project','air compressor as is','trailer needs work','mechanic special','ran when parked'],motivated:['must sell','need gone','make offer','OBO','moving sale','price reduced','cash today','as-is','project OBO','mechanic special OBO','ATV OBO','generator OBO'],straight:['generator','snowblower','lawn mower','zero turn','ATV','UTV','DeWalt','Milwaukee','welder','air compressor','trailer','toolbox']};
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));}
function saveLocation(){const x={zip:$('zip').value.trim(),radius:$('radius').value,radiusMiles:num($('radius').value),lat:lat,lon:lon};localStorage.setItem(H38_SHARED_LOCATION,JSON.stringify(x));localStorage.setItem(LOCATION_KEY,JSON.stringify(x));}
function restoreLocation(){try{const x=JSON.parse(localStorage.getItem(H38_SHARED_LOCATION)||localStorage.getItem(LOCATION_KEY)||'{}');$('zip').value=x.zip||'';$('radius').value=String(x.radius||x.radiusMiles||'50');lat=Number.isFinite(x.lat)?x.lat:null;lon=Number.isFinite(x.lon)?x.lon:null;if(Object.keys(x).length)localStorage.setItem(H38_SHARED_LOCATION,JSON.stringify(x));}catch(e){}}
function liveTerms(){const q=$('search').value.trim();if(tab==='deals'){if(q)return[q,q+' needs work',q+' project',q+' OBO',q+' as is',q+" won't start",q+' mechanic special'].slice(0,12);return(HUNTER_TERMS[lane]||HUNTER_TERMS.all).slice(0,12);}return q?[q]:['tools','electronics','appliances','lawn mower','generator','toolbox'];}
function payload(){saveLocation();const radius=num($('radius').value),q=$('search').value.trim();return{postal:$('zip').value.trim(),zip:$('zip').value.trim(),radius_miles:radius,radiusMiles:radius,radius:radius,lat:lat,lon:lon,deep_verify:true,terms:liveTerms(),query:q,search_lane:'mixed'};}
function rows(d,source){if(Array.isArray(d))return d;if(!d)return[];if(source==='deals')return[].concat(Array.isArray(d.opportunities)?d.opportunities:[],Array.isArray(d.candidates)?d.candidates.map(x=>Object.assign({verification_status:'CANDIDATE · NEEDS SOLD COMPS'},x)):[],Array.isArray(d.auction_candidates)?d.auction_candidates:[]);if(source==='facebook')return(Array.isArray(d.results)?d.results:[]).concat(Array.isArray(d.candidates)?d.candidates.map(x=>Object.assign({verification_status:'LOCATION NEEDS PROOF'},x)):[]);if(source==='stores')return Array.isArray(d.stores)?d.stores:(Array.isArray(d.readings)?d.readings:[]);if(Array.isArray(d.results))return d.results;if(Array.isArray(d.deals))return d.deals;if(Array.isArray(d.items))return d.items;if(Array.isArray(d.rows))return d.rows;return[];}
async function api(action){const q=await sb.functions.invoke('h38-resale-api',{body:{action:action,payload:payload()}});if(q.error)throw q.error;if(q.data&&q.data.error)throw new Error(q.data.error);return q.data?q.data.data:null;}
function first(r,keys,min){for(const k of keys){const raw=r[k];if(raw==null||txt(raw)==='')continue;const x=num(raw);if(Number.isFinite(x)&&x>=min)return x;}return NaN;}
function price(r){return first(r,['buy_price','price','current_price','sale_price','asking_price','current_bid','bid'],0);}
function resale(r){return first(r,['expected_resale','resale_price','market_value','estimated_value','comparable_price','resale_estimate'],.000001);}
function distance(r){const x=num(r.distance_miles);return r.distance_miles==null||txt(r.distance_miles)===''||!Number.isFinite(x)?NaN:x;}
function title(r){return txt(r.title||r.name||r.store_name||r.item_name||r.event_name||'Opportunity');}
function screeningSpread(r){const direct=num(r.screening_spread),b=price(r),s=resale(r),fees=num(r.estimated_fees||r.fees||0),ship=num(r.estimated_shipping||r.shipping||0),other=num(r.other_costs||0);if(r.screening_spread!=null&&txt(r.screening_spread)!==''&&Number.isFinite(direct))return direct;return Number.isFinite(b)&&Number.isFinite(s)?s-b-fees-ship-other:NaN;}
function verifiedProfit(r){const verified=r.profit_verified===true||r.verified_profit===true||txt(r.profit_status).toLowerCase()==='verified';if(!verified)return{verified:false,value:NaN};const direct=num(r.net_profit!=null?r.net_profit:r.verified_profit_amount);return{verified:true,value:Number.isFinite(direct)?direct:screeningSpread(r)};}
function candidateText(r){return [title(r),r.description,r.reason,r.notes,r.note,r.condition,r.status,r.verification_status,r.source,r.marketplace].map(txt).join(' ').toLowerCase();}
function analyzeOpportunity(r){const text=candidateText(r),repair=/needs work|project|mechanic special|won'?t move|slips?|clutch|belt|no reverse|won'?t start|needs carb|ran when parked|needs battery|as[- ]is|not running|for parts/.test(text),motivated=/\bobo\b|make offer|must sell|needs? gone|cash today|moving|price reduced|priced to sell|best offer|today only/.test(text),spread=screeningSpread(r),hasSource=!!txt(r.url||r.source_url||r.listing_url),local=!!(r.local_store_verified||r.location_verified||Number.isFinite(distance(r))),p=price(r);let score=0,tags=[],reasons=[];if(repair){score+=35;tags.push('REPAIRABLE');reasons.push('repair/distress language');}if(motivated){score+=25;tags.push('MOTIVATED SELLER');reasons.push('seller flexibility or urgency');}if(Number.isFinite(spread)){const ratio=Number.isFinite(p)&&p>0?spread/p:0;score+=Math.max(0,Math.min(30,Math.round(ratio*30)));if(spread>0){tags.push('PRICE-SPREAD LEAD');reasons.push('asking price leaves screening spread');}}if(local){score+=10;tags.push('LOCATION EVIDENCE');}if(hasSource)score+=5;if(!repair&&!motivated){tags.push('UNDERPRICE CHECK');reasons.push('candidate worth comping');score+=10;}return{repair,motivated,spread,score,tags:tags.slice(0,3),reason:reasons.join(' + ')};}
function laneMatch(r){if(tab!=='deals'||lane==='all')return true;const a=analyzeOpportunity(r);return lane==='repair'?a.repair:lane==='motivated'?a.motivated:lane==='straight'?!a.repair&&!a.motivated:true;}
function sourceUrl(r){const u=txt(r.url||r.source_url||r.listing_url);return u.startsWith('https://')?u:'';}
function sourceLink(r,label){const u=sourceUrl(r);return u?'<a class="action primary" href="'+esc(u)+'" target="_blank" rel="noopener">'+esc(label||'Open listing')+'</a>':'';}
function imageUrl(r){const u=txt(r.image_url||r.image||r.thumbnail||r.photo_url);return /^https:\/\//i.test(u)?u:'';}
function badges(r){const out=[],proof=txt(r.verification_status||r.location_status||r.price_scope||r.provider_status);if(tab==='facebook'){out.push('<span class="badge facebook">FACEBOOK</span>');out.push(r.location_verified===true?'<span class="badge good">LOCATION VERIFIED</span>':'<span class="badge warn">LOCATION NEEDS PROOF</span>');}else if(tab==='auctions')out.push('<span class="badge auction">'+esc(txt(r.source||'AUCTION'))+'</span>');if(proof&&!/location needs proof/i.test(proof))out.push('<span class="badge warn">'+esc(proof)+'</span>');return out.join('');}
function card(r){if(tab==='stores'){return'<div class="item noimg"><div><h3>'+esc(title(r))+'</h3><span class="badge good">NEARBY STORE</span><div class="meta">'+esc(txt(r.store_address||r.address||'Address not listed'))+(Number.isFinite(distance(r))?' · '+distance(r).toFixed(1)+' mi':'')+'</div><div class="meta">'+esc(txt(r.retailer||r.source||''))+'</div></div></div>';}
 const img=imageUrl(r),cls='item'+(img?'':' noimg'),media=img?'<img class="thumb" src="'+esc(img)+'" alt="" loading="lazy" referrerpolicy="no-referrer">':'',proof=badges(r),where=txt(r.location_label||r.location||r.city||''),dist=Number.isFinite(distance(r))?distance(r).toFixed(1)+' mi':'';
 if(tab==='garage'){const when=txt(r.date_label||r.event_time||r.event_date||'Date needs proof'),src=txt(r.source||r.marketplace||'Source not identified');return'<div class="'+cls+'">'+media+'<div><h3>'+esc(title(r))+'</h3><div><span class="badge">'+esc(txt(r.event_type||'SALE'))+'</span>'+proof+'</div><div class="meta"><strong>When:</strong> '+esc(when)+'</div><div class="meta"><strong>Where:</strong> '+esc(where||'Location needs proof')+(dist?' · '+dist:'')+'</div><div class="meta"><strong>Source:</strong> '+esc(src)+'</div><div class="actions">'+sourceLink(r,'Open sale')+'</div></div></div>';}
 if(tab==='auctions'){const bid=price(r),premium=r.buyer_premium_pct!=null?txt(r.buyer_premium_pct)+'%':'not established',pickup=txt(r.pickup_mode||'Pickup/shipping not established'),date=txt(r.date_label||r.closing_label||'');return'<div class="'+cls+'">'+media+'<div><h3>'+esc(title(r))+'</h3><div>'+proof+'</div><div class="money">'+(Number.isFinite(bid)?'Current bid $'+bid.toFixed(2):'Bid not listed')+'</div><div class="meta">'+esc([where,dist,date].filter(Boolean).join(' · '))+'</div><div class="meta">Buyer premium: '+esc(premium)+' · '+esc(pickup)+'</div><div class="actions">'+sourceLink(r,'Open auction')+'</div></div></div>';}
 const p=price(r),s=resale(r),vp=verifiedProfit(r),a=analyzeOpportunity(r),roi=Number.isFinite(a.spread)&&Number.isFinite(p)&&p>0?a.spread/p*100:NaN,status=vp.verified?'<span class="badge good">PROFIT VERIFIED</span>':'<span class="badge risk">PROFIT NOT VERIFIED</span>',tags=tab==='deals'?a.tags.map(x=>'<span class="badge hunter">'+esc(x)+'</span>').join(''):'';let math='';if(vp.verified&&Number.isFinite(vp.value))math='<span class="'+(vp.value>=0?'profit':'loss')+'">Verified profit $'+vp.value.toFixed(2)+'</span>';else if(Number.isFinite(a.spread))math='<span class="screen">Screening spread $'+a.spread.toFixed(2)+(Number.isFinite(roi)?' · '+Math.round(roi)+'%':'')+' · NOT PROFIT</span>';const source=txt(r.retailer||r.source||r.marketplace||''),location=[where,dist].filter(Boolean).join(' · ');return'<div class="'+cls+'">'+media+'<div><h3>'+esc(title(r))+'</h3><div>'+status+tags+proof+'</div><div class="money">'+(Number.isFinite(p)?'Buy $'+p.toFixed(2):'Buy price unknown')+(Number.isFinite(s)?' → est. resale $'+s.toFixed(2):'')+'</div><div class="meta">'+math+(math&&source?' · ':'')+esc(source)+'</div>'+(location?'<div class="meta">'+esc(location)+'</div>':'')+(tab==='deals'?'<div class="flag"><strong>Hunter '+a.score+':</strong> '+esc(a.reason||'candidate needs market verification')+'</div>':'')+'<div class="actions">'+sourceLink(r,'Open listing')+(tab==='deals'?'<a class="action" href="'+esc(couponLink(r))+'">Build coupon stack</a>':'')+'</div></div></div>';}
function dedupe(list){const seen=new Set();return list.filter(r=>{const k=[sourceUrl(r),title(r).toLowerCase(),Number.isFinite(price(r))?price(r):''].join('|');if(seen.has(k))return false;seen.add(k);return true;});}
function handoffQuery(){const p=new URLSearchParams(location.search);return{item:txt(p.get('item')),store:txt(p.get('store')),upc:txt(p.get('upc')),sku:txt(p.get('sku')),buy:txt(p.get('buy'))};}
function couponLink(r){const q=new URLSearchParams(),name=title(r),p=price(r);if(name)q.set('item',name);if(r.retailer)q.set('store',r.retailer);if(r.upc)q.set('upc',r.upc);if(r.sku)q.set('sku',r.sku);if(Number.isFinite(p))q.set('buy',String(p));return'/functions/v1/h38-coupon-web?'+q.toString();}
function healthEntries(d){const out=[];const h=d&&d.source_health;if(h&&typeof h==='object')Object.keys(h).forEach(name=>{const x=h[name]||{};out.push({name,state:txt(x.status||x.provider_status||'unknown'),count:Number(x.count??x.nearby??x.lots??x.verified_count??0),detail:txt(x.detail||x.warning||'')});});if(tab==='facebook'&&d){const ph=d.provider_health||d.providers||d.source_health;if(!out.length&&ph&&typeof ph==='object')Object.keys(ph).forEach(name=>{const x=ph[name]||{};out.push({name,state:txt(x.status||x.state||'unknown'),count:Number(x.count||x.results||0),detail:txt(x.detail||x.warning||'')});});if(!out.length&&d.provider_status)out.push({name:'Facebook public discovery',state:txt(d.provider_status),count:Number(d.results_count||d.result_count||0),detail:''});}if(tab==='deals'&&d&&d.facebook_source)out.push({name:'Facebook Marketplace',state:txt(d.facebook_source.provider_status||'unknown'),count:Number(d.facebook_source.verified_count||0)+Number(d.facebook_source.candidate_count||0),detail:''});return out;}
function renderHealth(d){const box=$('source-strip'),note=$('scan-note'),entries=healthEntries(d);if(entries.length){box.innerHTML=entries.map(x=>{const s=x.state.toLowerCase(),cls=/live|pass|verified/.test(s)?'live':/unavailable|error|failed/.test(s)?'off':/route/.test(s)?'route':'partial',label=x.count?x.count+' found':(x.state||'checked').replace(/_/g,' ');return'<div class="source-chip '+cls+'"><strong>'+esc(x.name)+'</strong><span>'+esc(label)+'</span></div>';}).join('');box.classList.remove('hidden');}else{box.innerHTML='';box.classList.add('hidden');}
 let message='';if(d&&d.warning)message=txt(d.warning);else if(tab==='facebook')message='Facebook is public-source discovery only. Verified local listings are shown first; candidates stay visible with LOCATION NEEDS PROOF instead of being discarded.';else if(tab==='auctions')message='Live auction catalogs and route-only directories are labeled separately. Unknown pickup distance, premium or shipping stays unknown.';else if(tab==='garage')message='Sale-level results combine current public sources. Date/location uncertainty stays labeled.';else if(tab==='deals')message='Hunter results are leads until sold comps, fees, condition and local availability are verified.';if(message){note.textContent=message;note.classList.remove('hidden');}else note.classList.add('hidden');}
function scanSummary(d,r){const q=$('search').value.trim(),label=tab==='deals'?'Opportunity Hunter / '+lane:tab;if(r.length)return r.length+' '+label+' results returned'+(q?' for “'+q+'”':'')+'.';if(d&&d.warning)return label+' returned no current results: '+d.warning;const state=txt(d&&d.provider_status);return state?label+' status: '+state.replace(/_/g,' ')+'. No usable rows returned.':label+' completed with no current verified or candidate rows.';}
function sortRows(r){if(tab==='deals')r.sort((a,b)=>analyzeOpportunity(b).score-analyzeOpportunity(a).score);else if(tab==='facebook')r.sort((a,b)=>(b.location_verified===true?1:0)-(a.location_verified===true?1:0)||(Number.isFinite(distance(a))?distance(a):9999)-(Number.isFinite(distance(b))?distance(b):9999)||(Number.isFinite(price(a))?price(a):999999)-(Number.isFinite(price(b))?price(b):999999));else if(tab==='auctions'||tab==='stores')r.sort((a,b)=>(Number.isFinite(distance(a))?distance(a):9999)-(Number.isFinite(distance(b))?distance(b):9999));return r;}
function emptyHtml(){const text=tab==='facebook'?'No public Facebook Marketplace cards were returned in this scan. Facebook often limits public indexing, so this does not mean Marketplace has no listings. Check the source status above or try a specific item.':tab==='auctions'?'No matching auction rows were returned. Check the source status above; live catalogs may be reachable even when the current item has no matching lots.':tab==='garage'?'No current garage/estate sale rows were returned for this radius. Source status above shows which public feeds answered.':tab==='stores'?'No nearby store rows were returned for this location/radius.':'No current results matched this lane. This is not proof that no resale opportunities exist.';return'<div class="empty"><strong>No results returned.</strong><div>'+esc(text)+'</div></div>';}
function render(){const q=$('search').value.trim().toLowerCase();let r=(cache[tab]||[]).filter(x=>laneMatch(x)&&(!q||JSON.stringify([title(x),x.description,x.reason,x.condition,x.retailer,x.source,x.marketplace,x.location_label,x.store_name,x.store_address]).toLowerCase().includes(q)));r=sortRows(r);const count=$('result-count');if(r.length){count.innerHTML='<strong>'+r.length+' result'+(r.length===1?'':'s')+'</strong><span>showing '+Math.min(visible,r.length)+' of '+r.length+'</span>';count.classList.remove('hidden');}else count.classList.add('hidden');$('content').innerHTML=r.length?r.slice(0,visible).map(card).join('')+(r.length>visible?'<button id="more" class="more">Show '+Math.min(20,r.length-visible)+' more</button>':''):emptyHtml();if($('more'))$('more').onclick=()=>{visible+=20;render();};}
async function load(){const requestId=++requestSeq,scan=$('scan');visible=20;const q=$('search').value.trim(),name=tab==='deals'?'Opportunity Hunter':tab==='facebook'?'Facebook public sources':tab;scan.disabled=true;$('source-strip').classList.add('hidden');$('scan-note').classList.add('hidden');$('result-count').classList.add('hidden');$('content').innerHTML='<div class="empty"><strong>Scanning '+esc(name)+(q?' for '+esc(q):'')+'…</strong><div>'+(tab==='facebook'?'Facebook public discovery can take longer because access/indexing varies by provider.':'Checking current source results and verification status.')+'</div></div>';try{const d=await api(tab);if(requestId!==requestSeq)return;rawCache[tab]=d;cache[tab]=dedupe(rows(d,tab));$('status').textContent=scanSummary(d,cache[tab]);renderHealth(d);render();}catch(e){if(requestId!==requestSeq)return;$('status').textContent=name+' failed: '+e.message;$('scan-note').textContent='Source unavailable is not the same as no listings. Other tabs remain usable.';$('scan-note').classList.remove('hidden');$('content').innerHTML=emptyHtml();}finally{if(requestId===requestSeq)scan.disabled=false;}}
async function loadSavedDeals(){const requestId=++requestSeq,scan=$('scan');visible=20;scan.disabled=true;$('source-strip').classList.add('hidden');$('scan-note').classList.add('hidden');$('result-count').classList.add('hidden');$('content').innerHTML='<div class="empty">Loading saved H38 buy leads…</div>';try{const d=await api('saved');if(requestId!==requestSeq)return;rawCache.deals=d;cache.deals=dedupe(rows(d,'deals'));$('status').textContent=cache.deals.length?cache.deals.length+' saved H38 buy leads loaded. Tap Scan Opportunity Hunter for current marketplace sources.':'No saved H38 resale leads are available yet. Tap Scan Opportunity Hunter for a current source check.';renderHealth(d);render();}catch(e){if(requestId!==requestSeq)return;$('status').textContent='Saved resale leads could not load: '+e.message;$('content').innerHTML=emptyHtml();}finally{if(requestId===requestSeq)scan.disabled=false;}}
function updateLaneUi(){document.querySelectorAll('.lane').forEach(b=>b.classList.toggle('active',b.dataset.lane===lane));$('hunter-lanes').classList.toggle('hidden',tab!=='deals');const copy={deals:'Hunter scans current/public sources for repairable equipment, motivated sellers and underpriced items. Screening spread is NOT verified profit.',facebook:'Public Facebook Marketplace discovery. Location-verified rows sort first; unverified candidates remain visible and labeled.',auctions:'Search live auction catalogs. Enter an item such as “tools”, “generator” or “trailer”; blank search shows broader auction results.',stores:'Find recognized resale/clearance stores near the saved ZIP or phone location.',garage:'Find current garage, estate, moving and yard-sale events from public sources.'};$('help').textContent=copy[tab]||'';$('search').placeholder=tab==='deals'?'Search item/model or leave blank for hunter scan…':tab==='facebook'?'Search Facebook item, e.g. mower or tools…':tab==='auctions'?'Search auction item or leave blank for nearby events…':tab==='garage'?'Optional filter; leave blank for local sale events…':'Optional store filter…';}
function selectTab(which){tab=which;visible=20;document.querySelectorAll('.tab').forEach(b=>b.classList.toggle('active',b.dataset.tab===tab));updateLaneUi();$('scan').textContent=tab==='deals'?'Scan Opportunity Hunter':'Scan '+document.querySelector('.tab.active').textContent;const d=rawCache[tab];if(cache[tab]){renderHealth(d);render();$('status').textContent=scanSummary(d,cache[tab]);}else{$('source-strip').classList.add('hidden');$('scan-note').classList.add('hidden');$('result-count').classList.add('hidden');$('content').innerHTML='<div class="empty"><strong>Ready to scan.</strong><div>Tap '+esc($('scan').textContent)+' to check current results.</div></div>';}}
document.querySelectorAll('.tab').forEach(b=>b.onclick=()=>selectTab(b.dataset.tab));document.querySelectorAll('.lane').forEach(b=>b.onclick=()=>{lane=b.dataset.lane||'all';visible=20;updateLaneUi();load();});$('scan').onclick=load;$('search').oninput=render;$('search').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();load();}};$('zip').onchange=()=>{lat=null;lon=null;cache={};rawCache={};saveLocation();if(tab==='deals')loadSavedDeals();};$('radius').onchange=()=>{cache={};rawCache={};saveLocation();if(tab==='deals')loadSavedDeals();};
$('loc').onclick=()=>{if(window.AndroidH38Deals&&AndroidH38Deals.requestLocation)AndroidH38Deals.requestLocation();else if(navigator.geolocation)navigator.geolocation.getCurrentPosition(p=>window.H38NativeLocationResult(p.coords.latitude,p.coords.longitude),()=>{$('status').textContent='Location unavailable. Enter a ZIP instead.';},{timeout:12000});else $('status').textContent='Location unavailable. Enter a ZIP instead.';};
window.H38NativeLocationResult=(a,b)=>{lat=Number(a);lon=Number(b);$('zip').value='';cache={};rawCache={};saveLocation();$('status').textContent='Phone location saved. Loading saved resale leads…';if(tab==='deals')loadSavedDeals();};
function applyHandoff(attempt){if($('app').classList.contains('hidden')){if(attempt<100)setTimeout(()=>applyHandoff(attempt+1),100);return;}restoreLocation();const h=handoffQuery();if(h.item||h.upc||h.sku){$('search').value=h.item||h.upc||h.sku;const box=document.createElement('section');box.className='card handoff';box.innerHTML='<strong>Deal ready to evaluate</strong><div class="meta">'+esc([h.item,h.store,h.upc?'UPC '+h.upc:'',h.sku?'SKU '+h.sku:'',h.buy?'Buy $'+h.buy:''].filter(Boolean).join(' · '))+'</div><p class="meta">Tap Scan Opportunity Hunter to search current sources. H38 will not call it proven profit until sold comps, fees, condition and availability are verified.</p>';$('app').insertBefore(box,$('app').children[1]);}}
async function gate(){const session=(await sb.auth.getSession()).data.session;if(!session){location.href='/functions/v1/h38-deals-shell';return;}const q=await sb.from('h38_product_entitlements').select('active,expires_at').eq('product_key','resale').maybeSingle();const ok=!!(q.data&&q.data.active&&(!q.data.expires_at||new Date(q.data.expires_at)>new Date()));$('app').classList.toggle('hidden',!ok);$('locked').classList.toggle('hidden',ok);if(ok){restoreLocation();selectTab('deals');await loadSavedDeals();applyHandoff(0);}}
gate();
</script></body></html>`;

Deno.serve(()=>new Response(html,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','content-security-policy':"default-src 'self'; img-src 'self' https: data:; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://unpkg.com; connect-src 'self' https://jqukmwtsgcsaruucnqja.supabase.co wss://jqukmwtsgcsaruucnqja.supabase.co; style-src 'self' 'unsafe-inline'; frame-ancestors 'self'"}}));
