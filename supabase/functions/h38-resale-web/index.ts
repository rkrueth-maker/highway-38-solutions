import "jsr:@supabase/functions-js/edge-runtime.d.ts";

const html = String.raw`<!doctype html>
<html><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover"><meta name="theme-color" content="#163650"><title>H38 Resale</title>
<style>
*{box-sizing:border-box}body{margin:0;font-family:system-ui,-apple-system,Segoe UI,Roboto,sans-serif;background:#eef3f6;color:#142735}.wrap{max-width:720px;margin:auto;padding:calc(14px + env(safe-area-inset-top)) 14px calc(28px + env(safe-area-inset-bottom))}.top{display:flex;align-items:center;gap:10px}.top a{text-decoration:none;color:#31526a;font-weight:750}.top h1{font-size:25px;margin:0}.card{background:#fff;border:1px solid #d7e1e7;border-radius:18px;padding:14px;margin:11px 0}.grid{display:grid;grid-template-columns:1fr 90px;gap:8px}input,select,button{font:inherit}input,select{width:100%;padding:12px;border:1px solid #c5d3dc;border-radius:12px}button{border:0;border-radius:12px;padding:12px 14px;background:#163650;color:#fff;font-weight:750;min-height:44px}.secondary{background:#dce7ed;color:#173449}.tabs,.lanes{display:flex;gap:7px;overflow:auto}.tab,.lane{white-space:nowrap;background:#dce7ed;color:#173449}.tab.active,.lane.active{background:#163650;color:#fff}.lanes{margin:0 0 10px}.lane{font-size:12px;padding:9px 10px;min-height:38px}.status{font-size:13px;color:#5a6e7b;margin:8px 0}.controls{display:grid;grid-template-columns:1fr auto;gap:8px;margin-bottom:4px}.item{padding:13px 0;border-top:1px solid #e2e9ed}.item:first-child{border-top:0}.item h3{font-size:16px;margin:0 0 4px}.meta{font-size:12px;color:#657987}.money{font-weight:850;font-size:18px}.profit{color:#14642d}.loss{color:#8c2d2d}.screen{color:#6b4d00}.badge{display:inline-block;border-radius:999px;background:#e6edf1;padding:4px 7px;font-size:11px;margin:0 4px 4px 0}.good{background:#dff3e5}.warn{background:#fff1d7}.risk{background:#fde7e7;color:#792828}.hunter{background:#e3eef8;color:#173d5d}.flag{margin-top:6px;padding:8px 9px;background:#f6f9fa;border-radius:10px;font-size:12px;color:#405865}.hidden{display:none}.empty{text-align:center;color:#71828c;padding:24px 8px}.more{width:100%;background:#dce7ed;color:#173449}.handoff{border-left:4px solid #163650}.actions{display:flex;gap:7px;flex-wrap:wrap;margin-top:8px}.action{display:inline-block;border:1px solid #bdccd5;border-radius:9px;padding:7px 9px;background:#f6f9fa;color:#173449;font-size:12px;font-weight:800;text-decoration:none}@media(max-width:480px){.grid{grid-template-columns:1fr 80px}.controls{grid-template-columns:1fr}.top h1{font-size:22px}}
</style></head>
<body data-h38-resale-web="opportunity-hunter-v13"><main class="wrap"><div class="top"><a href="/functions/v1/h38-deals-shell">‹ H38 Deals</a><h1>Resale</h1></div><section id="locked" class="card hidden"><h2>Not enabled</h2><p>This account does not include Resale.</p></section><div id="app" class="hidden"><section class="card"><div class="grid"><input id="zip" inputmode="numeric" maxlength="5" placeholder="ZIP code"><select id="radius"><option>25</option><option selected>50</option><option>75</option><option>100</option></select><button id="loc" class="secondary" style="grid-column:1/-1">Use phone location</button></div><div id="status" class="status">Loading saved H38 buy leads…</div></section><div class="tabs"><button class="tab active" data-tab="deals">Opportunity Hunter</button><button class="tab" data-tab="facebook">Facebook</button><button class="tab" data-tab="auctions">Auctions</button><button class="tab" data-tab="stores">Nearby Stores</button><button class="tab" data-tab="garage">Garage/Estate</button></div><section class="card"><div id="hunter-lanes" class="lanes"><button class="lane active" data-lane="all">All Opportunities</button><button class="lane" data-lane="repair">Repairable</button><button class="lane" data-lane="motivated">Motivated Seller</button><button class="lane" data-lane="straight">Underpriced</button></div><div class="controls"><input id="search" type="search" placeholder="Search item/model or leave blank for hunter scan…"><button id="scan">Scan Opportunity Hunter</button></div><div id="help" class="meta" style="margin-bottom:8px">Hunter scans current/public sources for repairable equipment, motivated sellers and ordinary underpriced items. Screening spread is NOT verified profit. Verify sold comps, fees, condition, title and local availability before buying.</div><section id="content"><div class="empty">Loading saved H38 buy leads…</div></section></section></div></main>
<script src="https://cdn.jsdelivr.net/npm/@supabase/supabase-js@2.95.0/dist/umd/supabase.min.js" onerror="this.onerror=null;this.src='https://unpkg.com/@supabase/supabase-js@2.95.0/dist/umd/supabase.min.js'"></script>
<script>
const SUPABASE_URL='https://jqukmwtsgcsaruucnqja.supabase.co';
const KEY='sb_publishable_XrF41kGmTC2SmSTgPvo5OQ_vqcBd0N1';
const sb=supabase.createClient(SUPABASE_URL,KEY);
const $=id=>document.getElementById(id);
let tab='deals',lane='all',cache={},lat=null,lon=null,visible=20;
const LOCATION_KEY='h38-resale-location-v1',H38_SHARED_LOCATION='h38-shopping-location-v1';
const txt=v=>String(v==null?'':v).trim(),num=v=>Number(v);
const HUNTER_TERMS={
  all:['ATV needs work','UTV project','snowmobile needs work','lawn tractor project','zero turn needs work',"generator won't start",'pressure washer needs work','welder project','air compressor as is','trailer needs work','mechanic special','make offer OBO'],
  repair:['ATV needs work','UTV project','snowmobile needs work','lawn tractor project','zero turn needs work',"generator won't start",'pressure washer needs work','welder project','air compressor as is','trailer needs work','mechanic special','ran when parked'],
  motivated:['must sell','need gone','make offer','OBO','moving sale','price reduced','cash today','as-is','project OBO','mechanic special OBO','ATV OBO','generator OBO'],
  straight:['generator','snowblower','lawn mower','zero turn','ATV','UTV','DeWalt','Milwaukee','welder','air compressor','trailer','toolbox']
};
function esc(v){return String(v==null?'':v).replace(/[&<>"']/g,function(c){return {'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c];});}
function saveLocation(){const x={zip:$('zip').value.trim(),radius:$('radius').value,radiusMiles:num($('radius').value),lat:lat,lon:lon};localStorage.setItem(H38_SHARED_LOCATION,JSON.stringify(x));localStorage.setItem(LOCATION_KEY,JSON.stringify(x));}
function restoreLocation(){try{const x=JSON.parse(localStorage.getItem(H38_SHARED_LOCATION)||localStorage.getItem(LOCATION_KEY)||'{}');$('zip').value=x.zip||'';$('radius').value=String(x.radius||x.radiusMiles||'50');lat=Number.isFinite(x.lat)?x.lat:null;lon=Number.isFinite(x.lon)?x.lon:null;if(Object.keys(x).length)localStorage.setItem(H38_SHARED_LOCATION,JSON.stringify(x));}catch(e){}}
function liveTerms(){const q=$('search').value.trim();if(tab==='deals'){if(q)return[q,q+' needs work',q+' project',q+' OBO',q+' as is',q+" won't start",q+' mechanic special'].slice(0,12);return(HUNTER_TERMS[lane]||HUNTER_TERMS.all).slice(0,12);}return q?[q]:['tools','electronics','appliances','lawn mower','generator','toolbox'];}
function payload(){saveLocation();const radius=num($('radius').value);return{postal:$('zip').value.trim(),zip:$('zip').value.trim(),radius_miles:radius,radiusMiles:radius,radius:radius,lat:lat,lon:lon,deep_verify:true,terms:liveTerms()};}
function rows(d,source){if(Array.isArray(d))return d;if(!d)return[];if(source==='deals')return[].concat(Array.isArray(d.opportunities)?d.opportunities:[],Array.isArray(d.candidates)?d.candidates.map(function(x){return Object.assign({verification_status:'CANDIDATE · NEEDS SOLD COMPS'},x);}):[],Array.isArray(d.auction_candidates)?d.auction_candidates:[]);if(source==='facebook')return(Array.isArray(d.results)&&d.results.length?d.results:[]).concat(Array.isArray(d.candidates)?d.candidates.map(function(x){return Object.assign({verification_status:'LOCATION NEEDS PROOF'},x);}):[]);if(source==='stores')return Array.isArray(d.stores)?d.stores:(Array.isArray(d.readings)?d.readings:[]);if(Array.isArray(d.results))return d.results;if(Array.isArray(d.deals))return d.deals;if(Array.isArray(d.items))return d.items;if(Array.isArray(d.rows))return d.rows;return[];}
async function api(action){const q=await sb.functions.invoke('h38-resale-api',{body:{action:action,payload:payload()}});if(q.error)throw q.error;if(q.data&&q.data.error)throw new Error(q.data.error);return q.data?q.data.data:null;}
function first(r,keys,min){for(let i=0;i<keys.length;i++){const raw=r[keys[i]];if(raw==null||txt(raw)==='')continue;const x=num(raw);if(Number.isFinite(x)&&x>=min)return x;}return NaN;}
function price(r){return first(r,['buy_price','price','current_price','sale_price','asking_price','bid'],0);}
function resale(r){return first(r,['expected_resale','resale_price','market_value','estimated_value','comparable_price'],0.000001);}
function screeningSpread(r){const raw=r.screening_spread,direct=raw==null||txt(raw)===''?NaN:num(raw),b=price(r),s=resale(r),fees=num(r.estimated_fees||r.fees||0),ship=num(r.estimated_shipping||r.shipping||0),other=num(r.other_costs||0);if(Number.isFinite(direct))return direct;return Number.isFinite(b)&&Number.isFinite(s)?s-b-fees-ship-other:NaN;}
function verifiedProfit(r){const verified=r.profit_verified===true||r.verified_profit===true||txt(r.profit_status).toLowerCase()==='verified';if(!verified)return{verified:false,value:NaN};const raw=r.net_profit!=null&&txt(r.net_profit)!==''?r.net_profit:r.verified_profit_amount,direct=raw==null||txt(raw)===''?NaN:num(raw);return{verified:true,value:Number.isFinite(direct)?direct:screeningSpread(r)};}
function title(r){return txt(r.title||r.name||r.store_name||r.item_name||r.event_name||'Opportunity');}
function distance(r){const raw=r.distance_miles;return raw==null||txt(raw)===''?NaN:num(raw);}
function candidateText(r){return [title(r),r.description,r.reason,r.notes,r.note,r.condition,r.status,r.verification_status,r.source,r.marketplace].map(txt).join(' ').toLowerCase();}
function analyzeOpportunity(r){const text=candidateText(r),repair=/needs work|project|mechanic special|won'?t move|slips?|clutch|belt|no reverse|won'?t start|needs carb|ran when parked|needs battery|as[- ]is|not running|for parts/.test(text),motivated=/\bobo\b|make offer|must sell|needs? gone|cash today|moving|price reduced|priced to sell|best offer|today only/.test(text),spread=screeningSpread(r),hasSource=!!txt(r.url||r.source_url||r.listing_url),local=!!(r.local_store_verified||r.location_verified||Number.isFinite(distance(r))),p=price(r);let score=0,tags=[],reasons=[];if(repair){score+=35;tags.push('REPAIRABLE');reasons.push('repair/distress language');}if(motivated){score+=25;tags.push('MOTIVATED SELLER');reasons.push('seller flexibility or urgency');}if(Number.isFinite(spread)){const ratio=Number.isFinite(p)&&p>0?spread/p:0;score+=Math.max(0,Math.min(30,Math.round(ratio*30)));if(spread>0){tags.push('PRICE-SPREAD LEAD');reasons.push('asking/buy price leaves a screening spread');}}if(local){score+=10;tags.push('LOCATION EVIDENCE');}if(hasSource)score+=5;if(!repair&&!motivated){tags.push('UNDERPRICE CHECK');reasons.push('ordinary resale candidate worth comping');score+=10;}let hint='Verify exact model, condition, ownership/title, sold comps, fees and pickup cost before buying.';if(/slips?|clutch|belt/.test(text))hint='Inspect belt/CVT/clutch condition, engagement and burnt-belt smell before valuing.';else if(/no reverse|won'?t move|transmission|differential/.test(text))hint='Treat drivetrain/transmission/differential as high-risk until movement and fluid are checked.';else if(/won'?t start|needs carb|needs battery|ran when parked/.test(text))hint='Verify engine turns, battery, fuel, spark and compression; do not assume a carb-only repair.';else if(/trailer/.test(text))hint='Inspect frame, axle/bearings, tires, lights and title/registration before valuing.';return{repair:repair,motivated:motivated,spread:spread,score:score,tags:tags.slice(0,3),reason:reasons.join(' + '),hint:hint};}
function laneMatch(r){if(tab!=='deals'||lane==='all')return true;const a=analyzeOpportunity(r);if(lane==='repair')return a.repair;if(lane==='motivated')return a.motivated;if(lane==='straight')return !a.repair&&!a.motivated;return true;}
function sourceLink(r){const u=txt(r.url||r.source_url||r.listing_url);return u.startsWith('https://')?'<a href="'+esc(u)+'" target="_blank" rel="noopener" style="font-size:12px">Open source</a>':'';}
function handoffQuery(){const p=new URLSearchParams(location.search);return{item:txt(p.get('item')),store:txt(p.get('store')),upc:txt(p.get('upc')),sku:txt(p.get('sku')),buy:txt(p.get('buy'))};}
function couponLink(r){const q=new URLSearchParams(),name=title(r),p=price(r);if(name)q.set('item',name);if(r.retailer)q.set('store',r.retailer);if(r.upc)q.set('upc',r.upc);if(r.sku)q.set('sku',r.sku);if(Number.isFinite(p))q.set('buy',String(p));return'/functions/v1/h38-coupon-web?'+q.toString();}
function card(r){if(tab==='stores')return'<div class="item"><h3>'+esc(title(r))+'</h3><span class="badge good">NEARBY STORE</span><div class="meta">'+esc(txt(r.store_address||r.address||''))+(Number.isFinite(distance(r))?' · '+distance(r).toFixed(1)+' miles':'')+'</div><div class="meta">'+esc(txt(r.retailer||r.source||''))+'</div></div>';const proof=txt(r.verification_status||r.location_status||r.price_scope||r.provider_status);if(tab==='garage'){const source=txt(r.source||r.marketplace||'Source not identified'),place=txt(r.location_label||r.location||r.city||'Location needs proof'),when=txt(r.date_label||r.event_time||r.event_date||'Date needs proof');return'<div class="item"><h3>'+esc(title(r))+'</h3><div><span class="badge">'+esc(txt(r.event_type||'SALE'))+'</span>'+(proof?'<span class="badge warn">'+esc(proof)+'</span>':'')+'</div><div class="meta"><strong>When:</strong> '+esc(when)+'</div><div class="meta"><strong>Where:</strong> '+esc(place)+(Number.isFinite(distance(r))?' · '+distance(r).toFixed(1)+' miles':' · distance unknown')+'</div><div class="meta"><strong>Source:</strong> '+esc(source)+'</div>'+sourceLink(r)+'</div>';}const p=price(r),s=resale(r),a=analyzeOpportunity(r),vp=verifiedProfit(r),roi=Number.isFinite(a.spread)&&Number.isFinite(p)&&p>0?a.spread/p*100:NaN,status=vp.verified?'<span class="badge good">PROFIT VERIFIED</span>':'<span class="badge risk">INSPECT · PROFIT NOT VERIFIED</span>',tags=tab==='deals'?a.tags.map(function(x){return'<span class="badge hunter">'+esc(x)+'</span>';}).join(''):'';let money=(Number.isFinite(p)?'Buy $'+p.toFixed(2):'Buy price unknown')+(Number.isFinite(s)?' → resale estimate $'+s.toFixed(2):'');let math='';if(vp.verified&&Number.isFinite(vp.value))math='<span class="'+(vp.value>=0?'profit':'loss')+'">Verified profit $'+vp.value.toFixed(2)+'</span>';else if(Number.isFinite(a.spread))math='<span class="screen">Screening spread $'+a.spread.toFixed(2)+(Number.isFinite(roi)?' · '+Math.round(roi)+'% screening ROI':'')+' · NOT PROFIT</span>';const why=tab==='deals'?'<div class="flag"><strong>Hunter '+a.score+':</strong> '+esc(a.reason||'candidate needs market verification')+'<br><strong>Inspect:</strong> '+esc(a.hint)+'</div>':'';return'<div class="item"><h3>'+esc(title(r))+'</h3><div>'+status+tags+(proof?'<span class="badge warn">'+esc(proof)+'</span>':'')+'</div><div class="money">'+money+'</div><div class="meta">'+math+(math?' · ':'')+esc(txt(r.retailer||r.source||r.marketplace||r.location_label||''))+'</div>'+why+sourceLink(r)+'<div class="actions"><a class="action" href="'+esc(couponLink(r))+'">Build coupon stack</a></div></div>';}
function dedupe(list){const seen=new Set();return list.filter(function(r){const k=[txt(r.url||r.source_url||r.listing_url),title(r).toLowerCase(),Number.isFinite(price(r))?price(r):''].join('|');if(seen.has(k))return false;seen.add(k);return true;});}
function healthSummary(d){const h=d&&d.source_health;if(!h||typeof h!=='object')return'';const list=Object.keys(h).map(function(name){const x=h[name]||{},n=Number(x.count||0),state=txt(x.status||x.provider_status||'unknown').replace(/_/g,' ');return name+': '+(n?n+' found':state);});return list.length?' Sources — '+list.join(' · ')+'.':'';}
function scanSummary(d,r){const q=$('search').value.trim(),health=healthSummary(d),label=tab==='deals'?'Opportunity Hunter / '+lane:tab;if(r.length)return r.length+' '+label+' results returned'+(q?' for "'+q+'"':'')+'.'+health;if(d&&d.warning)return label+' returned no current results: '+d.warning+health;if(d&&d.provider_status)return label+' status: '+d.provider_status+'. No current results.'+health;return label+' completed with no current verified or candidate results.'+health;}
async function load(){visible=20;const q=$('search').value.trim();$('content').innerHTML='<div class="empty">Scanning '+esc(tab==='deals'?'Opportunity Hunter':tab)+(q?' for '+esc(q):'')+'…</div>';try{const d=await api(tab);cache[tab]=dedupe(rows(d,tab));$('status').textContent=scanSummary(d,cache[tab]);render();}catch(e){$('status').textContent=(tab==='deals'?'Opportunity Hunter':tab)+' failed: '+e.message;$('content').innerHTML='<div class="empty">Source unavailable. Your other tabs remain usable. This is not proof that no opportunities exist.</div>';}}
async function loadSavedDeals(){visible=20;$('content').innerHTML='<div class="empty">Loading saved H38 buy leads…</div>';try{const d=await api('saved');cache.deals=dedupe(rows(d,'deals'));$('status').textContent=cache.deals.length?cache.deals.length+' saved H38 buy leads loaded. These are leads, not verified profit. Tap Scan Opportunity Hunter to search current sources for repairable, motivated-seller and underpriced opportunities.':'No saved H38 resale leads are available yet. Tap Scan Opportunity Hunter for a current source check.';render();}catch(e){$('status').textContent='Saved resale leads could not load: '+e.message;$('content').innerHTML='<div class="empty">Tap Scan Opportunity Hunter for a current source check.</div>';}}
function render(){const q=$('search').value.trim().toLowerCase();let r=(cache[tab]||[]).filter(function(x){return laneMatch(x)&&(!q||JSON.stringify([title(x),x.description,x.reason,x.condition,x.retailer,x.source,x.marketplace,x.location_label,x.store_name,x.store_address]).toLowerCase().includes(q));});if(tab==='deals')r.sort(function(a,b){const aa=analyzeOpportunity(a),bb=analyzeOpportunity(b);if(bb.score!==aa.score)return bb.score-aa.score;return(Number.isFinite(bb.spread)?bb.spread:-999999)-(Number.isFinite(aa.spread)?aa.spread:-999999);});$('content').innerHTML=r.length?r.slice(0,visible).map(card).join('')+(r.length>visible?'<button id="more" class="more">Show '+Math.min(20,r.length-visible)+' more</button>':''):'<div class="empty"><strong>No results returned.</strong><p>This can mean the source is unavailable, the selected lane has no current matches, or no verified/candidate rows were returned. It is not proof that no resale opportunities exist.</p></div>';if($('more'))$('more').onclick=function(){visible+=20;render();};}
function updateLaneUi(){document.querySelectorAll('.lane').forEach(function(b){b.classList.toggle('active',b.dataset.lane===lane);});$('hunter-lanes').classList.toggle('hidden',tab!=='deals');$('help').textContent=tab==='deals'?'Hunter scans current/public sources for repairable equipment, motivated sellers and ordinary underpriced items. Screening spread is NOT verified profit. Verify sold comps, fees, condition, title and local availability before buying.':'Type an item to search that live source. Source unavailable is not the same as no listings.';}
function selectTab(which){tab=which;visible=20;document.querySelectorAll('.tab').forEach(function(b){b.classList.toggle('active',b.dataset.tab===tab);});updateLaneUi();$('scan').textContent=tab==='deals'?'Scan Opportunity Hunter':'Scan '+document.querySelector('.tab.active').textContent;if(cache[tab])render();else $('content').innerHTML='<div class="empty">Tap '+esc($('scan').textContent)+' when you are ready.</div>';}
document.querySelectorAll('.tab').forEach(function(b){b.onclick=function(){selectTab(b.dataset.tab);};});
document.querySelectorAll('.lane').forEach(function(b){b.onclick=function(){lane=b.dataset.lane||'all';visible=20;updateLaneUi();load();};});
$('scan').onclick=load;
$('search').oninput=render;
$('search').onkeydown=function(e){if(e.key==='Enter'){e.preventDefault();load();}};
$('zip').onchange=function(){lat=null;lon=null;cache={};saveLocation();if(tab==='deals')loadSavedDeals();};
$('radius').onchange=function(){cache={};saveLocation();if(tab==='deals')loadSavedDeals();};
$('loc').onclick=function(){if(window.AndroidH38Deals&&AndroidH38Deals.requestLocation)AndroidH38Deals.requestLocation();else if(navigator.geolocation)navigator.geolocation.getCurrentPosition(function(p){window.H38NativeLocationResult(p.coords.latitude,p.coords.longitude);},function(){$('status').textContent='Location unavailable. Enter a ZIP instead.';},{timeout:12000});else $('status').textContent='Location unavailable. Enter a ZIP instead.';};
window.H38NativeLocationResult=function(a,b){lat=Number(a);lon=Number(b);$('zip').value='';cache={};saveLocation();$('status').textContent='Phone location saved. Loading saved resale leads…';if(tab==='deals')loadSavedDeals();};
function applyHandoff(attempt){if($('app').classList.contains('hidden')){if(attempt<100)setTimeout(function(){applyHandoff(attempt+1);},100);return;}restoreLocation();const h=handoffQuery();if(h.item||h.upc||h.sku){$('search').value=h.item||h.upc||h.sku;const box=document.createElement('section');box.className='card handoff';box.innerHTML='<strong>Deal ready to evaluate</strong><div class="meta">'+esc([h.item,h.store,h.upc?'UPC '+h.upc:'',h.sku?'SKU '+h.sku:'',h.buy?'Buy $'+h.buy:''].filter(Boolean).join(' · '))+'</div><p class="meta">Tap Scan Opportunity Hunter to search the item plus repairable, project, OBO and mechanic-special variants. H38 will not call it proven profit until sold comps, fees, condition and availability are verified.</p>';$('app').insertBefore(box,$('app').children[1]);}}
async function gate(){const session=(await sb.auth.getSession()).data.session;if(!session){location.href='/functions/v1/h38-deals-shell';return;}const q=await sb.from('h38_product_entitlements').select('active,expires_at').eq('product_key','resale').maybeSingle();const ok=!!(q.data&&q.data.active&&(!q.data.expires_at||new Date(q.data.expires_at)>new Date()));$('app').classList.toggle('hidden',!ok);$('locked').classList.toggle('hidden',ok);if(ok){restoreLocation();selectTab('deals');await loadSavedDeals();applyHandoff(0);}}
gate();
</script></body></html>`;

Deno.serve(()=>new Response(html,{headers:{'content-type':'text/html; charset=utf-8','cache-control':'no-store','content-security-policy':"default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://cdn.jsdelivr.net https://unpkg.com; connect-src 'self' https://jqukmwtsgcsaruucnqja.supabase.co wss://jqukmwtsgcsaruucnqja.supabase.co; style-src 'self' 'unsafe-inline'; frame-ancestors 'self'"}}));
