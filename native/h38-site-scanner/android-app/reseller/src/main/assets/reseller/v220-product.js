'use strict';
(function(){
  window.H38_SCOUT_V220_PRODUCT=true;
  function ensure220Styles(){if(document.getElementById('h38v220Styles'))return;const s=document.createElement('style');s.id='h38v220Styles';s.textContent='.decision-card{border:2px solid #0d2a3e}.decision-head{display:flex;align-items:flex-start;justify-content:space-between;gap:10px}.decision-head h2{margin:2px 0 0;font-size:26px}.decision-score{font-size:13px;font-weight:900;border-radius:999px;padding:7px 10px;background:#e9f0f3}.decision-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin:12px 0}.decision-metric{background:#f5f8fa;border-radius:12px;padding:10px;min-width:0}.decision-metric strong{display:block;font-size:18px;overflow-wrap:anywhere}.decision-metric span{font-size:10px;color:var(--muted);font-weight:800}.decision-actions{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.decision-actions button{min-width:0}.truth-note{padding:9px 10px;border-radius:10px;background:#fff7dc;font-size:11px;line-height:1.45}.track-card{border-left:4px solid var(--accent)}.track-filters{display:grid;grid-template-columns:1fr 1fr;gap:8px}.score-reason{display:flex;justify-content:space-between;gap:12px;padding:8px 0;border-bottom:1px solid var(--line);font-size:12px}.feed-lanes{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.feed-lane{background:#f5f8fa;border-radius:12px;padding:10px}.feed-lane strong{display:block}.feed-mini{padding:8px 0;border-top:1px solid var(--line);font-size:12px}.feed-mini:first-of-type{border-top:0}.max-bid{background:#eef7f3;border-radius:12px;padding:12px;margin-top:10px}.max-bid strong{font-size:24px}.where-look{background:#eef4f8;border-radius:12px;padding:10px;margin:10px 0}.price-origin{display:block;margin-top:-7px;margin-bottom:8px;font-size:10px;color:var(--muted);font-weight:800}@media(max-width:390px){.decision-grid,.feed-lanes,.track-filters{grid-template-columns:1fr}.decision-actions{grid-template-columns:1fr 1fr}.decision-actions .wide-action{grid-column:1/-1}}';document.head.appendChild(s)}
  const money=v=>Number.isFinite(Number(v))?'$'+Number(v).toFixed(2):'Unknown';
  const pct=v=>Number.isFinite(Number(v))?Math.round(Number(v)*100)+'%':'Unknown';
  const oldAddWatch=addWatch;
  addWatch=function(term,targets={}){oldAddWatch(term,targets);const existing=H38Track.list().find(x=>x.watchType==='keywords'&&x.keywords.some(k=>norm(k)===norm(term)));if(!existing)H38Track.fromOpportunity({title:term},{radius:state.radius,centerLocation:{...state.location},minimumExpectedProfit:Number(targets.min_profit)||null,minimumROI:Number(targets.min_roi)?Number(targets.min_roi)/100:null,maxBuyPrice:Number(targets.max_buy)||null});};
  function scanComp(){const m=state.scan.market||{},sold=m.market||m.evidence?.sold?.[0]||{},active=m.evidence?.active?.[0]||m.active||{},verified=String(sold.status||'').toLowerCase()==='verified';return {soldCount:verified?Number(sold.sample_count||sold.samples||m.comp_samples||0):0,activeCount:Number(active.count||active.sample_count||0)||null,soldMedian:verified?Number(sold.median||sold.typical||m.flip?.estimated_resale||0)||null:null,soldLow:verified?Number(sold.low||sold.range_low||0)||null:null,soldHigh:verified?Number(sold.high||sold.range_high||0)||null:null,askingLow:Number(active.low||active.range_low||0)||null,askingHigh:Number(active.high||active.range_high||0)||null,marketplace:sold.marketplace||m.marketplace||'',conditionMatch:sold.condition_match||'unknown',freshnessDays:Number(sold.freshness_days||0)||null,confidence:sold.confidence||state.scan.identification?.confidence||'unknown'};}
  function scanOpportunity(){const id=state.scan.identification||{},m=state.scan.market||{},settings=H38Track.settings(),adj=state.scan.profitAdjust||{},buy=state.scan.buyPrice!==''&&Number.isFinite(Number(state.scan.buyPrice))?Number(state.scan.buyPrice):null,comp=scanComp();return {input:{acquisitionPrice:buy,...comp,marketplaceFeeRate:Number(adj.marketplaceFeeRate??settings.marketplaceFeeRate),paymentFeeRate:Number(adj.paymentFeeRate??settings.paymentFeeRate),shipping:Number(adj.shipping??settings.defaultShipping),taxRate:Number(adj.taxRate??0),travelCost:Number(adj.travelCost??0),buyerPremium:Number(adj.buyerPremium??0),otherAcquisitionFees:Number(adj.otherAcquisitionFees??0),minimumProfit:Number(settings.minimumProfit),minimumROI:Number(settings.minimumROI),distanceMiles:null,shippingBurden:Number(adj.shipping??settings.defaultShipping)>25?'high':Number(adj.shipping??settings.defaultShipping)>0?'moderate':'low',sizeWeightBurden:'low'},op:{title:id.likely_item||id.search_query||state.scan.hint,upc:state.scan.upc,brand:id.brand,category:id.category,source:comp.marketplace||'Scan',acquisitionPrice:buy}};}
  function detailComps(ev){detail(`<div class="sheet-head"><div><div class="eyebrow">RESALE EVIDENCE</div><h2>Sold comps</h2></div><button class="icon-btn" onclick="show('detailSheet',false)">×</button></div><section class="card"><div class="decision-grid"><div class="decision-metric"><strong>${ev.comp.soldCount}</strong><span>SOLD COMPS</span></div><div class="decision-metric"><strong>${money(ev.comp.median)}</strong><span>MEDIAN SOLD</span></div><div class="decision-metric"><strong>${ev.comp.low!==null&&ev.comp.high!==null?money(ev.comp.low)+'–'+money(ev.comp.high):'Unknown'}</strong><span>TRIMMED RANGE</span></div><div class="decision-metric"><strong>${ev.comp.activeCount??'Unknown'}</strong><span>ACTIVE LISTINGS</span></div></div><p class="small muted">Marketplace: ${esc(ev.comp.marketplace||'not specified')} · condition match: ${esc(ev.comp.conditionMatch||'unknown')} · confidence: ${esc(ev.comp.confidence||'unknown')}</p>${!ev.comp.established?'<div class="truth-note"><strong>ASKING MARKET ONLY</strong><br>Active asking prices never establish expected resale.</div>':''}</section>`);}
  function detailScore(ev){detail(`<div class="sheet-head"><div><div class="eyebrow">DEAL SCORE</div><h2>${ev.dealScore===null?'Not scored':ev.dealScore+'/100'}</h2></div><button class="icon-btn" onclick="show('detailSheet',false)">×</button></div><section class="card">${(ev.scoreReasons||[]).map(x=>`<div class="score-reason"><span>${esc(x.split(':')[0])}</span><strong>${esc(x.split(':').slice(1).join(':').trim())}</strong></div>`).join('')||'<p class="muted small">A score is withheld until price and sold-value evidence are sufficient.</p>'}${(ev.reasons||[]).map(x=>`<p class="small">• ${esc(x)}</p>`).join('')}${(ev.warnings||[]).map(x=>`<p class="small muted">⚠ ${esc(x)}</p>`).join('')}</section>`);}
  function auctionBlock(ev){const a=state.scan.auctionContext;if(!a)return'';const s=H38Track.settings(),bid=H38Profit.maxBid({...ev.comp,buyerPremiumRate:a.premiumRate,quantity:Number(a.quantity||1),marketplaceFeeRate:Number(state.scan.profitAdjust?.marketplaceFeeRate??s.marketplaceFeeRate),paymentFeeRate:Number(state.scan.profitAdjust?.paymentFeeRate??s.paymentFeeRate),shipping:Number(state.scan.profitAdjust?.shipping??s.defaultShipping),travelCost:Number(a.travelCost||0),riskBuffer:Number(a.riskBuffer||0),otherFixedAcquisitionFees:Number(a.otherFees||0),taxRate:Number(a.taxRate||0),minimumProfit:Number(a.minimumProfit??s.auctionTargetProfit),minimumROI:Number(a.minimumROI??s.auctionMinimumROI)});return `<div class="max-bid"><div class="eyebrow">AUCTION MAX BID</div>${bid.status==='PROVEN'?`<strong>${money(bid.maxHammerBid)}</strong><div class="small muted">Maximum hammer bid using sold comps, premium/tax, selling costs, travel, target profit and ROI.</div>`:`<strong>NOT YET PROVEN</strong><div class="small muted">${esc(bid.reason)}</div>`}<div class="track-filters" style="margin-top:10px"><label>Buyer premium %<input id="auctionPremiumInput" inputmode="decimal" value="${a.premiumRate!==null&&a.premiumRate!==undefined?(Number(a.premiumRate)*100):''}" placeholder="Required"></label><label>Tax %<input id="auctionTaxInput" inputmode="decimal" value="${Number(a.taxRate||0)*100}"></label><label>Travel cost<input id="auctionTravelInput" inputmode="decimal" value="${Number(a.travelCost||0)}"></label><label>Risk buffer<input id="auctionRiskInput" inputmode="decimal" value="${Number(a.riskBuffer||0)}"></label></div></div>`;}
  function decorateScan(){ensure220Styles();const p=$('scanPage');if(!p||!state.scan.identification)return;const {input,op}=scanOpportunity(),ev=H38Profit.evaluateOpportunity(input),form=p.querySelectorAll('.card')[1]||null;if(form){const price=$('scanPrice');if(price&&!p.querySelector('.price-origin'))price.insertAdjacentHTML('afterend','<span class="price-origin">Acquisition price source: Entered by you</span>');}
    const existing=$('profitDecisionCard');if(existing)existing.remove();const card=document.createElement('section');card.id='profitDecisionCard';card.className='card decision-card';const score=ev.dealScore===null?'—':ev.dealScore;card.innerHTML=`<div class="decision-head"><div><div class="eyebrow">SCOUT DECISION</div><h2>${esc(ev.recommendedAction)}${ev.dealScore===null?'':' — '+score+'/100'}</h2></div><span class="decision-score">${ev.comp?.confidence?esc(String(ev.comp.confidence).toUpperCase()):'UNKNOWN'} CONF.</span></div>${ev.recommendedAction==='NEEDS COMP'?'<div class="truth-note"><strong>Resale estimate: NOT YET PROVEN</strong><br>Sold/completed evidence is required. Active asking prices remain secondary context only.</div>':''}<div class="decision-grid"><div class="decision-metric"><strong>${money(ev.acquisitionPrice)}</strong><span>PURCHASE PRICE</span></div><div class="decision-metric"><strong>${ev.resaleEstimate!==undefined?money(ev.resaleEstimate):'NOT PROVEN'}</strong><span>${ev.comp?.established?'SOLD-BASED RESALE':'RESALE'}</span></div><div class="decision-metric"><strong>${ev.expectedProfit!==undefined?money(ev.expectedProfit):'Unknown'}</strong><span>EXPECTED PROFIT</span></div><div class="decision-metric"><strong>${ev.roi!==undefined?pct(ev.roi):'Unknown'}</strong><span>ROI</span></div><div class="decision-metric"><strong>${ev.expectedNetProceeds!==undefined?money(ev.expectedNetProceeds):'Unknown'}</strong><span>EXPECTED NET</span></div><div class="decision-metric"><strong>${ev.sellThrough!==null&&ev.sellThrough!==undefined?pct(ev.sellThrough):'Unknown'}</strong><span>SELL-THROUGH</span></div></div><div class="truth-note">Current cost model: ${esc(H38Track.settings().defaultMarketplace)} resale defaults · marketplace fee ${pct(Number(state.scan.profitAdjust?.marketplaceFeeRate??H38Track.settings().marketplaceFeeRate))} · shipping ${money(Number(state.scan.profitAdjust?.shipping??H38Track.settings().defaultShipping))}. Adjust below when the actual selling path differs.</div><div class="track-filters" style="margin-top:10px"><label>Marketplace fee %<input id="profitFeeInput" inputmode="decimal" value="${Number(state.scan.profitAdjust?.marketplaceFeeRate??H38Track.settings().marketplaceFeeRate)*100}"></label><label>Shipping<input id="profitShippingInput" inputmode="decimal" value="${Number(state.scan.profitAdjust?.shipping??H38Track.settings().defaultShipping)}"></label></div>${auctionBlock(ev)}<div class="decision-actions" style="margin-top:12px"><button id="decisionBuy" class="primary" ${ev.recommendedAction==='NEEDS COMP'||ev.recommendedAction==='NEEDS PRICE'?'disabled':''}>BUY</button><button id="decisionTrack" class="secondary">TRACK</button><button id="decisionPass" class="secondary">PASS</button><button id="decisionComps" class="mini-btn">See comps</button><button id="decisionScore" class="mini-btn">Why this score?</button></div>`;p.appendChild(card);
    $('decisionBuy').onclick=async()=>{H38Track.recordDecision('BUY',op,ev,{purchasePrice:ev.acquisitionPrice});await recordPurchase({title:op.title,buy_price:ev.acquisitionPrice,resale_estimate:ev.resaleEstimate,source:op.source});notice('BUY saved with the current evidence snapshot.','good')};$('decisionTrack').onclick=()=>{const t=H38Track.fromOpportunity(op,{radius:state.radius,centerLocation:{...state.location},minimumExpectedProfit:H38Track.settings().minimumProfit,minimumROI:H38Track.settings().minimumROI});H38Track.recordDecision('TRACK',op,ev);notice(`Tracked ${t.keywords[0]||t.upc||op.title}.`,'good')};$('decisionPass').onclick=()=>{H38Track.recordDecision('PASS',op,ev,{purchasePrice:ev.acquisitionPrice});notice('PASS saved with the current evidence snapshot.','good')};$('decisionComps').onclick=()=>detailComps(ev);$('decisionScore').onclick=()=>detailScore(ev);
    const bindNumber=(id,field,scale=1)=>{const el=$(id);if(el)el.onchange=()=>{state.scan.profitAdjust=state.scan.profitAdjust||{};state.scan.profitAdjust[field]=(Number(el.value)||0)*scale;renderScan()}};bindNumber('profitFeeInput','marketplaceFeeRate',.01);bindNumber('profitShippingInput','shipping',1);
    if(state.scan.auctionContext){const a=state.scan.auctionContext;const bindAuction=(id,field,scale=1,nullable=false)=>{const el=$(id);if(el)el.onchange=()=>{const raw=el.value.trim();a[field]=nullable&&raw===''?null:(Number(raw)||0)*scale;renderScan()}};bindAuction('auctionPremiumInput','premiumRate',.01,true);bindAuction('auctionTaxInput','taxRate',.01);bindAuction('auctionTravelInput','travelCost');bindAuction('auctionRiskInput','riskBuffer');}
    H38Track.scanMatches(op,ev);
  }
  const oldRenderScan=renderScan;renderScan=function(){oldRenderScan();decorateScan()};
  const oldClearScan=clearScan;clearScan=function(){oldClearScan();state.scan.profitAdjust={};state.scan.auctionContext=null};
  const oldSetPage=setPage;setPage=function(page){if(page!=='track'){show('trackPage',false);return oldSetPage(page)}state.page='track';for(const p of ['discover','hunt','scan','auction','more'])show(`${p}Page`,false);show('trackPage',true);document.querySelectorAll('[data-page]').forEach(b=>b.classList.toggle('active',b.dataset.page==='track'));$('topSubtitle').textContent='Track';show('locationStrip',true);renderLocationStrip();window.scrollTo({top:0,behavior:'instant'});renderTrack()};
  function trackTitle(t){return t.upc?`UPC ${t.upc}`:t.sku?`SKU ${t.sku}`:t.keywords[0]||t.category||t.brand||'Tracked rule'}
  function renderTrack(){ensure220Styles();const p=$('trackPage');if(!p)return;const rows=H38Track.list(),events=H38Track.events();p.innerHTML=`<div class="page-head"><div><h1>Tracked</h1><p>Scout remembers exact items, categories and target economics so you do not have to repeat the hunt.</p></div></div><section class="card"><div class="track-filters"><label>Watch type<select id="trackType"><option value="keywords">Item / keywords</option><option value="exact">Exact UPC/SKU</option><option value="category">Category</option><option value="brand_category">Brand + category</option><option value="retailer">Retailer-specific</option></select></label><label>Item / category / UPC<input id="trackTerm" placeholder="Milwaukee M18 blower"></label><label>Max buy price<input id="trackMaxBuy" inputmode="decimal" placeholder="Optional"></label><label>Minimum profit<input id="trackMinProfit" inputmode="decimal" value="${H38Track.settings().minimumProfit}"></label><label>Minimum ROI %<input id="trackMinRoi" inputmode="decimal" value="${Math.round(H38Track.settings().minimumROI*100)}"></label><label>Radius<input id="trackRadius" inputmode="numeric" value="${state.radius}"></label></div><button id="trackAdd" class="primary wide" style="margin-top:10px">Add track</button></section><div class="result-list">${rows.length?rows.map(t=>{const matches=events.filter(e=>e.trackId===t.id).length;return `<section class="card track-card"><div class="item-top"><span class="badge ${t.enabled?'good':'warn'}">${t.enabled?'ACTIVE':'PAUSED'}</span><span class="badge">${esc(t.watchType)}</span></div><h3>${esc(trackTitle(t))}</h3><div class="meta">${t.maxBuyPrice!==null?`<span>Target buy ≤ ${money(t.maxBuyPrice)}</span>`:''}${t.minimumExpectedProfit!==null?`<span>Profit ≥ ${money(t.minimumExpectedProfit)}</span>`:''}${t.minimumROI!==null?`<span>ROI ≥ ${pct(t.minimumROI)}</span>`:''}${t.radius!==null?`<span>${t.radius} mi</span>`:''}</div><p class="small muted">Last checked: ${t.lastCheckedAt?new Date(t.lastCheckedAt).toLocaleString():'Not checked yet'} · ${matches?matches+' match event'+(matches===1?'':'s'):'NO CURRENT MATCH'}</p><div class="card-actions"><button class="mini-btn primary" data-track-search="${esc(t.id)}">View matches</button><button class="mini-btn" data-track-edit="${esc(t.id)}">Edit</button><button class="mini-btn" data-track-toggle="${esc(t.id)}">${t.enabled?'Pause':'Resume'}</button><button class="mini-btn" data-track-delete="${esc(t.id)}">Delete</button></div></section>`}).join(''):'<div class="empty"><strong>No tracked items yet</strong>Track from Scan, Hunt, Discover, Auctions, or add a rule above.</div>'}</div>`;
    $('trackAdd').onclick=()=>{const type=$('trackType').value,term=txt($('trackTerm').value);if(!term){notice('Enter an item, category, retailer, UPC or SKU.','warn');return}const base={watchType:type,radius:Number($('trackRadius').value)||state.radius,centerLocation:{...state.location},maxBuyPrice:$('trackMaxBuy').value===''?null:Number($('trackMaxBuy').value),minimumExpectedProfit:$('trackMinProfit').value===''?null:Number($('trackMinProfit').value),minimumROI:$('trackMinRoi').value===''?null:Number($('trackMinRoi').value)/100};if(type==='exact'){if(/^\d{7,}$/.test(term))base.upc=term;else base.sku=term}else if(type==='category')base.category=term;else if(type==='retailer')base.retailer=term;else if(type==='brand_category'){const parts=term.split(/\s+/,2);base.brand=parts[0];base.category=term.slice(parts[0].length).trim()}else base.keywords=[term];H38Track.upsert(base);renderTrack()};
    p.querySelectorAll('[data-track-toggle]').forEach(b=>b.onclick=()=>{H38Track.toggle(b.dataset.trackToggle);renderTrack()});p.querySelectorAll('[data-track-delete]').forEach(b=>b.onclick=()=>{H38Track.remove(b.dataset.trackDelete);renderTrack()});p.querySelectorAll('[data-track-search]').forEach(b=>b.onclick=()=>{const t=H38Track.list().find(x=>x.id===b.dataset.trackSearch);state.discover.query=t?.keywords?.[0]||t?.category||t?.brand||t?.upc||t?.sku||'';setPage('discover');runDiscover()});p.querySelectorAll('[data-track-edit]').forEach(b=>b.onclick=()=>openTrackEdit(b.dataset.trackEdit));}
  function openTrackEdit(trackId){const t=H38Track.list().find(x=>x.id===trackId);if(!t)return;detail(`<div class="sheet-head"><div><div class="eyebrow">TRACK RULE</div><h2>${esc(trackTitle(t))}</h2></div><button class="icon-btn" onclick="show('detailSheet',false)">×</button></div><label>Max buy<input id="editTrackBuy" inputmode="decimal" value="${t.maxBuyPrice??''}"></label><label>Minimum profit<input id="editTrackProfit" inputmode="decimal" value="${t.minimumExpectedProfit??''}"></label><label>Minimum ROI %<input id="editTrackRoi" inputmode="decimal" value="${t.minimumROI!==null?t.minimumROI*100:''}"></label><label>Minimum Deal Score<input id="editTrackScore" inputmode="numeric" value="${t.minimumDealScore??''}"></label><label>Radius miles<input id="editTrackRadius" inputmode="numeric" value="${t.radius??state.radius}"></label><button id="saveTrackEdit" class="primary wide">Save</button>`);$('saveTrackEdit').onclick=()=>{H38Track.upsert({...t,maxBuyPrice:$('editTrackBuy').value===''?null:Number($('editTrackBuy').value),minimumExpectedProfit:$('editTrackProfit').value===''?null:Number($('editTrackProfit').value),minimumROI:$('editTrackRoi').value===''?null:Number($('editTrackRoi').value)/100,minimumDealScore:$('editTrackScore').value===''?null:Number($('editTrackScore').value),radius:Number($('editTrackRadius').value)||state.radius});show('detailSheet',false);renderTrack()}};
  function evalDeal(r){const sold=Number(r.comp_samples||r.sold_comp_count||0),resale=Number(r.resale_estimate||0),buy=Number(r.estimated_all_in||r.buy_price||0),profit=Number(r.net_profit),combinedCosts=sold>0&&resale>0&&buy>=0&&Number.isFinite(profit)?Math.max(0,resale-buy-profit):0;return H38Profit.evaluateOpportunity({acquisitionPrice:buy>0?buy:null,soldCount:sold,soldMedian:sold>0&&resale>0?resale:null,activeCount:Number(r.active_count||0)||null,confidence:r.comp_confidence||r.confidence||'medium',freshnessDays:Number(r.comp_age_days||0)||null,marketplaceFees:combinedCosts,paymentFees:0,shipping:0,taxAllowance:0,travelCost:0,buyerPremium:0,otherAcquisitionFees:0,minimumProfit:H38Track.settings().minimumProfit,minimumROI:H38Track.settings().minimumROI,distanceMiles:Number.isFinite(Number(r.distance_miles))?Number(r.distance_miles):null});}
  function decorateDiscover(){ensure220Styles();const p=$('discoverPage');if(!p)return;const old=$('v220Feed');if(old)old.remove();const rows=[...(state.discover.deals?.opportunities||[]),...(state.discover.deals?.candidates||[])].map(r=>({r,ev:evalDeal(r),glitch:H38Profit.classifyGlitch({currentPrice:r.estimated_all_in||r.buy_price,referencePrice:r.reference_price||r.original_price||r.msrp})}));for(const x of rows)H38Track.scanMatches({...x.r,acquisitionPrice:x.ev.acquisitionPrice},x.ev);const ranked=rows.filter(x=>x.ev.dealScore!==null).sort((a,b)=>b.ev.dealScore-a.ev.dealScore),needs=rows.filter(x=>x.ev.recommendedAction.startsWith('NEEDS')).slice(0,3),hot=ranked.slice(0,3),highProfit=ranked.filter(x=>Number(x.ev.expectedProfit)>=H38Track.settings().minimumProfit).slice(0,3),highRoi=ranked.filter(x=>Number(x.ev.roi)>=H38Track.settings().minimumROI).slice(0,3),glitches=rows.filter(x=>x.glitch).slice(0,3),tracked=H38Track.events().slice(0,3);const section=document.createElement('section');section.id='v220Feed';section.className='card';const mini=(x,label)=>`<div class="feed-mini"><strong>${esc(x.r.title||'Opportunity')}</strong><div class="small muted">${esc(label)} · ${esc(x.r.source||'source')} ${x.ev.dealScore!==null?'· score '+x.ev.dealScore:''}</div></div>`;section.innerHTML=`<div class="section-head"><h2>Opportunity feed</h2><span>decision-first</span></div><div class="feed-lanes"><div class="feed-lane"><strong>HOT NEARBY</strong>${hot.map(x=>mini(x,`${x.ev.recommendedAction} · ${money(x.ev.expectedProfit)} profit`)).join('')||'<div class="small muted">No scored local opportunities yet.</div>'}</div><div class="feed-lane"><strong>TRACKED MATCHES</strong>${tracked.map(e=>`<div class="feed-mini"><strong>${esc(e.title||'Tracked item')}</strong><div class="small muted">${esc(e.recommendedAction||'MATCH')} ${e.dealScore!==null?'· score '+e.dealScore:''}</div></div>`).join('')||'<div class="small muted">No current tracked matches.</div>'}</div><div class="feed-lane"><strong>HIGH PROFIT / ROI</strong>${[...highProfit,...highRoi].filter((x,i,a)=>a.indexOf(x)===i).slice(0,3).map(x=>mini(x,`${money(x.ev.expectedProfit)} · ${pct(x.ev.roi)}`)).join('')||'<div class="small muted">No proven high-profit results yet.</div>'}</div><div class="feed-lane"><strong>NEEDS QUICK VERIFICATION</strong>${needs.map(x=>mini(x,x.ev.recommendedAction)).join('')||'<div class="small muted">Nothing waiting on a critical boundary.</div>'}</div>${glitches.length?`<div class="feed-lane"><strong>GLITCH CANDIDATES</strong>${glitches.map(x=>mini(x,`${x.glitch.dropPct}% unusual drop · verify`)).join('')}</div>`:''}</div>`;const hero=p.querySelector('.hero');if(hero)hero.insertAdjacentElement('afterend',section);else p.prepend(section);}
  const oldRenderDiscover=renderDiscover;renderDiscover=function(){oldRenderDiscover();decorateDiscover()};
  const oldAuctionCard=auctionCard;auctionCard=function(r){let html=oldAuctionCard(r),price=Number(r.current_bid)||0,prem=r.buyer_premium!==null&&r.buyer_premium!==undefined&&r.buyer_premium!==''?Number(r.buyer_premium)/100:null;return html.replace(`data-auction-price="${price}"`,`data-auction-price="${price}" data-auction-premium="${prem===null?'':prem}" data-auction-source="${esc(r.source||'Auction')}"`)};
  const oldBindAuction=bindAuction;bindAuction=function(root){oldBindAuction(root);root.querySelectorAll('[data-auction-research]').forEach(b=>b.onclick=()=>{state.scan.hint=b.dataset.auctionResearch||'';state.scan.buyPrice=Number(b.dataset.auctionPrice)>0?String(b.dataset.auctionPrice):'';state.scan.auctionContext={source:b.dataset.auctionSource||'Auction',premiumRate:b.dataset.auctionPremium===''?null:Number(b.dataset.auctionPremium),taxRate:0,travelCost:0,riskBuffer:0,otherFees:0,quantity:1};state.scan.identification=null;state.scan.market=null;setPage('scan');renderScan();notice('Auction lot loaded. Research sold comps; max bid stays unproven until required costs are known.','good')})};
  const oldOpenLeadDetail=openLeadDetail;openLeadDetail=async function(key){const r=state.hunt.rows.find(x=>itemKey(x)===key);await oldOpenLeadDetail(key);if(!r)return;const card=$('detailCard');if(!card)return;const aisle=txt(r.aisle),bay=txt(r.bay),department=txt(r.department||r.store_department),area=txt(r.clearance_section||r.store_area||r.where_to_look),specific=[aisle?`Aisle ${aisle}`:'',bay?`Bay ${bay}`:''].filter(Boolean).join(' · '),look=specific||department||area||'Store location unknown';const box=document.createElement('div');box.className='where-look';box.innerHTML=`<div class="eyebrow">WHERE TO LOOK</div><strong>${esc(look)}</strong>${specific||department||area?'<div class="small muted">Store-location guidance shown only from sourced fields.</div>':'<div class="small muted">No aisle/bay/department evidence is available; Scout will not fabricate one.</div>'}`;const actions=card.querySelector('.card-actions');if(actions)actions.before(box);else card.appendChild(box);if(isPenny(r)){const web=Number(r.retailer_web_price??r.web_price);if(Number.isFinite(web)&&web>0&&web!==.01){const truth=document.createElement('div');truth.className='truth-note';truth.innerHTML=`<strong>PENNY CANDIDATE</strong><br>Crawler/community signal: $0.01<br>Retail web: ${money(web)}<br>Local register: ${esc(r.local_register_price!==undefined&&r.local_register_price!==null?money(r.local_register_price):'NOT CHECKED')}<br><strong>Physical scan/register required.</strong>`;box.after(truth)}}};
  const oldRenderMore=renderMore;renderMore=function(){if(state.moreView==='profit-settings')return renderProfitSettings();oldRenderMore();if((state.moreView||'home')==='home'){const grid=$('morePage')?.querySelector('.more-grid');if(grid&&!grid.querySelector('[data-more="profit-settings"]')){const b=document.createElement('button');b.className='more-tile';b.dataset.more='profit-settings';b.innerHTML='<strong>Profitability Settings</strong><span>Minimum profit/ROI, marketplace fees, shipping, mileage and auction targets.</span>';grid.prepend(b);b.onclick=()=>{state.moreView='profit-settings';renderMore()}}}};
  function renderProfitSettings(){const p=$('morePage'),s=H38Track.settings();p.innerHTML=`<div class="page-head"><div><button id="moreBack" class="ghost">‹ More</button><h1>Profitability Settings</h1><p>Sane owner defaults. Scan still works without configuring this screen first.</p></div></div><section class="card"><label>Minimum desired profit<input id="setMinProfit" inputmode="decimal" value="${s.minimumProfit}"></label><label>Minimum ROI %<input id="setMinRoi" inputmode="decimal" value="${s.minimumROI*100}"></label><label>Default marketplace<select id="setMarket"><option value="local">Local / no platform fee</option><option value="ebay">eBay / enter fee below</option><option value="other">Other</option></select></label><label>Marketplace fee %<input id="setFee" inputmode="decimal" value="${s.marketplaceFeeRate*100}"></label><label>Default shipping allowance<input id="setShip" inputmode="decimal" value="${s.defaultShipping}"></label><label>Mileage cost / mile<input id="setMileage" inputmode="decimal" value="${s.mileageRate}"></label><label>Auction target profit<input id="setAuctionProfit" inputmode="decimal" value="${s.auctionTargetProfit}"></label><label>Auction minimum ROI %<input id="setAuctionRoi" inputmode="decimal" value="${s.auctionMinimumROI*100}"></label><button id="saveProfitSettings" class="primary wide">Save defaults</button></section>`;$('setMarket').value=s.defaultMarketplace;$('moreBack').onclick=()=>{state.moreView='home';renderMore()};$('saveProfitSettings').onclick=()=>{H38Track.saveSettings({minimumProfit:Number($('setMinProfit').value)||0,minimumROI:(Number($('setMinRoi').value)||0)/100,defaultMarketplace:$('setMarket').value,marketplaceFeeRate:(Number($('setFee').value)||0)/100,defaultShipping:Number($('setShip').value)||0,mileageRate:Number($('setMileage').value)||0,auctionTargetProfit:Number($('setAuctionProfit').value)||0,auctionMinimumROI:(Number($('setAuctionRoi').value)||0)/100});notice('Profitability defaults saved.','good');renderProfitSettings()}};
  const openMore=$('openMore');if(openMore)openMore.onclick=()=>{show('accountSheet',false);state.moreView='home';setPage('more')};
  ensure220Styles();
})();
