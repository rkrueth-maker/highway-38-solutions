name: Retired Google-Native Office Beta — Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired. Supabase is the only supported Business Office runtime.'
          exit 1

# Historical non-executable markers:
# clasp push
# deploy-commercial-google-native-beta.sh
# h38-office-gateway
# verify-h38-office-gateway-source.js
# verify-commercial-startup.js
# run-commercial-webapp-startup-acceptance.sh
# verify-commercial-delivery-acceptance.js
# verify-commercial-full-demo-acceptance.js
# release-report.json
