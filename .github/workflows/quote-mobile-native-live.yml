name: Retired Apps Script Quote Mobile Acceptance — Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired. Quote Builder acceptance now belongs to the Supabase Business Office.'
          exit 1

# Historical non-executable markers:
# run-quote-mobile-live-acceptance-authorized.sh
# Deploy Unified Owner Portal
