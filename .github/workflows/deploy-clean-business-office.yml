name: Retired Standalone Google Office Deployment — Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired. New client tenants use the shared Supabase Business Office.'
          exit 1

# Historical non-executable markers:
# deploy-business-office-clean-acceptance.sh
# clasp
