name: Retired Owner Portal Runtime Acceptance — Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired. Supabase is the only supported Business Office runtime.'
          exit 1

# Historical non-executable markers:
# script.googleapis.com
# clasp list-deployments
