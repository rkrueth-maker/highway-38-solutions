name: Archived H38 Scout v2.5.2 Phone Location Repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow notice
        run: |
          echo 'This v2.5.2 version-pinned workflow is archived and no longer runs on Scout pushes.'
          echo 'Use Build Private Reseller Scout, the current version gate, and Firebase acceptance workflows for active Scout validation.'
