name: Archived H38 Scout v3.0.7 Typed Facebook Search Repair

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  archived:
    runs-on: ubuntu-latest
    steps:
      - name: Historical workflow notice
        run: |
          echo 'This v3.0.7 version-pinned workflow is archived and no longer runs on Scout pushes.'
          echo 'Use Build Private Reseller Scout, the current version gate, and Firebase acceptance workflows for active Scout validation.'
