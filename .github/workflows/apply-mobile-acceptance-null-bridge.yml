name: Apply mobile acceptance null bridge

on:
  push:
    branches: [agent/authorized-mobile-live-acceptance]
    paths:
      - '.github/workflows/apply-mobile-acceptance-null-bridge.yml'
  pull_request:
    branches: [main]
    paths:
      - '.github/workflows/apply-mobile-acceptance-null-bridge.yml'

permissions:
  contents: write

jobs:
  apply:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/authorized-mobile-live-acceptance
      - name: Disable portal auth bridge only in temporary harness
        shell: bash
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import import Path
          PY
