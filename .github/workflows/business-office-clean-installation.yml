name: Retired Apps Script Clean Installation — Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired. Client onboarding uses the Supabase tenant installer.'
          exit 1

# Historical non-executable markers:
# deploy-clean-business-office-installation-v8.sh
# clasp
