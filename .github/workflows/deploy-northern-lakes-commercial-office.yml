name: Retired Northern Lakes Google Office Deployment — Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired. Northern Lakes uses the shared Supabase Business Office only.'
          exit 1

# Historical verification markers only. They are not executable:
# clasp push --force
# EXPECTED_DEPLOYMENT_ID
# nlpm-office-gateway
