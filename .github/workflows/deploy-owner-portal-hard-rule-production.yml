name: Retired Google Office Deployment — Disabled

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    if: ${{ false }}
    runs-on: ubuntu-latest
    steps:
      - run: |
          echo 'Retired. Supabase is the only supported Business Office runtime.'
          exit 1

# Historical verification markers only. They are not executable:
# name: Deploy Unified Owner Portal
# node scripts/verify-unified-app-architecture.js
# verify-ai-approval-and-deployment.js
# deploy-unified-owner-portal-web.sh
