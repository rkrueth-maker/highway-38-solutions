#!/usr/bin/env node
'use strict';
const fs=require('fs');
const path=require('path');
const root=path.resolve(__dirname,'..');
const read=file=>fs.readFileSync(path.join(root,file),'utf8');
const json=file=>JSON.parse(read(file));
const failures=[];const checks=[];
function check(name,condition,detail=''){checks.push({name,status:condition?'PASS':'FAIL'});if(!condition)failures.push(detail?`${name}: ${detail}`:name);}
const packageConfig=json('businesses/northern-lakes/commercial-office-package.json');
const runtime=json('businesses/northern-lakes/app-deployment.json');
const launcher=read('businesses/northern-lakes/business-office.html');
const launchJs=read('businesses/northern-lakes/app-launch.js');
const pwa=read('businesses/northern-lakes/commercial-app/index.html');
const db=read('businesses/northern-lakes/commercial-app/db.js');
const bridge=read('businesses/northern-lakes/commercial-app/bridge.js');
const startup=read('businesses/northern-lakes/commercial-app/startup-fix.js');
const brand=read('businesses/northern-lakes/commercial-app/brand-patch.js');
const manifest=json('businesses/northern-lakes/commercial-app/manifest.webmanifest');
const gateway=read('supabase/functions/nlpm-office-gateway/index.ts');
const supabase=read('supabase/config.toml');
const builder=read('scripts/build-northern-lakes-commercial-office.js');
const portal=read('portal.html');
const signIn=read('sign-in.html');
check('package is prepared but not auto-deployed',packageConfig.status==='prepared-not-deployed'&&packageConfig.safety.deploymentRequiresExplicitPermission===true);
check('existing NL Apps Script target is locked',packageConfig.existingAppsScriptProjectId===runtime.scriptId&&packageConfig.existingAppsScriptDeploymentId===runtime.deploymentId);
check('new Apps Script project or deployment is forbidden',packageConfig.safety.createAppsScriptProjectAllowed===false&&packageConfig.safety.createAppsScriptDeploymentAllowed===false);
check('external actions remain disabled',packageConfig.safety.externalActionsEnabled===false&&runtime.externalActionsEnabled===false);
check('legacy NL unified Office stays disabled until verified package activation',launchJs.includes("config.coreEngine==='commercial-office'")&&launchJs.includes("var PACKAGE='nlpm-commercial-office-trial-v1'")&&launcher.includes('retired unified Office is no longer opened'));
check('NL PWA uses approved branding',pwa.includes('Northern Lakes Business Office')&&pwa.includes('diamond-logo.png')&&brand.includes('Northern Lakes Property Maintenance LLC'));
check('NL PWA has isolated browser database',db.includes("DB_NAME='nlpm-commercial-offline'")&&!db.includes("DB_NAME='h38-commercial-offline'"));
check('NL PWA has isolated opaque session',bridge.includes("SESSION_KEY='nlpm-gateway-session-v1'")&&!bridge.includes("SESSION_KEY='h38-gateway-session-v1'"));
check('NL startup uses local launcher and storage',startup.includes('/businesses/northern-lakes/business-office.html')&&startup.includes("NLPM_SELECTED_BUSINESS_KEY='nlpm-selected-business'"));
check('NL manifest is installable and branded',manifest.name==='Northern Lakes Commercial Office'&&manifest.start_url==='./'&&manifest.icons.length>=2);
check('NL gateway locks exact existing script and deployment',gateway.includes(runtime.scriptId)&&gateway.includes(runtime.deploymentId)&&gateway.includes('nlpm-office-gateway'));
check('NL gateway never exposes Google token',gateway.includes('browserReceivesGoogleToken: false')&&!gateway.includes('browserReceivesGoogleToken: true'));
check('NL gateway has no wildcard CORS',!gateway.includes('access-control-allow-origin": "*"')&&!gateway.includes("'Access-Control-Allow-Origin':'*'"));
check('Supabase package declares dedicated function',supabase.includes('[functions.nlpm-office-gateway]')&&supabase.includes('verify_jwt = false'));
check('builder rewrites Commercial Office tenant values',builder.includes('northern-lakes-commercial-office-trial')&&builder.includes('nlpm-office-gateway')&&builder.includes('northernlakesproperty@gmail.com'));
check('H38 portal no longer contains obsolete deployment',portal.includes('open-business-office.html')&&!portal.includes('AKfycbzr0hoImRF4iQ1gR90Cr17juP8PODkEWRorXxW6qralEYTGLhOU33E1wYEPU_3duQKpQg'));
check('H38 sign-in uses new secure launcher',signIn.includes('href="open-business-office.html"')&&!signIn.includes('portal.html#business-office'));
console.log(JSON.stringify({status:failures.length?'FAIL':'PASS',packageVersion:packageConfig.packageVersion,existingNlProjectPreserved:true,existingNlDeploymentUpdatedInPlaceOnly:true,activationPermissionRequired:true,checks,failures},null,2));
if(failures.length)process.exit(1);
