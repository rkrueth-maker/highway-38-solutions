#!/usr/bin/env node
'use strict';
const fs=require('fs');
const read=p=>fs.readFileSync(p,'utf8');
const voice=read('commercial-app/field-visit-transcription.js');
const capture=read('commercial-app/field-visit-voice-capture.js');
const edge=read('supabase/functions/h38-walkthrough-transcription/index.ts');
const cameraFix=read('commercial-app/android-camera-direct-fix.js');
const operator=read('commercial-app/operator-direct-controls.js');
const nativeGuard=read('commercial-app/android-native-walkthrough-guard.js');
const guided=read('commercial-app/field-visit-guided-controller.js');
const recovery=read('commercial-app/field-visit-recovery.js');
const auth=read('commercial-app/auth-autofill.js');
const android=read('native/h38-site-scanner/android-app/app/src/main/java/com/highway38/sitescanner/MainActivity.java');
const nativeCapture=read('native/h38-site-scanner/android-app/app/src/main/java/com/highway38/sitescanner/WalkthroughCaptureActivity.java');
const nativeBridge=read('native/h38-site-scanner/android-app/app/src/main/java/com/highway38/sitescanner/NativeScannerBridge.java');
const nativeAudio=read('native/h38-site-scanner/android-app/app/src/main/java/com/highway38/sitescanner/WalkthroughAudioExtractor.java');
const gradle=read('native/h38-site-scanner/android-app/app/build.gradle');
const compact=s=>s.replace(/\s+/g,'');
const compactEdge=compact(edge);
const failures=[];
const check=(name,ok)=>{if(!ok)failures.push(name);};
check('voice client exposes status for captured walkthrough',voice.includes('fieldVoiceStatus')&&voice.includes('Notes from walkthrough video'));
check('no-audio walkthrough is explicitly rejected',voice.includes("status='NO_AUDIO'")&&voice.includes('not accepted because microphone audio is required'));
check('transcription requests private derived audio when available',voice.includes('latestAudioAttachment')&&voice.includes("audioAttachmentId||''"));
check('same saved video produces private voice evidence without opening another microphone',capture.includes('derivedSameVideoAudio:true')&&capture.includes('nativeSameVideoAudio:true')&&capture.includes('Walkthrough Voice Audio')&&!capture.includes('navigator.mediaDevices')&&!capture.includes('new MediaRecorder'));
check('retired recorder preserves same-video contract',capture.includes('retiredRecorder:true')&&capture.includes('sameWalkthroughSpeech:true')&&capture.includes('microphoneRequired:true')&&capture.includes('videoOnlyFallback:false'));
check('native audio is copied from the exact CameraX MP4',nativeAudio.includes('MediaExtractor')&&nativeAudio.includes('MediaMuxer')&&nativeAudio.includes('mime.startsWith("audio/")')&&!nativeAudio.includes('AudioRecord')&&!nativeAudio.includes('MediaRecorder'));
check('native bridge exposes private audio recovery',nativeBridge.includes('getRecoveredWalkthroughAudioInfo')&&nativeBridge.includes('readRecoveredWalkthroughAudioChunk')&&nativeBridge.includes('confirmRecoveredWalkthroughAudioConsumed'));
check('CameraX completion requires native same-video audio',nativeCapture.includes('WalkthroughAudioExtractor.prepare(this,outputFile)'));
check('voice upload refreshes auth',capture.includes('refreshAuthBeforeAudioSync:true'));
check('Site Visit sync refreshes auth before queue processing',voice.includes('refreshAuthBeforeSiteVisitSync:true'));
check('edge preserves original video authority',edge.includes('Video Walkthrough')&&compactEdge.includes('attachmentId:videoId'));
check('edge uses derived audio when provided and original video otherwise',compactEdge.includes('letsource=video;if(audioId)source=awaitrequireEvidence'));
check('edge does not reject a large original video before selecting derived audio',compactEdge.includes('if(expectedType!=="VideoWalkthrough"&&fileSize>MAX_BYTES)')&&compactEdge.includes('if(blob.size>MAX_BYTES)'));
check('edge returns explicit no usable audio code',edge.includes('NO_USABLE_AUDIO'));
check('edge keeps unverified speech unverified but preserves explicit operator verification',edge.includes('UNVERIFIED_SPOKEN')&&edge.includes('OPERATOR_VERIFIED')&&edge.includes('normalizeSpokenMeasurements')&&edge.includes('isOperatorVerified')&&edge.includes('isMaterialSpecification')&&edge.includes('Do not put product or material specifications into spokenMeasurements'));
check('edge is Site Visit first and quote optional',compactEdge.includes('siteVisitFirst:true')&&compactEdge.includes('quoteOptional:true'));
check('walkthrough notes become editable saved Site Visit content',voice.includes('editableSavedNotes:true')&&voice.includes('fieldSaveWalkthroughNotes')&&voice.includes('walkthroughEditableNotes'));
check('spoken dimensions feed measurement candidates without fake depth',voice.includes('walkthroughMeasurementCandidates')&&voice.includes('plainVideoDepthGeometry:false'));
check('old Android WebView camera authority is retired',cameraFix.includes('retired:true')&&cameraFix.includes('cameraAuthority:false')&&!cameraFix.includes("addEventListener('click'"));
check('operator does not hijack Android walkthrough',operator.includes("if(!apple()||android())return false")&&operator.includes("androidWalkthroughAuthority:'android-native-walkthrough-guard'"));
check('iPhone uses native video input',operator.includes("iphoneWalkthroughAuthority:'native-video-input'")&&operator.includes("document.getElementById('fieldVideoInput')"));
check('android guard is the native walkthrough authority',nativeGuard.includes('privateCameraX:true')&&nativeGuard.includes('noWebRTCWalkthrough:true'));
check('native recovered video is streamed from private storage',nativeGuard.includes('readNativeFile')&&nativeGuard.includes('nativeStreamingRecovery:true')&&nativeGuard.includes('nativeChunkRecovery:false'));
const acceptStart=nativeGuard.indexOf('async function acceptFile');
const acceptEnd=nativeGuard.indexOf('async function recover',acceptStart);
const acceptPath=acceptStart>=0&&acceptEnd>acceptStart?nativeGuard.slice(acceptStart,acceptEnd):'';
check('native video persists before frame extraction',acceptPath.indexOf('await persistVideoFirst(file)')>=0&&acceptPath.indexOf('await persistVideoFirst(file)')<acceptPath.indexOf('await bestEffortFrames(file)'));
check('native recording is consumed only after attachment count advances',acceptPath.includes('walkthroughCount()<=before')&&acceptPath.includes('confirmConsumed()'));
check('post-capture queue drains automatically',recovery.includes('automaticPostCaptureSync:true')&&recovery.includes('automaticWalkthroughProcessing:true'));
check('actual notes are surfaced by authoritative guided review',guided.includes('visibleSpokenNotes:true')&&guided.includes('What you said'));
check('visible hammer swing is enforced by authoritative guided review',guided.includes('inlineWorkingHammer:true')&&guided.includes('@keyframes h38GuidedHammer'));
check('measurement verification precedes photo guidance when needed',guided.includes("if(measureList.length)return{kind:'measure'")&&guided.includes("next.kind==='measure'?'MEASUREMENT':'PHOTO'"));
check('saved AR measurement advances only after ingest',guided.includes('await C.ingest(result)')&&guided.includes('await completeMeasurementRequest(request,saved)')&&guided.includes('walkthroughCompletedMeasurements'));
check('guided review does not start a measurement/review feedback loop',guided.includes('automaticMeasurementReanalysis:false')&&guided.includes('mutationObserver:false'));
check('android host opens H38 CameraX walkthrough activity',android.includes('WalkthroughCaptureActivity.class')&&android.includes('pendingFileCapture'));
check('CameraX records microphone in same video',nativeCapture.includes('withAudioEnabled()')&&nativeCapture.includes('VideoCapture.withOutput'));
check('CameraX auto-starts recording after camera bind',nativeCapture.includes('handler.postDelayed(this::startRecording,120)'));
check('CameraX has live rear preview',nativeCapture.includes('PreviewView')&&nativeCapture.includes('CameraSelector.DEFAULT_BACK_CAMERA'));
check('CameraX has torch control',nativeCapture.includes('Light On')&&nativeCapture.includes('Light Off')&&nativeCapture.includes('enableTorch'));
check('walkthrough remains in private H38 storage',nativeCapture.includes('getFilesDir()')&&nativeCapture.includes('FileProvider.getUriForFile'));
check('external Android camera intent remains retired',!android.includes('MediaStore.ACTION_VIDEO_CAPTURE')&&!android.includes('MediaStore.EXTRA_OUTPUT'));
check('native return survives activity recreation',android.includes('persistCaptureTracking')&&android.includes('restoreCaptureTracking')&&android.includes('recoveredCaptureUri'));
check('saved Android login still uses Credential Manager',nativeBridge.includes('CredentialManager')&&nativeBridge.includes('GetPasswordOption')&&nativeBridge.includes('PasswordCredential')&&nativeBridge.includes('fillWebLogin'));
check('saved owner login fills automatically but never auto-submits',auth.includes('automaticSavedOwnerFill:true')&&auth.includes('h38:saved-login-filled')&&!nativeBridge.includes("document.getElementById('h38AuthForm').submit"));
check('Android source is current v0.5.28 candidate',gradle.includes('versionCode 33')&&gradle.includes("versionName '0.5.28'"));
if(failures.length){console.error(JSON.stringify({status:'FAIL',failures},null,2));process.exit(1);}
console.log(JSON.stringify({status:'PASS',android:'CameraX auto-start only',iphone:'native video input',sameVideoAudioDerivative:true,nativeSameVideoAudio:true,nativeStreamingRecovery:true,automaticPostCaptureSync:true,automaticWalkthroughProcessing:true,automaticSavedOwnerFill:true,editableSavedNotes:true,operatorVerifiedMeasurements:true,materialSpecificationsExcludedFromMeasurements:true,measurementVerificationFirst:true,oneGuidedController:true,walkthroughArAdvanceLinked:true,selectedTranscriptionSourceSizeGate:true,currentCandidateSource:'0.5.28',acceptedOwnerApkBaseline:'0.5.27',physicalPhoneAcceptanceExternal:true},null,2));