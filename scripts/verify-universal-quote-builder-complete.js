#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path'),root=path.resolve(__dirname,'..');
const read=rel=>fs.readFileSync(path.join(root,rel),'utf8'),exists=rel=>fs.existsSync(path.join(root,rel));
const passes=[],failures=[];
const check=(name,condition,evidence='')=>{(condition?passes:failures).push({name,evidence});console.log(`${condition?'PASS':'FAIL'}: ${name}${evidence?` — ${evidence}`:''}`);};
const files={catalog:'apps-script/business-office/BusinessOffice_UniversalQuoteBuilder.gs',records:'apps-script/business-office/BusinessOffice_UniversalQuoteBuilder_Records.gs',controls:'apps-script/business-office/BusinessOffice_UniversalQuoteBuilder_Controls.gs',client:'apps-script/business-office/BusinessOffice_UniversalQuoteBuilder_Workspace_Client.html',review:'apps-script/business-office/BusinessOffice_UniversalQuoteBuilder_Control_Client.html',index:'apps-script/business-office/BusinessOffice_QuoteBuilder_Index.html'};
Object.values(files).forEach(file=>check(`required file ${file}`,exists(file)));
if(failures.length)process.exit(1);
const catalog=read(files.catalog),records=read(files.records),controls=read(files.controls),client=read(files.client),review=read(files.review),index=read(files.index),server=[catalog,records,controls].join('\n');
try{new Function(server);check('Apps Script source parses',true);}catch(error){check('Apps Script source parses',false,error.message);}
for(const [label,html] of [['workspace',client],['Owner review',review]]){const js=[...html.matchAll(/<script>([\s\S]*?)<\/script>/g)].map(m=>m[1]).join('\n');try{new Function(js);check(`${label} client source parses`,true);}catch(error){check(`${label} client source parses`,false,error.message);}}
const levelBlock=(catalog.match(/LEVELS:Object\.freeze\(\[([\s\S]*?)\]\),\n  PRICING_METHODS/)||[])[1]||'';
const levels=(levelBlock.match(/level:\d/g)||[]).length;
const pricingBlock=(catalog.match(/PRICING_METHODS:Object\.freeze\(\[([\s\S]*?)\]\)/)||[])[1]||'';
const pricingMethods=(pricingBlock.match(/'[^']+'/g)||[]).length;
const agentBlock=(catalog.match(/AGENTS:Object\.freeze\(\[([\s\S]*?)\]\)/)||[])[1]||'';
const agents=(agentBlock.match(/Object\.freeze\(\{key:/g)||[]).length;
const exampleBlock=(catalog.match(/function boUniversalQuoteExamples_\(\)\{[\s\S]*?return \[([\s\S]*?)\];\n\}/)||[])[1]||'';
const examples=(exampleBlock.match(/\{key:/g)||[]).length;
check('five progressive quote levels',levels===5,String(levels));
check('34 deterministic pricing methods',pricingMethods===34,String(pricingMethods));
check('eight controlled universal agents',agents===8,String(agents));
check('18 cross-industry scenarios',examples===18,String(examples));
check('eight drawing classifications',catalog.includes("'Conceptual','Estimating','Subcontractor bidding','Field layout','Construction-ready','Permit submission','Engineer or licensed-professional review required','Approved final'"));
const storeBlock=(records.match(/SHEETS:Object\.freeze\(\{([\s\S]*?)\n  \}\)\n\}\);/)||[])[1]||'',stores=(storeBlock.match(/Object\.freeze\(\{name:'UQB /g)||[]).length;
check('persistent structured storage has 14 record types',stores===14,String(stores));
[['projects','UQB Projects'],['project revisions','UQB Project Revisions'],['sub-quotes','UQB Subquotes'],['quote items','UQB Quote Items'],['measurements','UQB Measurements'],['calculations','UQB Calculations'],['scope sections','UQB Scope Sections'],['drawings','UQB Drawings'],['drawing revisions','UQB Drawing Revisions'],['bid packages','UQB Bid Packages'],['subcontractor bids','UQB Subcontractor Bids'],['knowledge packs','UQB Knowledge Packs'],['agent runs','UQB Agent Runs'],['estimate actuals','UQB Estimate Actuals']].forEach(([label,marker])=>check(`structured ${label}`,records.includes(marker)));
const recordFns=['boUniversalEnsureStore','boUniversalListProjects','boUniversalCreateProject','boUniversalUpdateProject','boUniversalReviseProject','boUniversalGetProject','boUniversalCreateSubquote','boUniversalUpdateSubquote','boUniversalAddQuoteItem','boUniversalAddMeasurement','boUniversalAddScopeSection','boUniversalCreateDrawing','boUniversalReviseDrawing','boUniversalCreateBidPackage','boUniversalRecordBid','boUniversalCompareBids','boUniversalSelectBid','boUniversalCreateKnowledgePack','boUniversalActivateKnowledgePack','boUniversalRunAgent','boUniversalReviewAgentRun','boUniversalRecordActual','boUniversalMaterializeQuote','boUniversalPrepareExecution','boUniversalGenerateDocument','boUniversalPrepareExampleSuite','boUniversalPrepareHouseDemonstration','boUniversalAcceptance'];
recordFns.forEach(name=>check(`server function ${name}`,new RegExp(`function ${name}\\(`).test(records)));
const controlFns=['boUniversalListKnowledgePacks','boUniversalApproveRecord','boUniversalCompleteConsistencyReview','boUniversalReviewProject','boUniversalGenerateCompletionPackage','boUniversalPrepareChangeOrderRecord'];
controlFns.forEach(name=>check(`control function ${name}`,new RegExp(`function ${name}\\(`).test(controls)));
const names=[...server.matchAll(/function\s+(boUniversal[A-Za-z0-9_]+)\s*\(/g)].map(m=>m[1]),duplicates=[...new Set(names.filter((name,index)=>names.indexOf(name)!==index))];
check('no duplicate Universal Quote server functions',duplicates.length===0,duplicates.join(', '));
check('demo-only preview is isolated',catalog.includes('boUniversalPrepareHouseDemonstrationPreview_')&&!catalog.includes('function boUniversalPrepareHouseDemonstration(runKey)'));
const docs=['Customer Proposal','Individual Subquote','Combined Proposal Package','Subcontractor Bid Package','Drawing Package','Internal Estimate','Material Takeoff','Labor Estimate','Work Instructions','Work Order','Change Order','Revision Comparison'];
docs.forEach(type=>check(`document output ${type}`,records.includes(`'${type}'`)&&client.includes(type)));
check('customer proposal excludes internal cost',records.includes("if(documentType==='Internal Estimate'||documentType==='Material Takeoff'||documentType==='Labor Estimate')")&&records.includes("if(documentType==='Customer Proposal'||documentType==='Combined Proposal Package'||documentType==='Individual Subquote')"));
check('documents are private and unsent',records.includes('sent:false,delivered:false,externalActionsPerformed:false'));
check('combined completion package is supported',controls.includes("['Customer Proposal','Drawing Package','Internal Estimate','Material Takeoff','Labor Estimate','Work Instructions','Revision Comparison']"));
check('drawing changes trigger consistency review',records.includes("'Consistency Status':'Review Required'")&&records.includes("'Quantity Review Required'")&&records.includes("'Scope Review Required'"));
check('regulated drawing truth boundary is explicit',records.includes('conceptual or estimating information is not a permit, stamped engineering document, code approval, or professional certification'));
check('regulated drawing approval requires professional review',controls.includes('requires documented licensed-professional review before approval'));
check('AI cannot independently execute external actions',!/MailApp|GmailApp|sendEmail|UrlFetchApp/.test(records+controls));
check('bid packages cannot send without approval',records.includes("'Send Allowed':'No'")&&controls.includes("patch['Send Allowed']=decision==='Approved'?'Yes':'No'"));
check('projects and sub-quotes have independent approvals',controls.includes("var map={Project:'PROJECTS',Subquote:'SUBQUOTES',Drawing:'DRAWINGS','Bid Package':'BID_PACKAGES'}"));
check('approval history uses canonical records',controls.includes('H38_BO_SHEETS.APPROVALS')&&controls.includes('boUniversalRecordApproval_'));
check('canonical quote connection',records.includes('boCreateQuoteFast_')&&records.includes("'Master Quote ID'")&&records.includes("'Existing Quote ID'"));
check('work and purchase preparation connection',records.includes('H38_BO_SHEETS.WORK_ORDERS')&&records.includes('H38_BO_SHEETS.PURCHASE_ORDERS')&&records.includes('H38_BO_SHEETS.PO_LINES'));
check('change orders use canonical records',controls.includes('H38_BO_SHEETS.CHANGE_ORDERS'));
check('Proof Log evidence is written throughout',(server.match(/boProof_\(/g)||[]).length>=25,String((server.match(/boProof_\(/g)||[]).length));
check('audit history is preserved',records.includes("boAudit_('CREATE','Universal Quote Project'")&&records.includes("boAudit_('UPDATE','Universal Quote Project'"));
check('quote review covers required areas',['Completeness','Pricing','Quantity','Drawings','Quality','Professional review'].every(marker=>controls.includes(`area:'${marker}'`)));
const tabs=['overview','subquotes','pricing','measurements','scope','drawings','bids','setup','exports','actuals'];tabs.forEach(tab=>check(`workspace tab ${tab}`,client.includes(`'${tab}'`)));
const actions=['new','addSubquote','addItem','addMeasurement','addScope','addDrawing','reviseDrawing','addBidPackage','recordBid','compareBids','addKnowledge','runAgent','export','addActual','materialize','execution','examples'];actions.forEach(action=>check(`workspace action ${action}`,client.includes(`name==='${action}'`)||client.includes(`data-uqb="${action}"`)));
check('progressive disclosure uses project tabs',client.includes('uqb-tabs')&&client.includes('uqb-pane'));
check('responsive operational workspace',client.includes('@media(max-width:620px)')&&client.includes('@media(max-width:960px)'));
check('accessible operational workspace',client.includes('aria-labelledby="uqbDialogTitle"')&&client.includes('aria-label="Project sections"'));
controlFns.forEach(name=>check(`Owner review UI calls ${name}`,review.includes(name)));
check('Owner review covers all independently approved records',['Project|','Subquote','Drawing','Bid Package'].every(marker=>review.includes(marker)));
check('Owner review workspace is responsive',review.includes('@media(max-width:850px)'));
check('operational workspace load order',index.indexOf('BusinessOffice_UniversalQuoteBuilder_Client')<index.indexOf('BusinessOffice_UniversalQuoteBuilder_Workspace_Client'));
check('Owner review workspace load order',index.indexOf('BusinessOffice_UniversalQuoteBuilder_Workspace_Client')<index.indexOf('BusinessOffice_UniversalQuoteBuilder_Control_Client'));
check('owner-labeled example runs',records.includes('boUniversalPrepareExampleSuite(runKey)')&&records.includes("'Run Key'")&&records.includes('boUniversalRunKey_(runKey)'));
check('house creates structured records',records.includes('house.subquotes.forEach')&&records.includes('house.drawings.forEach')&&records.includes('house.bidPackages.forEach'));
check('fixed Demo 08 is not regenerated',!records.includes('boGenerateAllCabinSubquotes')&&!records.includes('cabin_seed'));
check('no new app database or automatic send',!/(SupabaseClient|createClient\(|Firebase|sendEmail|GmailApp|MailApp)/.test(records+controls+client+review));
const result={status:failures.length?'HOLD':'PASS',generatedAt:new Date().toISOString(),architecture:'one-business-office-one-shared-quote-builder',quoteLevels:levels,pricingMethods,agents,examples,structuredRecordTypes:stores,documentTypes:docs.length,workspaceTabs:tabs.length,ownerReviewWorkspace:true,externalActionsPerformed:false,passed:passes.length,failed:failures.length,passes,failures};
const out=path.join(root,'artifacts','universal-quote-builder-complete');fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'verification.json'),JSON.stringify(result,null,2)+'\n');console.log(JSON.stringify(result,null,2));process.exit(failures.length?1:0);
