#!/usr/bin/env node
'use strict';
const fs=require('fs');
const path=require('path');
const vm=require('vm');
const root=path.resolve(__dirname,'..');
const failures=[],passes=[];
const read=rel=>fs.readFileSync(path.join(root,rel),'utf8');
const exists=rel=>fs.existsSync(path.join(root,rel));
const check=(name,condition,detail='')=>{(condition?passes:failures).push({name,detail});console.log(`${condition?'PASS':'FAIL'}: ${name}${detail?` — ${detail}`:''}`);};
const required=['index.html','sample-library-now.html','universal-quote-builder.html','universal-quote-builder-example.html','assets/js/uqb-public-examples.js','solutions.html','pricing.html','pricing-data.js','quote-builder.html','business-systems.html','about.html','contact.html','start-request.html','portal.html','cabin-project-complete.html','contractor-quote-complete.html','assets/js/h38-site-v2.js','assets/css/h38-site-v2.css','scripts/config/public-website-routes.json','scripts/config/approved-public-assets.json','scripts/config/approved-public-image-placements.json','catalog-data.js','apps-script/commercial-intake/FormBuilder.gs','apps-script/business-office/BusinessOffice_UniversalQuoteBuilder_PublicDemo.gs','apps-script/business-office/BusinessOffice_UniversalQuoteBuilder_PublicExamples.gs','apps-script/business-office/ZZZ_BusinessOffice_UniversalQuoteBuilder_PublicRoute.gs'];
required.forEach(file=>check(`required ${file}`,exists(file)));
const routeManifest=JSON.parse(read('scripts/config/public-website-routes.json'));
const primary=(routeManifest.primary||[]).map(item=>item.path);
const expectedPrimary=['index.html','sample-library-now.html','solutions.html','pricing.html','about.html','contact.html','start-request.html','portal.html'];
check('project-first route manifest owns the eight current gateways',expectedPrimary.every(routePath=>primary.includes(routePath))&&primary.length===8,JSON.stringify(primary));
check('Universal Quote Builder compatibility route is public and canonical',routeManifest.demonstrations.some(item=>item.path==='universal-quote-builder.html'&&item.visibility==='public'));
check('retired catalog routes point to current project-first pages',routeManifest.retired&&routeManifest.retired['products.html']==='pricing.html'&&routeManifest.retired['catalog.html']==='pricing.html'&&routeManifest.retired['packages.html']==='pricing.html'&&routeManifest.retired['tools.html']==='sample-library-now.html');
const shell=read('assets/js/h38-site-v2.js');
check('one canonical public shell owns current navigation',['Quote Builder','What We Do','Pricing','About','Contact','Owner Access'].every(label=>shell.includes(label))&&!/label:'Project Examples'/.test(shell)&&shell.includes('class="pi-menu"'));
check('canonical shell preserves Owner gateway',shell.includes("['Owner Access','portal.html']"));
check('canonical shell locks image source changes',/imagePolicy:\{changeSource:false,insertImages:false,fallbackImages:false/.test(shell));
const home=read('index.html');
check('homepage is project first',home.includes('Bring us the problem.')&&home.includes('complete project plan.')&&home.includes('See it. Scope it. Run it.'));
check('homepage routes to request and complete examples',home.includes('href="start-request.html"')&&home.includes('href="sample-library-now.html"'));
check('homepage does not restore retired catalog as primary experience',!home.includes('Choose Your Path')&&!home.includes('15 fixed-price services')&&!home.includes('9 approved bundles'));
const solutions=read('solutions.html');
const capabilities=['Automation & Robotics','CNC Machining & Process Planning','CNC Fixturing & Workholding','AI-Assisted Quote Builder','Highway 38 Business Office'];
check('What We Do exposes five accepted capabilities',capabilities.every(value=>solutions.includes(value))&&(solutions.match(/data-capability=/g)||[]).length===5);
check('What We Do uses specialist links',['robotics-automation.html','manufacturing-cnc.html','quote-builder.html','business-systems.html'].every(link=>solutions.includes(link)));
check('What We Do does not restore fixed-price product cards',!solutions.includes('Problem Snapshot')&&!solutions.includes('Basic Layout Snapshot')&&!solutions.includes('Workflow Opportunity Snapshot'));
const pricing=read('pricing.html');
check('pricing page has final three-product structure',(pricing.match(/class="price-card(?:\s|"| popular)/g)||[]).length===3&&pricing.includes('$59')&&pricing.includes('$249')&&pricing.includes('Starting at $499'));
check('pricing preserves Snapshot and approval boundaries',pricing.includes('$299 one-time')&&pricing.includes('Nothing is activated or charged from the request form.'));
check('pricing marks Business Office Most Popular',pricing.includes('Most Popular')&&pricing.includes('Implementation: $2,500'));
const samples=read('sample-library-now.html');
const existingExampleCount=(samples.match(/class="project-card"/g)||[]).length;
const houseIndex=samples.indexOf('data-project="cabin"');
const uqbIndex=samples.indexOf('id="universal-quote-builder-examples"');
check('Project Examples preserves existing examples and supports future additions',existingExampleCount>=8&&samples.includes('Open-ended example library'),String(existingExampleCount));
check('Project Examples places Universal Quote Builder beneath the house',houseIndex>=0&&uqbIndex>houseIndex);
check('Project Examples removes separate Universal result board',!samples.includes('class="universal-card"')&&!samples.includes('See What It Produced'));
check('Project Examples removes fixed example-count wording',!/Eight complete|Explore the Eight|current eight-project/i.test(samples));
check('Project Examples preserves representative disclosure',samples.includes('Representative demonstrations.')&&samples.includes('data-image-classification="hypothetical-demonstration"'));
const exactExampleImages=['deck-existing-condition.webp','deck-finished-concept.webp','irrigation-before-clean.webp','irrigation-after-clean.webp','kitchen-existing-condition.webp','kitchen-remodel-concept.webp'];
check('deck irrigation and kitchen use six direct approved files',exactExampleImages.every(name=>samples.includes(`assets/demo-workthroughs/${name}`))&&!samples.includes('at.adobe.com')&&!samples.includes('background-image'));
check('cabin example includes plan and finished concept',samples.includes('cabin-plan-sheet.png')&&samples.includes('cabin-exterior-render.png')&&samples.includes('cabin-project-complete.html'));
const universalRedirect=read('universal-quote-builder.html');
const universalViewer=read('universal-quote-builder-example.html');
const publicDataSource=read('assets/js/uqb-public-examples.js');
const publicDemo=read('apps-script/business-office/BusinessOffice_UniversalQuoteBuilder_PublicDemo.gs');
const publicExamples=read('apps-script/business-office/BusinessOffice_UniversalQuoteBuilder_PublicExamples.gs');
const publicRoute=read('apps-script/business-office/ZZZ_BusinessOffice_UniversalQuoteBuilder_PublicRoute.gs');
const publicContext={window:{}};
vm.createContext(publicContext);
try{vm.runInContext(publicDataSource,publicContext,{filename:'assets/js/uqb-public-examples.js'});}catch(error){check('static public UQB dataset parses',false,error.message);}
const publicData=publicContext.window.H38_UQB_PUBLIC_EXAMPLES;
const packages=publicData&&Array.isArray(publicData.packages)?publicData.packages:[];
const drawings=publicData&&publicData.drawings?Object.keys(publicData.drawings):[];
const assigned=packages.flatMap(item=>item.sheets||[]);
check('Universal Quote Builder demonstrates matched public packages',samples.includes('Universal Quote Builder overview')&&samples.includes('Complete quote examples matched to their CAD drawings')&&packages.length===7&&drawings.length===10&&assigned.length===10&&new Set(assigned).size===10);
check('Universal demonstration provides all three package actions',samples.includes('View full quote')&&samples.includes('View full-size CAD sheets')&&samples.includes('Print / save complete package')&&universalViewer.includes('Print / Save PDF'));
check('Universal demonstration preserves public-only safety boundary',samples.includes('Public examples only:')&&samples.includes('Demonstration control:')&&!samples.includes('script.google.com')&&!samples.includes('<iframe')&&publicExamples.includes('No live customers, private Highway 38 records')&&publicExamples.includes("quote['Customer Visible']==='Yes'")&&publicDemo.includes('externalActionsPerformed:false'));
check('Universal compatibility route redirects to embedded house section',universalRedirect.includes('sample-library-now.html#universal-quote-builder-examples'));
check('Universal demonstration assigns seven packages and ten drawings exactly once',packages.length===7&&drawings.length===10&&assigned.length===10&&new Set(assigned).size===10);
check('Universal demonstration removes the result-board and renovation tangents',!samples.includes('What Office creates')&&!samples.includes('What Quote Builder produced')&&!samples.includes('Whole-House Renovation and Property Improvement')&&!samples.includes('$342,815'));
check('Office demo defines complete records',(publicDemo.match(/Object\.freeze\(\{n:'\d{2}',key:/g)||[]).length===14&&(publicDemo.match(/Object\.freeze\(\{n:'(?:G|A|M|P|E|C-S-L)-/g)||[]).length===10&&publicRoute.includes('boRenderUniversalPublicExamples_'));
const request=read('start-request.html');
const requestFlow=read('request-flow.js');
check('request page keeps three-step secure intake',[1,2,3].every(step=>request.includes(`data-request-step="${step}"`))&&request.includes('id="request-submit"')&&request.includes('data-intake-endpoint='));
check('request page preserves no-charge and Owner review language',/No charge|no-charge/i.test(request)&&/owner review/i.test(request));
check('request uses approved offer selector',request.includes('pricing-data.js')&&request.includes('id="offer"')&&!request.includes('id="bundle"'));
check('request page preserves draft recovery and email fallback',requestFlow.includes('H38Platform.saveDraft')&&requestFlow.includes('H38Platform.loadDraft')&&requestFlow.includes('emailFallback'));
const portal=read('portal.html');
check('public Owner gateway opens one unified Business Office',portal.includes('Opening Highway 38 Business Office')&&portal.includes('location.replace(target)')&&!/<iframe\b/i.test(portal));
const context={window:{}};
vm.createContext(context);
vm.runInContext(read('catalog-data.js'),context,{filename:'catalog-data.js'});
const catalog=context.window.H38_CATALOG;
check('retired catalog compatibility data remains complete',catalog&&catalog.products.length===15&&catalog.bundles.length===9);
if(catalog){const productIds=catalog.products.map(item=>item.id),bundleIds=catalog.bundles.map(item=>item.id);check('compatibility product and bundle IDs remain unique',new Set(productIds).size===productIds.length&&new Set(bundleIds).size===bundleIds.length);check('compatibility bundles reference existing products',catalog.bundles.every(bundle=>(bundle.products||[]).every(id=>productIds.includes(id))));}
const formBuilder=read('apps-script/commercial-intake/FormBuilder.gs');
check('commercial form builder performs no automatic external action',!/createTrigger|newTrigger|sendEmail|GmailApp|MailApp|UrlFetchApp/.test(formBuilder));
check('commercial form builder preserves Owner review boundary',formBuilder.includes('OWNER REVIEW REQUIRED BEFORE LINK REPLACEMENT'));
const activeText=expectedPrimary.map(read).join('\n')+'\n'+shell+'\n'+samples+'\n'+universalRedirect+'\n'+universalViewer+'\n'+publicDataSource;
check('public source contains no private owner email or committed secrets',!/rkrueth@gmail\.com|AIza[0-9A-Za-z_-]{20,}|-----BEGIN (?:RSA |EC |OPENSSH )?PRIVATE KEY-----|Bearer\s+[A-Za-z0-9._-]{20,}/i.test(activeText));
check('prohibited CNC quantity claim remains absent',!/25,000\+\s*(?:CNC\s+)?programs?/i.test(activeText));
const result={status:failures.length?'HOLD':'PASS',generatedAt:new Date().toISOString(),passed:passes.length,failed:failures.length,architecture:'project-first-embedded-public-quote-cad-packages',pricingProducts:3,existingProjectExamples:existingExampleCount,universalDemonstration:true,publicPlacement:'directly beneath whole-building house example',officeGeneratedDemo:true,publicMatchedPackages:packages.length,publicCadSheets:drawings.length,catalogCompatibilityOnly:true,passes,failures};
const out=path.join(root,'artifacts','commercial-system');fs.mkdirSync(out,{recursive:true});fs.writeFileSync(path.join(out,'verification.json'),JSON.stringify(result,null,2)+'\n');
if(failures.length){console.error(JSON.stringify(result,null,2));process.exit(1);}console.log(JSON.stringify(result,null,2));
