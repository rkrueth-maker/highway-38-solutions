#!/usr/bin/env node
'use strict';
const fs=require('fs'),path=require('path'),vm=require('vm'),cp=require('child_process');
const root=path.resolve(__dirname,'..'),read=rel=>fs.readFileSync(path.join(root,rel),'utf8'),failures=[];
const check=(name,value)=>{if(!value)failures.push(name);};
const files={
  server:'apps-script/commercial-office-beta/CommercialBeta_CompletionStartup_01.gs',web:'apps-script/commercial-office-beta/CommercialBeta_Web.gs',office:'apps-script/commercial-office-beta/CommercialBeta_Office.html',manifest:'apps-script/commercial-office-beta/appsscript.json',
  launcher:'open-business-office.html',index:'commercial-app/index.html',db:'commercial-app/db.js',bridge:'commercial-app/bridge.js',startup:'commercial-app/startup-fix.js',worker:'commercial-app/service-worker.js',
  browser:'scripts/verify-commercial-browser-signin.js',public:'scripts/verify-commercial-public-shell.js',deployed:'scripts/verify-commercial-webapp-startup.js',runner:'scripts/run-commercial-webapp-startup-acceptance.sh',workflow:'.github/workflows/commercial-google-native-beta.yml'
};
for(const rel of Object.values(files))check(`file ${rel}`,fs.existsSync(path.join(root,rel)));
if(failures.length){console.error(JSON.stringify({status:'FAIL',failures},null,2));process.exit(1);}
const source=Object.fromEntries(Object.entries(files).map(([key,rel])=>[key,read(rel)]));
for(const key of ['db','bridge','startup','worker','browser','public','deployed']){const result=cp.spawnSync(process.execPath,['--check',path.join(root,files[key])],{encoding:'utf8'});check(`syntax ${files[key]}`,result.status===0);}
for(const key of ['server','web']){try{new vm.Script(source[key],{filename:files[key]});}catch(error){check(`syntax ${files[key]}`,false);}}
const manifest=JSON.parse(source.manifest);
check('fast authorized startup',source.server.includes('function cbStartupBootstrap(')&&source.server.includes("startupMode:'FAST'")&&source.server.includes('fullRefreshPending:true')&&!source.server.includes('cbCompletionContext_('));
check('execution handoff includes token and startup',source.server.includes('function cbPwaExecutionHandoff(')&&source.server.includes('ScriptApp.getOAuthToken()')&&source.server.includes("handoffType:'H38_EXECUTION_HANDOFF'")&&source.server.includes('apiDeploymentId:deploymentId')&&source.server.includes('startup:startup'));
check('Office handoff requires a user-activated same-tab return',source.office.includes('cbPwaExecutionHandoff')&&source.office.includes("target.hash='h38='")&&source.office.includes('id="continueButton"')&&source.office.includes('target="_top"')&&source.office.includes("button.textContent='Open Business Office'")&&source.office.includes("button.dataset.ready='true'")&&source.office.includes('button.focus')&&source.office.includes('No second window is used.')&&!source.office.includes('window.open(')&&!source.office.includes('officeFrame'));
check('Office opens before full refresh',source.startup.indexOf('openPage(state.page,false)')<source.startup.indexOf("state.bridge.request('fullStartupRefresh'"));
check('owner-only deployed acceptance',source.web.includes("parameters.acceptance)==='startup'")&&source.server.includes('cbRequireOwner_()'));
check('same-tab launcher clears stale browser state',source.launcher.includes('serviceWorker.getRegistrations')&&source.launcher.includes('registration.unregister()')&&source.launcher.includes('caches.keys()')&&source.launcher.includes("sessionStorage.removeItem('h38-execution-session-v1')")&&source.launcher.includes('window.location.replace(destination)'));
check('support runtimes are privately scoped',source.db.includes('(()=>{')&&source.db.includes('window.H38DB=')&&source.bridge.includes('(()=>{')&&source.bridge.includes('window.H38Bridge=H38Bridge'));
check('fragment handoff is consumed and removed',source.bridge.includes('consumeHashHandoff')&&source.bridge.includes("get('h38')")&&source.bridge.includes('history.replaceState')&&source.bridge.includes("handoff.handoffType==='H38_EXECUTION_HANDOFF'"));
check('execution token stays in session storage',source.bridge.includes("const SESSION_KEY='h38-execution-session-v1'")&&source.bridge.includes('sessionStorage.setItem(SESSION_KEY')&&!source.bridge.includes('localStorage.setItem(SESSION_KEY'));
check('Google execution API carries all requests',source.bridge.includes('https://script.googleapis.com/v1/scripts/')&&source.bridge.includes('authorization:`Bearer ${this.session.accessToken}`')&&source.bridge.includes("function:'cbApi'")&&source.bridge.includes('parameters:[{action,args:args||{}}]'));
check('persistent authentication window is removed',!source.bridge.includes("'popup'")&&!source.bridge.includes('BroadcastChannel')&&!source.index.includes('window.open(')&&!source.startup.includes('window.open(')&&!source.office.includes('window.open('));
check('legacy worker retires itself',source.worker.includes("const RETIRED_BUILD='20260803-1530'")&&source.worker.includes('self.registration.unregister()')&&!source.worker.includes('caches.open('));
check('browser gate requires authorization click, top-level return and execution API',source.browser.includes('clickAuthorizedOfficeButton')&&source.browser.includes("#continueButton")&&source.browser.includes('authorizationContinueClick:true')&&source.browser.includes('BROWSER_AUTHORIZED_TOP_LEVEL_EXECUTION_OFFICE')&&source.browser.includes('executionApiBusinessCount')&&source.browser.includes('secondaryPageCount:0')&&source.browser.includes('officeRuntimeErrors:0'));
check('browser gate uses protected credential only for Apps Script authorization',source.browser.includes('isScriptHost(parsed.hostname)')&&source.browser.includes('authorization:`Bearer ${accessToken}`')&&source.runner.includes('verify-commercial-browser-signin.js "$PUBLIC_URL" "$DEPLOYMENT_URL" "$CREDENTIALS_PATH"'));
check('live public verifier requires execution handoff build',source.public.includes('PUBLIC_HIGHWAY38_TOP_LEVEL_EXECUTION_ENTRY')&&source.public.includes('executionApiTransport')&&source.public.includes('persistentAuthWindow:false'));
check('existing deployment remains pinned',source.runner.includes('AKfycbyY8cbfvGLzllw7rMhRY46wx_eIKhsK5oLlV6vIcDxDIKuCzX0_oTi4EyVufSxonLdxow'));
check('web app and execution API are deployed',manifest.webapp&&manifest.webapp.executeAs==='USER_ACCESSING'&&manifest.executionApi&&manifest.executionApi.access==='ANYONE');
check('workflow uses authorized browser acceptance',source.workflow.includes('playwright@1.55.0')&&source.workflow.includes('run-commercial-webapp-startup-acceptance.sh'));
const session={accessToken:'TOKEN',apiDeploymentId:'DEPLOYMENT',scriptId:'SCRIPT',issuedAt:new Date().toISOString(),refreshAfterMs:600000,startup:{status:'PASS',businesses:[{businessId:'BUS-1'}],selectedBusinessId:'BUS-1',snapshot:{business:{businessId:'BUS-1'},user:{roleName:'Owner'}}}};
const context={window:null,location:{hash:'',pathname:'/commercial-app/',search:'',assign(){}},history:{replaceState(){}},sessionStorage:{getItem(){return JSON.stringify(session);},setItem(){},removeItem(){}},atob,TextDecoder,URLSearchParams,fetch:async()=>({ok:true,status:200,json:async()=>({done:true,response:{result:{status:'PASS'}}})}),AbortController,setTimeout,clearTimeout,queueMicrotask:fn=>fn(),Error,console};context.window=context;
try{vm.runInNewContext(source.db,{window:{},indexedDB:{open(){return{};}},crypto:{randomUUID:()=> 'ID'},setTimeout,clearTimeout,Promise,Error},{filename:'db.js'});vm.runInNewContext(source.bridge,context,{filename:'bridge.js'});vm.runInNewContext("const H38Bridge=window.H38Bridge;const state={};function toast(){}",context,{filename:'app-global-contract.js'});check('support helpers do not collide with Office globals',true);}catch(error){check('support helpers do not collide with Office globals',false);}
let bootstrapped=false;const instance=new context.H38Bridge(null,'',()=>{},startup=>{bootstrapped=startup.selectedBusinessId==='BUS-1';},()=>{},()=>{});instance.connect();check('stored execution handoff bootstraps top-level Office',bootstrapped&&instance.bootstrapped&&instance.ready&&instance.transport==='execution-api');
const output={status:failures.length?'FAIL':'PASS',checks:19,failures,fastSignedInStartup:true,globalRuntimeIsolated:true,topLevelOffice:true,userActivatedReturn:true,executionApiTransport:true,persistentAuthWindow:false,ownerOnlyBusinessSwitcher:true,officeBeforeFullRefresh:true,publicDomainEntry:true,browserExecutionAcceptance:true,deployedWebAppAcceptance:true};
if(failures.length){console.error(JSON.stringify(output,null,2));process.exit(1);}console.log(JSON.stringify(output,null,2));
